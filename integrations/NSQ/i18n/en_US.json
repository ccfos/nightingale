{
    "1) 从告警标签取 topic 与 channel，去消费端应用日志里找处理耗时超过 msg-timeout 的记录；\n2) 若处理确实需要更久，在消费端对长任务周期性调用 Touch() 续期，或调大 nsqd 的 --msg-timeout；\n3) 若是下游依赖（数据库、第三方接口）变慢拖垮处理，先修下游并在消费端加超时与熔断；\n4) 确认业务逻辑幂等，否则重投递会造成重复写入。": "1) Take topic and channel from the alert labels and look in the consumer application logs for handlers exceeding msg-timeout;\n2) If processing genuinely needs longer, call Touch() periodically on long tasks in the consumer, or raise --msg-timeout on nsqd;\n3) If a slow downstream dependency (database, third-party API) is dragging processing down, fix the dependency first and add timeouts and circuit breaking in the consumer;\n4) Make sure the business logic is idempotent, otherwise redelivery will cause duplicate writes.",
    "1) 从告警标签取 topic 与 channel，执行 curl -s http://\u003cnsqd\u003e:4151/stats?format=json 查看该 channel 的 client_count 与 in_flight_count；\n2) client_count 正常但 in_flight 长期打满，说明单条消息处理慢，去消费端应用日志与链路追踪里定位慢在哪一步；\n3) 处理逻辑本身慢则扩容消费者实例数，或调大消费端的 max_in_flight；\n4) 若积压由一次性流量洪峰造成，评估后可用 curl -X POST 'http://\u003cnsqd\u003e:4151/channel/empty?topic=..\u0026channel=..' 清空，注意会丢消息，须先确认业务可接受。": "1) Take topic and channel from the alert labels and run curl -s http://\u003cnsqd\u003e:4151/stats?format=json to inspect client_count and in_flight_count for that channel;\n2) If client_count looks normal but in_flight stays at its ceiling, individual messages are slow to process; go to the consumer application logs and tracing to find the slow step;\n3) If the processing logic itself is slow, scale out consumer instances or raise max_in_flight on the consumer;\n4) If the backlog came from a one-off traffic spike, you may run curl -X POST 'http://\u003cnsqd\u003e:4151/channel/empty?topic=..\u0026channel=..' after evaluating the impact, but note this drops messages and must be confirmed with the business first.",
    "1) 从告警标签取 topic 与 channel，确认对应的消费端服务是否已全部下线或崩溃；\n2) 消费端在跑则检查它到 nsqlookupd/nsqd 的连接（netstat -anp | grep 4150）与消费端日志里的连接报错；\n3) 若是发布后消费者版本换了 channel 名，导致老 channel 变成孤儿，确认无用后执行 curl -X POST 'http://\u003cnsqd\u003e:4151/channel/delete?topic=..\u0026channel=..' 删除；\n4) 消费端确实需要恢复的，拉起后观察 depth 是否开始下降。": "1) Take topic and channel from the alert labels and confirm whether the consuming service has been fully taken offline or has crashed;\n2) If the consumer is running, check its connections to nsqlookupd/nsqd (netstat -anp | grep 4150) and look for connection errors in the consumer logs;\n3) If a release changed the channel name and left the old channel orphaned, confirm it is unused and delete it with curl -X POST 'http://\u003cnsqd\u003e:4151/channel/delete?topic=..\u0026channel=..';\n4) If the consumer does need to come back, start it and watch depth begin to fall.",
    "1) 从消费端日志统计失败原因，区分是业务校验失败（不应重试）还是依赖抖动（应重试）；\n2) 业务校验失败的应改为 FIN 掉并落死信存储，而不是无限 REQ；\n3) 依赖抖动的检查重试退避是否设置合理，避免固定间隔重试形成风暴；\n4) 结合 nsq_channel_timeout_count 一起看，若两者同时高说明消费端整体已经过载，需要先扩容。": "1) Aggregate the failure reasons from the consumer logs and separate business validation failures (which should not be retried) from dependency flapping (which should);\n2) Validation failures should be FIN-ed and written to a dead-letter store rather than requeued forever;\n3) For dependency flapping, check that retry backoff is configured sensibly, since fixed-interval retries create storms;\n4) Read this together with nsq_channel_timeout_count: if both are high the consumer is simply overloaded and needs to be scaled out first.",
    "1) 执行 curl -s http://\u003cnsqd\u003e:4151/stats?format=json 对比 depth 与 backend_depth，确认落盘规模；\n2) 落盘的根因一般还是消费慢，优先按积压问题扩容消费者或优化消费逻辑；\n3) 检查 nsqd 数据目录（--data-path）所在磁盘剩余空间，用 df -h 确认不会被写满；\n4) 若是可预期的短时洪峰，可调大 --mem-queue-size 换取内存缓冲，但要同步评估 nsqd 进程内存上限。": "1) Run curl -s http://\u003cnsqd\u003e:4151/stats?format=json and compare depth with backend_depth to size the spill;\n2) The root cause is usually still slow consumption, so scale out consumers or optimise consumption logic as for a backlog;\n3) Check the free space on the disk holding the nsqd data directory (--data-path) with df -h so it cannot fill up;\n4) For an expected short spike you can raise --mem-queue-size to buy memory buffering, but re-check the nsqd process memory ceiling at the same time.",
    "NSQ Channel 无消费者但有积压": "NSQ channel has no consumer but has a backlog",
    "NSQ Channel 消息溢出到磁盘": "NSQ channel overflowing to disk",
    "NSQ Channel 消息积压": "NSQ channel backlog",
    "NSQ 消息处理超时激增": "NSQ message processing timeouts spiking",
    "NSQ 消息重新入队激增": "NSQ message requeues spiking"
}
