{
    "1) 从 member 标签取节点名，执行 consul members 确认它当前的状态；\n2) Failed 状态先确认主机是否存活、consul 进程是否在跑；\n3) 主机正常则检查 8301 端口 TCP/UDP 双向连通与 MTU（gossip 依赖 UDP，MTU 不一致会导致间歇失联）；\n4) 节点确实已下线则执行 consul force-leave \u003cnode\u003e，Left 状态的残留条目 72 小时后会自动清理。": "1) Take the node name from the member label and run consul members to confirm its current state;\n2) For Failed, first check that the host is up and the consul process is running;\n3) If the host is fine, check bidirectional TCP/UDP connectivity on port 8301 and the MTU (gossip relies on UDP, and an MTU mismatch causes intermittent loss of contact);\n4) If the node really is decommissioned, run consul force-leave \u003cnode\u003e; entries left in the Left state are cleaned up automatically after 72 hours.",
    "1) 从告警标签取 node 与 service_name，执行 consul catalog nodes -service=\u003cservice_name\u003e 确认还剩几个健康实例，只剩一个时立即扩容；\n2) 登录 node 执行 curl -s http://127.0.0.1:8500/v1/health/checks/\u003cservice_name\u003e 看 Output 字段里的具体失败原因；\n3) 按 Output 排查对应进程与端口（检查脚本超时、HTTP 返回码不符、TCP 拒绝连接）；\n4) 若是检查本身配置有误（超时过短、路径写错），修 service 定义后执行 consul reload。": "1) Take node and service_name from the alert labels and run consul catalog nodes -service=\u003cservice_name\u003e to see how many healthy instances remain; scale out immediately if only one is left;\n2) Log in to the node and run curl -s http://127.0.0.1:8500/v1/health/checks/\u003cservice_name\u003e to read the exact failure reason from the Output field;\n3) Follow that Output to the matching process and port (check script timeout, unexpected HTTP status, TCP connection refused);\n4) If the check itself is misconfigured (timeout too short, wrong path), fix the service definition and run consul reload.",
    "1) 从告警标签取 node，执行 consul members 确认该节点的 Serf 状态是 alive 还是 failed；\n2) failed 说明 gossip 不通，检查 8301（LAN）端口的双向连通性与安全组，以及主机是否已宕机；\n3) 节点正常但 serfHealth 反复抖动，通常是网络丢包或主机负载过高导致 gossip 超时，用 mtr 与 uptime 确认；\n4) 节点已确认下线则执行 consul force-leave \u003cnode\u003e 清理，避免残留条目持续告警。": "1) Take node from the alert labels and run consul members to see whether the node's Serf state is alive or failed;\n2) failed means gossip is not getting through: check bidirectional connectivity on port 8301 and security groups, and whether the host is down;\n3) If the node is up but serfHealth flaps repeatedly, it is usually packet loss or high host load causing gossip timeouts; confirm with mtr and uptime;\n4) Once the node is confirmed decommissioned, clean it up with consul force-leave \u003cnode\u003e so the stale entry stops alerting.",
    "1) 在每个 server 节点执行 consul operator raft list-peers，确认存活的 server 数是否已不足多数派；\n2) 存活数不足时优先把宕掉的 server 拉起来恢复仲裁，不要急于做 peers.json 恢复；\n3) 存活数够但仍选不出主，检查 server 之间 8300/8301 端口连通性与时钟偏差（chronyc tracking）；\n4) 确认无法自动恢复时，再按官方 outage recovery 流程用 raft/peers.json 手工重建 peer 列表。": "1) Run consul operator raft list-peers on each server node and confirm whether the number of surviving servers has fallen below a quorum;\n2) If quorum is lost, bring the failed servers back up first rather than jumping to a peers.json recovery;\n3) If enough servers survive but no leader is elected, check connectivity on ports 8300/8301 between servers and clock skew (chronyc tracking);\n4) Only once automatic recovery is ruled out should you rebuild the peer list by hand with raft/peers.json following the official outage recovery procedure.",
    "1) 执行 consul operator autopilot state 查看每个 server 的 Healthy 与 LastContact；\n2) 对 Healthy=false 的节点检查进程存活与到 Leader 的网络延迟；\n3) LastIndex 落后很多说明该节点在追日志，观察是否持续收敛，磁盘 IO 慢是常见原因（用 iostat -x 1 确认）；\n4) 节点确认报废时执行 consul force-leave \u003cnode\u003e 或 consul operator raft remove-peer 把它移出集群。": "1) Run consul operator autopilot state and check Healthy and LastContact for every server;\n2) For nodes with Healthy=false, check that the process is alive and measure network latency to the leader;\n3) A LastIndex far behind means the node is still catching up on logs; watch whether it keeps converging, and note that slow disk IO is a common cause (confirm with iostat -x 1);\n4) Once a node is confirmed dead, remove it from the cluster with consul force-leave \u003cnode\u003e or consul operator raft remove-peer.",
    "1) 执行 consul operator raft list-peers 统计当前健康 server 数（3 节点集群容错 1，5 节点容错 2）；\n2) 有节点异常则优先修复该节点而不是新增节点，避免 peer 数变成偶数；\n3) 确实需要扩容时按奇数扩到 5 台，并确认新节点 raft 追平后容错度恢复；\n4) 修复期间避免做任何滚动重启或版本升级操作。": "1) Run consul operator raft list-peers and count the healthy servers (a 3-node cluster tolerates 1 failure, a 5-node cluster tolerates 2);\n2) If a node is unhealthy, repair that node rather than adding a new one, so the peer count does not become even;\n3) When you genuinely need to grow, scale to an odd number such as 5 and confirm fault tolerance recovers once the new node has caught up on raft;\n4) Avoid any rolling restart or version upgrade until this is resolved.",
    "1) 登录主机执行 systemctl status consul 与 consul members 确认进程与集群视图；\n2) 进程存活则执行 curl -s http://127.0.0.1:8500/v1/status/leader 验证 API 是否可用，403 说明采集用的 ACL Token 权限不足或已过期；\n3) 进程不在则查看 journalctl -u consul 定位退出原因（常见为 raft 数据目录损坏、端口占用）；\n4) 恢复前先确认其余节点仍构成多数派，避免误操作导致集群失去仲裁。": "1) Log in to the host and run systemctl status consul and consul members to check the process and the cluster view;\n2) If the process is alive, run curl -s http://127.0.0.1:8500/v1/status/leader to verify the API; a 403 means the ACL token used for collection lacks permission or has expired;\n3) If the process is gone, check journalctl -u consul for the cause (a corrupted raft data directory or a port conflict are common);\n4) Before recovering, confirm the remaining nodes still form a quorum so you do not accidentally push the cluster out of quorum.",
    "Consul Autopilot 判定集群不健康": "Consul Autopilot reports the cluster unhealthy",
    "Consul 实例不可达": "Consul instance unreachable",
    "Consul 服务健康检查 critical": "Consul service health check critical",
    "Consul 节点健康检查 critical": "Consul node health check critical",
    "Consul 集群容错度归零": "Consul cluster fault tolerance is zero",
    "Consul 集群成员失联": "Consul cluster member not alive",
    "Consul 集群没有 Leader": "Consul cluster has no leader",
    "categraf 对同一条检查会按 passing/warning/critical/maintenance 各推一个同名同标签的样本，落库后数值会互相覆盖，因此这里用 status 标签的存在性判断而不是比较数值。": "categraf pushes one sample per status (passing/warning/critical/maintenance) with an identical metric name and label set, so the values overwrite each other once stored. This rule therefore tests for the presence of the status label rather than comparing values."
}
