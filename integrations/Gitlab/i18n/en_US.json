{
    "1) 从告警标签取 job/instance 定位是哪个组件（gitaly/puma/sidekiq/registry 等）；\n2) 在对应节点用 gitlab-ctl status 确认进程状态；\n3) 用 gitlab-ctl tail \u003cservice\u003e 看日志定位退出原因；\n4) 拉起后用 gitlab-rake gitlab:check 做一次整体健康检查。": "1) Take job/instance from the alert labels to identify the component (gitaly/puma/sidekiq/registry, etc.);\n2) On that node run gitlab-ctl status to check the process state;\n3) Use gitlab-ctl tail \u003cservice\u003e to find the exit reason in the logs;\n4) After restarting, run gitlab-rake gitlab:check for an overall health check.",
    "1) 从告警标签取 name 确认是哪个队列积压；\n2) 在 GitLab 管理后台的 Background Jobs 页或用 gitlab-rails runner 看队列深度与失败任务；\n3) 确认 Sidekiq 进程是否存活、并发数配置是否偏小（sidekiq['max_concurrency']）；\n4) 大量任务失败重试导致的，先定位失败根因（Redis/PG 异常、外部依赖超时）再清理。": "1) Take name from the alert labels to identify the backed-up queue;\n2) Check queue depth and failed jobs in the GitLab admin Background Jobs page or via gitlab-rails runner;\n3) Confirm the Sidekiq process is alive and that concurrency is not set too low (sidekiq['max_concurrency']);\n4) If it is caused by mass job failures and retries, find the failure root cause (Redis/PG problems, external timeouts) before clearing them.",
    "1) 在 GitLab 节点用 gitlab-ctl status 确认 puma 进程状态，看 /var/log/gitlab/puma/ 下日志有无超时；\n2) 结合 PumaQueueing 告警判断排队深度；\n3) 短期可调大 puma['worker_processes'] 与 max_threads 后 gitlab-ctl reconfigure；\n4) 排查是否有慢请求（大 MR diff、大仓库页面）长时间占住 worker，必要时限流。": "1) On the GitLab node run gitlab-ctl status to check the puma process and look for timeouts in /var/log/gitlab/puma/;\n2) Combine with the PumaQueueing alert to judge the queue depth;\n3) Short term, raise puma['worker_processes'] and max_threads then run gitlab-ctl reconfigure;\n4) Look for slow requests (large MR diffs, big repository pages) holding workers for a long time and rate limit them if needed.",
    "1) 在 Gitaly 节点看日志确认是哪类 RPC 被限流（gitaly_concurrency_limiting 相关）；\n2) 用 gitlab-ctl status 与节点 CPU/IO 确认 Gitaly 是否已过载；\n3) 定位是否有超大仓库或异常客户端在反复拉取，必要时临时限制该来源；\n4) 容量确实不足则调大 Gitaly 的并发限制并扩容节点。": "1) On the Gitaly node, check the logs to see which RPCs are being limited (gitaly_concurrency_limiting related);\n2) Use gitlab-ctl status plus node CPU/IO to confirm Gitaly is overloaded;\n3) Determine whether an oversized repository or a misbehaving client is pulling repeatedly, and throttle that source temporarily;\n4) If capacity is genuinely short, raise Gitaly's concurrency limits and add nodes.",
    "1) 在 PG 上确认 log_lock_waits=on，从日志里取死锁双方的完整 SQL；\n2) 关联 pg_locks 与 pg_stat_activity 确认是哪两张表互相等待；\n3) GitLab 场景常见于 CI 并发更新同一批记录——降低并发或升级 GitLab 版本（上游常修此类问题）；\n4) 检查是否有长事务放大了死锁概率。": "1) On PostgreSQL confirm log_lock_waits=on and take the full SQL of both deadlock participants from the log;\n2) Join pg_locks with pg_stat_activity to identify which two tables are waiting on each other;\n3) In GitLab this commonly comes from CI concurrently updating the same records — reduce concurrency or upgrade GitLab (upstream frequently fixes these);\n4) Check whether a long transaction is amplifying the deadlock probability.",
    "1) 用 gitlab-ctl tail gitlab-workhorse 看具体报错与出错的路由；\n2) 常见根因是后端 puma 超时或 Gitaly 不可用——先确认这两者健康；\n3) 大文件相关的检查磁盘空间与临时目录权限；\n4) 若与近期升级时间吻合，优先回滚。": "1) Run gitlab-ctl tail gitlab-workhorse to see the exact errors and the failing routes;\n2) The usual root cause is a puma timeout or Gitaly being unavailable — confirm both are healthy first;\n3) For large-file issues, check disk space and temporary directory permissions;\n4) If it lines up with a recent upgrade, roll back first.",
    "1) 登录 PG 节点确认进程存活（gitlab-ctl status postgresql 或 systemctl status postgresql）；\n2) 进程存活则用 psql 手工连一次，区分实例夯住与网络/pg_hba 问题；\n3) 进程不在则看 PG 日志尾部定位（磁盘满、WAL 目录写满、OOM）再拉起；\n4) 有备库的评估是否需要切换。": "1) Log in to the PostgreSQL node and confirm the process is alive (gitlab-ctl status postgresql or systemctl status postgresql);\n2) If it is alive, connect by hand with psql to tell a hung instance from a network or pg_hba problem;\n3) If it is not running, read the tail of the PG log (full disk, full WAL directory, OOM) before starting it;\n4) If a standby exists, assess whether to fail over.",
    "1) 登录 Redis 节点用 gitlab-ctl status redis 或 systemctl status redis 确认进程；\n2) 进程存活则 redis-cli ping 手工验证，区分夯住与网络/密码问题；\n3) 进程不在则看日志定位（OOM、磁盘满导致 RDB 写失败）再拉起；\n4) 恢复后确认 Sidekiq 队列是否积压，必要时观察消费进度。": "1) Log in to the Redis node and check the process with gitlab-ctl status redis or systemctl status redis;\n2) If it is alive, verify with redis-cli ping to tell a hang from a network or password problem;\n3) If it is not running, check the logs (OOM, or a full disk breaking the RDB write) before starting it;\n4) After recovery, check whether the Sidekiq queues are backed up and watch them drain.",
    "1) 结合 HighPumaUtilization 确认 worker 利用率；\n2) 看 /var/log/gitlab/puma/puma_stdout.log 与 gitlab-rails 日志找耗时最长的请求路径；\n3) 短期调大 puma['worker_processes'] 后 gitlab-ctl reconfigure；\n4) 长期排查慢请求根因（大仓库、大 MR、未加索引的查询），必要时扩容节点。": "1) Combine with HighPumaUtilization to confirm worker utilization;\n2) Check /var/log/gitlab/puma/puma_stdout.log and the gitlab-rails log for the slowest request paths;\n3) Short term, raise puma['worker_processes'] and run gitlab-ctl reconfigure;\n4) Longer term, find the root cause of the slow requests (large repositories, large MRs, unindexed queries) and add nodes if needed."
}
