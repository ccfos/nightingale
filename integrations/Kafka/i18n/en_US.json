{
    "1) 从告警标签取 consumergroup 与 topic，执行 kafka-consumer-groups.sh --describe --group \u003cgroup\u003e --bootstrap-server \u003cbroker\u003e 看各分区 LAG 分布；\n2) 积压集中在个别分区说明数据倾斜，检查生产端分区键；\n3) 均匀积压则是消费者处理慢——先看消费端日志有无异常重试，再考虑扩consumer 实例（不超过分区数）或提高单条处理效率；\n4) 确认消费者没有全部掉线（MEMBERS 为 0 要先把消费者拉起来）。": "1) Take consumergroup and topic from the alert labels and run kafka-consumer-groups.sh --describe --group \u003cgroup\u003e --bootstrap-server \u003cbroker\u003e to see the LAG distribution per partition;\n2) Lag concentrated on a few partitions means data skew — check the partition key on the producer side;\n3) Evenly spread lag means consumers are slow: first check the consumer logs for abnormal retries, then scale out consumer instances (never beyond the partition count) or make per-message processing faster;\n4) Confirm the consumers have not all dropped out (MEMBERS at 0 means you must start the consumers first).",
    "1) 从告警标签取 topic，执行 kafka-topics.sh --describe --topic \u003ctopic\u003e --bootstrap-server \u003cbroker\u003e，对比 Isr 与 Replicas 找出缺失的副本所在 broker；\n2) 到该 broker 看 server.log 是否有磁盘满、GC 卡顿或网络超时；\n3) broker 已下线则先恢复它，副本会自动追平；\n4) 长时间追不平可用 kafka-reassign-partitions.sh 把副本迁到健康 broker。": "1) Take topic from the alert labels and run kafka-topics.sh --describe --topic \u003ctopic\u003e --bootstrap-server \u003cbroker\u003e, comparing Isr against Replicas to find which broker holds the missing replica;\n2) Check that broker's server.log for a full disk, GC pauses or network timeouts;\n3) If the broker is down, bring it back and the replicas will catch up automatically;\n4) If it cannot catch up for a long time, use kafka-reassign-partitions.sh to move the replica to a healthy broker.",
    "1) 执行 kafka-broker-api-versions.sh --bootstrap-server \u003cbroker\u003e 确认当前在线 broker 列表，对比配置找出缺失的节点；\n2) 到该节点确认进程是否存活、server.log 里是否有 OOM 或磁盘错误；\n3) 拉起后用 kafka-topics.sh --describe --under-replicated-partitions 确认副本已追平；\n4) 若节点短期修不好，用 kafka-reassign-partitions.sh 把其上的分区迁走以恢复冗余。": "1) Run kafka-broker-api-versions.sh --bootstrap-server \u003cbroker\u003e to list the currently online brokers and compare against the configuration to find the missing node;\n2) On that node, confirm the process is alive and check server.log for OOM or disk errors;\n3) After bringing it back, run kafka-topics.sh --describe --under-replicated-partitions to confirm the replicas caught up;\n4) If the node cannot be fixed soon, use kafka-reassign-partitions.sh to move its partitions away and restore redundancy.",
    "kafka broker 数量下降": "Kafka broker count dropped",
    "kafka 分区副本未同步": "Kafka partitions are under-replicated",
    "kafka 消费组积压超过 1 万条": "Kafka consumer group lag exceeds 10000 messages",
    "副本不同步预案\n1. Restart the Zookeeper leader.\n2. Restart the broker\\brokers that are not replicating some of the partitions.": "Runbook for out-of-sync replicas\n1. Restart the Zookeeper leader.\n2. Restart the broker\\brokers that are not replicating some of the partitions.",
    "文档": "Documentation"
}
