{
    "1) 从告警标签取 upstream 与 name（后端地址），在 Nginx 所在主机执行 curl -sv http://\u003c后端地址\u003e/ 确认是否真的不通；\n2) 后端不通则登录该后端主机确认应用进程与端口存活；\n3) 后端正常则检查 Nginx 到后端的网络与安全组，以及 check 指令配置的探测路径是否与后端健康检查接口一致；\n4) 用 curl http://\u003cnginx\u003e/status?format=json 确认同一 upstream 还剩几个可用节点，只剩一个时立即扩容。": "1) Take upstream and name (the backend address) from the alert labels and run curl -sv http://\u003cbackend address\u003e/ on the Nginx host to confirm it is genuinely unreachable;\n2) If it is unreachable, log in to that backend host and confirm the application process and port are alive;\n3) If the backend is healthy, check the network and security groups between Nginx and the backend, and whether the probe path in the check directive matches the backend's health check endpoint;\n4) Run curl http://\u003cnginx\u003e/status?format=json to see how many nodes are left in the same upstream, and scale out immediately if only one remains.",
    "1) 查看 error_log 是否有 worker_connections are not enough 或 too many open files；\n2) 按 worker_processes × worker_connections 估算连接上限，对照 nginx_active 当前水位后调大 worker_connections；\n3) 同步调大 worker_rlimit_nofile 与 systemd 的 LimitNOFILE；\n4) 执行 nginx -s reload 生效后确认 accepts 与 handled 的差值不再增长。": "1) Check error_log for worker_connections are not enough or too many open files;\n2) Estimate the connection ceiling as worker_processes × worker_connections, compare it with the current nginx_active level, and raise worker_connections;\n3) Raise worker_rlimit_nofile and the systemd LimitNOFILE to match;\n4) Run nginx -s reload and confirm the gap between accepts and handled stops growing.",
    "1) 登录主机执行 systemctl status nginx 或 ps -ef | grep nginx，确认 master 进程是否存活；\n2) 进程存活则执行 curl -s http://127.0.0.1/nginx_status 手工验证 stub_status 是否被 allow/deny 或防火墙挡住；\n3) 进程不在则先执行 nginx -t 校验配置再 systemctl start nginx，并查看 error_log 尾部定位退出原因；\n4) 确认上游 VIP 或负载均衡已把该节点摘除，避免继续打流量。": "1) Log in to the host and run systemctl status nginx or ps -ef | grep nginx to confirm the master process is alive;\n2) If it is alive, run curl -s http://127.0.0.1/nginx_status to verify by hand whether stub_status is blocked by allow/deny rules or a firewall;\n3) If it is not running, validate the config with nginx -t before systemctl start nginx, and read the tail of error_log for the exit reason;\n4) Confirm the upstream VIP or load balancer has taken this node out of rotation so traffic stops flowing to it.",
    "Nginx upstream 后端节点不可用": "Nginx upstream backend node unavailable",
    "Nginx 出现连接丢弃": "Nginx dropping connections",
    "Nginx 实例不可达": "Nginx instance unreachable"
}
