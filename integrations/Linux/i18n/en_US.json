{
    "/proc/meminfo中的DirectMap所统计的不是关于内存的使用，而是一个反映TLB效率的指标。TLB(Translation Lookaside Buffer)是位于CPU上的缓存，用于将内存的虚拟地址翻译成物理地址，由于TLB的大小有限，不能缓存的地址就需要访问内存里的page table来进行翻译，速度慢很多。为了尽可能地将地址放进TLB缓存，新的CPU硬件支持比4k更大的页面从而达到减少地址数量的目的， 比如2MB，4MB，甚至1GB的内存页，视不同的硬件而定。”DirectMap4k”表示映射为4kB的内存数量， “DirectMap2M”表示映射为2MB的内存数量，以此类推。所以DirectMap其实是一个反映TLB效率的指标": "DirectMap in /proc/meminfo does not describe memory usage; it is an indicator of TLB efficiency. The TLB (Translation Lookaside Buffer) is a cache on the CPU used to translate virtual memory addresses into physical addresses. Because the TLB has a limited size, addresses that cannot be cached require a lookup in the page table in memory, which is much slower. To fit as many addresses as possible into the TLB, newer CPU hardware supports pages larger than 4k so that the number of addresses is reduced, for example 2MB, 4MB or even 1GB memory pages depending on the hardware. \"DirectMap4k\" is the amount of memory mapped with 4kB pages, \"DirectMap2M\" is the amount of memory mapped with 2MB pages, and so on. So DirectMap is in fact an indicator of TLB efficiency",
    "1) 先用 ping 与 ssh 确认机器本身是否存活，机器宕机要按宕机处理；\n2) 机器存活则用 systemctl status categraf 确认 agent 进程，看日志 journalctl -u categraf -n 100 定位；\n3) 常见原因是 agent 崩溃、配置错误、或到服务端的网络/认证不通——用 curl 手工打一次服务端的写入接口验证；\n4) 恢复后确认数据已连续，注意盲区期间的故障不会有历史告警。": "1) First confirm the machine itself is alive with ping and ssh — if it is down, handle it as a host outage;\n2) If it is alive, run systemctl status categraf to check the agent process and journalctl -u categraf -n 100 to locate the cause;\n3) Common causes are an agent crash, a config error, or network/auth failure to the server — verify by curling the server's write endpoint by hand;\n4) After recovery, confirm the data is continuous, and note that failures during the blind window produced no alerts at all.",
    "1) 在采集机上手工 curl -sv \u003c目标URL\u003e 复现，看是连接失败、超时还是状态码不符；\n2) 连接失败则先确认目标端口通、进程活；\n3) 状态码不符则看目标服务日志定位；\n4) 确认探测配置里的超时、期望状态码、证书校验设置是否与实际相符，避免配置导致的误报。": "1) Reproduce it by hand on the collector host with curl -sv \u003ctarget URL\u003e and see whether it is a connection failure, a timeout or an unexpected status code;\n2) For a connection failure, first confirm the target port is reachable and the process is alive;\n3) For an unexpected status code, check the target service logs;\n4) Confirm the probe's timeout, expected status code and certificate verification settings match reality, so the config itself is not causing the alert.",
    "1) 在采集机上手工 ping -c 5 \u003c目标\u003e 复现，再用 mtr -n \u003c目标\u003e 定位丢包出现在哪一跳；\n2) 目标机若存活，确认它是否禁 ICMP（很多安全策略会禁），是则该探测本身不适用，应改用端口探测；\n3) 中间跳丢包的联系网络侧排查；\n4) 确认目标机自身是否宕机或网卡故障。": "1) Reproduce it on the collector host with ping -c 5 \u003ctarget\u003e, then use mtr -n \u003ctarget\u003e to find which hop is dropping packets;\n2) If the target host is alive, check whether it blocks ICMP (many security policies do) — if so this probe does not apply and you should switch to a port probe;\n3) For loss at an intermediate hop, involve the network team;\n4) Confirm the target host itself is not down or suffering a NIC failure.",
    "1) 在采集机上手工 telnet \u003c目标\u003e \u003c端口\u003e 或 nc -vz \u003c目标\u003e \u003c端口\u003e 复现；\n2) 到目标机用 ss -lntp | grep \u003c端口\u003e 确认进程是否在监听；\n3) 进程在监听则排查防火墙、安全组、以及是否只绑定了 127.0.0.1；\n4) 进程不在则按该服务自身的恢复流程拉起。": "1) Reproduce it on the collector host with telnet \u003ctarget\u003e \u003cport\u003e or nc -vz \u003ctarget\u003e \u003cport\u003e;\n2) On the target host, run ss -lntp | grep \u003cport\u003e to confirm a process is listening;\n3) If it is listening, check the firewall, security groups, and whether it is bound only to 127.0.0.1;\n4) If nothing is listening, start the service through its own recovery procedure.",
    "1) 在采集机上按配置的匹配方式手工执行 ps/pgrep 复现；\n2) 进程确实不在则按服务恢复流程处理；\n3) 进程在但探测失败，检查 procstat 配置的 pid_file、exe、pattern 是否写错，以及采集账号是否有权限读取 /proc；\n4) 容器环境下确认 categraf 是否挂载了宿主机的 /proc。": "1) Reproduce the configured match by hand on the collector host with ps/pgrep;\n2) If the process is genuinely gone, follow the service's recovery procedure;\n3) If the process exists but the probe fails, check the procstat pid_file / exe / pattern settings and whether the collector account can read /proc;\n4) In containers, confirm categraf mounts the host's /proc.",
    "1) 用 cat /proc/\u003cpid\u003e/limits 确认 Max open files 的软硬上限；\n2) systemd 管理的服务在 unit 文件里加 LimitNOFILE=65535 后 systemctl daemon-reload 并重启；\n3) 非 systemd 的在启动脚本里加 ulimit -n 65535，或改 /etc/security/limits.conf；\n4) 注意软上限修改后必须重启进程才生效。": "1) Run cat /proc/\u003cpid\u003e/limits to see the soft and hard Max open files;\n2) For systemd services, add LimitNOFILE=65535 to the unit file, then systemctl daemon-reload and restart;\n3) For non-systemd, add ulimit -n 65535 to the startup script or edit /etc/security/limits.conf;\n4) Note the process must be restarted for a changed soft limit to take effect.",
    "1) 用 cat /proc/sys/fs/file-nr 确认已分配与上限；\n2) 用 lsof | awk '{print $2}' | sort | uniq -c | sort -rn | head 找出占用句柄最多的进程；\n3) 应急调大：sysctl -w fs.file-max=\u003c更大值\u003e，并检查该进程的 ulimit -n 与 systemd LimitNOFILE；\n4) 根因通常是连接或文件未关闭，需在应用侧修复泄漏。": "1) Run cat /proc/sys/fs/file-nr to see allocated versus the limit;\n2) Run lsof | awk '{print $2}' | sort | uniq -c | sort -rn | head to find the processes holding the most handles;\n3) As an emergency measure raise it with sysctl -w fs.file-max=\u003clarger value\u003e, and check the process's ulimit -n and systemd LimitNOFILE;\n4) The root cause is usually connections or files never closed — fix the leak in the application.",
    "1) 用 chronyc tracking（或 ntpq -p）确认当前偏移量与上游源状态；\n2) 上游不可达则检查网络与防火墙是否放行 123/UDP；\n3) 用 chronyc sources -v 确认是否所有源都被标记为不可用，必要时更换 NTP 服务器；\n4) 偏移过大时 chrony 会拒绝平滑校正，需用 chronyc makestep 强制对时（注意对时间敏感的业务先做好准备）。": "1) Run chronyc tracking (or ntpq -p) to see the current offset and upstream source state;\n2) If upstream is unreachable, check the network and whether the firewall allows 123/UDP;\n3) Run chronyc sources -v to see whether every source is marked unusable, and switch NTP servers if needed;\n4) When the offset is large chrony refuses to slew, so use chronyc makestep to step the clock (prepare time-sensitive workloads first).",
    "1) 用 date 与 chronyc tracking 确认本机时间与偏移量；\n2) 确认 chronyd/ntpd 服务是否在运行，未运行则启动并设为开机自启；\n3) 检查到 NTP 服务器的网络是否放行 123/UDP，必要时换用内网 NTP 源；\n4) 偏移过大时用 chronyc makestep 强制对时，对时间敏感的业务先做好准备。": "1) Run date and chronyc tracking to check the local time and the offset;\n2) Confirm chronyd/ntpd is running, and if not, start it and enable it at boot;\n3) Check that 123/UDP to the NTP server is allowed, switching to an internal NTP source if needed;\n4) When the offset is large, use chronyc makestep to step the clock, preparing time-sensitive workloads first.",
    "1) 用 df -h 定位挂载点，用 du -x --max-depth=1 \u003c挂载点\u003e | sort -h 找出占用最大的目录；\n2) 清理日志与过期数据，注意用 lsof +L1 检查已删除但未释放的文件；\n3) 评估是否该把数据目录迁到独立磁盘或加盘扩容；\n4) 若是数据库或对象存储的数据盘，按业务侧的归档策略下沉冷数据。": "1) Run df -h to find the mount point, then du -x --max-depth=1 \u003cmount\u003e | sort -h to find the largest directories;\n2) Clean up logs and expired data, and use lsof +L1 to check for deleted-but-unreleased files;\n3) Evaluate whether the data directory should move to its own disk or the disk should be expanded;\n4) For a database or object-store data disk, tier cold data off according to the business archiving policy.",
    "1) 用 df -h 定位挂载点，用 du -x --max-depth=1 \u003c挂载点\u003e | sort -h 找出占用最大的目录；\n2) 系统盘常见是 /var/log 与容器镜像层（docker system df 看占用，docker system prune 清理）；\n3) 用 lsof +L1 检查已删除但句柄未释放的文件；\n4) 长期需扩容或把大数据目录迁到独立磁盘。": "1) Run df -h to find the mount point, then du -x --max-depth=1 \u003cmount\u003e | sort -h to find the largest directories;\n2) On a system disk the usual suspects are /var/log and container image layers (docker system df to inspect, docker system prune to clean);\n3) Use lsof +L1 to check for deleted-but-unreleased file handles;\n4) Longer term, expand the disk or move large data directories to a dedicated disk.",
    "1) 用 df -h 确认是哪个挂载点，再用 du -x --max-depth=1 \u003c挂载点\u003e | sort -h 逐层找出增长最快的目录；\n2) 优先清理日志（journalctl --vacuum-size=500M、清 *.log.N）与临时文件，这是最快见效的；\n3) 用 lsof +L1 找出已删除但句柄未释放的文件，重启对应进程才能真正释放空间；\n4) 确认是否有异常写入（应用死循环打日志、备份任务堆积），不解决根因清理只是拖延。": "1) Run df -h to identify the mount point, then drill down with du -x --max-depth=1 \u003cmount\u003e | sort -h to find the fastest-growing directory;\n2) Start with logs (journalctl --vacuum-size=500M, rotate out *.log.N) and temporary files — that is the quickest win;\n3) Use lsof +L1 to find deleted files whose handles are still open; the owning process must be restarted to actually free the space;\n4) Confirm whether something abnormal is writing (an application looping on logs, backups piling up) — without fixing the root cause, cleanup only buys time.",
    "1) 用 df -i 确认是哪个挂载点的 inode 吃紧；\n2) 用 for d in /path/*; do echo \"$(find $d | wc -l) $d\"; done | sort -rn | head 逐层找出小文件最多的目录；\n3) 常见来源是 session 文件、邮件队列、缓存碎片、未清理的容器层——按业务确认后批量删除；\n4) 长期可在格式化时调大 inode 密度，或把小文件业务迁到独立分区。": "1) Run df -i to see which mount point is short on inodes;\n2) Drill down with for d in /path/*; do echo \"$(find $d | wc -l) $d\"; done | sort -rn | head to find the directories with the most small files;\n3) Common sources are session files, mail queues, cache fragments and uncleaned container layers — confirm with the owner and bulk delete;\n4) Longer term, raise inode density at format time or move small-file workloads to a dedicated partition.",
    "1) 用 dmesg -T | grep -i 'killed process' 确认被杀的是哪个进程、当时占用多少内存；\n2) 用 free -h 与该进程的内存曲线判断是内存泄漏还是配置的上限过高；\n3) 给关键进程调整 oom_score_adj 降低被杀优先级，同时给次要进程设 cgroup 内存上限；\n4) 根因是泄漏的要在应用侧抓堆定位，只扩内存会反复复发。": "1) Run dmesg -T | grep -i 'killed process' to see which process was killed and how much memory it held;\n2) Use free -h and that process's memory history to tell a leak apart from a limit set too high;\n3) Lower oom_score_adj for critical processes and set cgroup memory limits on secondary ones;\n4) If it is a leak, capture a heap profile in the application — adding memory alone will just postpone the recurrence.",
    "1) 用 dmesg -T | grep -iE 'i/o error|ata|scsi' 看内核报的具体错误与设备名；\n2) 用 smartctl -a /dev/sdX 看 SMART 属性，重点关注 Reallocated_Sector_Ct 与 Current_Pending_Sector；\n3) 确认该盘上的数据是否有副本或备份，优先保证数据安全再处理硬件；\n4) 确认为硬件故障的，安排停机换盘，RAID 环境下先确认阵列降级状态再操作。": "1) Run dmesg -T | grep -iE 'i/o error|ata|scsi' to see the exact kernel error and device name;\n2) Run smartctl -a /dev/sdX and focus on Reallocated_Sector_Ct and Current_Pending_Sector;\n3) Confirm the data on that disk has a replica or backup — data safety comes before hardware work;\n4) Once confirmed a hardware fault, schedule downtime to swap the disk; in a RAID array, check the degraded state first.",
    "1) 用 ethtool -S \u003c网卡\u003e 看 rx_crc_errors、rx_frame_errors 等具体计数；\n2) 用 ethtool \u003c网卡\u003e 确认协商的速率与双工模式是否与对端一致（半双工是经典错包源）；\n3) 检查网线/光模块，必要时换线或换口做交叉验证；\n4) 联系网络侧确认交换机对应端口的错包计数。": "1) Run ethtool -S \u003cnic\u003e and look at rx_crc_errors, rx_frame_errors and similar counters;\n2) Run ethtool \u003cnic\u003e to confirm the negotiated speed and duplex match the peer (half duplex is the classic source of errors);\n3) Check the cable/optical module, swapping the cable or port to cross-check;\n4) Ask the network team for the error counters on the corresponding switch port.",
    "1) 用 ethtool -S \u003c网卡\u003e 看 tx_errors、tx_carrier_errors 等具体计数；\n2) 用 ethtool \u003c网卡\u003e 确认速率与双工模式与对端一致；\n3) 检查网线/光模块并做交叉验证；\n4) 确认网卡驱动与固件版本，必要时升级。": "1) Run ethtool -S \u003cnic\u003e and look at tx_errors, tx_carrier_errors and similar counters;\n2) Run ethtool \u003cnic\u003e to confirm speed and duplex match the peer;\n3) Check the cable/optical module and cross-check by swapping;\n4) Verify the NIC driver and firmware versions and upgrade if needed.",
    "1) 用 free -h 确认 available 与 swap 使用情况；\n2) 用 ps aux --sort=-%mem | head 找出占用最高的进程，对比它们的历史内存曲线判断是否泄漏；\n3) 内核回收不及时可用 sync; echo 1 \u003e /proc/sys/vm/drop_caches 释放 page cache（生产慎用）；\n4) 确认是业务增长还是进程泄漏——泄漏要在应用侧抓堆定位，不能只靠重启。": "1) Run free -h to check available memory and swap usage;\n2) Run ps aux --sort=-%mem | head to find the largest consumers and compare against their memory history to judge whether it is a leak;\n3) If the kernel is slow to reclaim, sync; echo 1 \u003e /proc/sys/vm/drop_caches frees page cache (use with care in production);\n4) Determine whether it is growth or a process leak — a leak must be located with a heap profile, not just a restart.",
    "1) 用 iostat -x 1 确认是哪块盘、await 与 %util 的具体数值；\n2) 用 iotop -oP 或 pidstat -d 1 找出占用 IO 最高的进程；\n3) 常见元凶是数据库大查询、备份任务、日志刷盘——把可延后的任务错峰执行；\n4) 若是持续高负载而非突发，考虑换 SSD 或把热数据分散到多块盘。": "1) Run iostat -x 1 to see which disk it is and the actual await and %util values;\n2) Run iotop -oP or pidstat -d 1 to find the process doing the most IO;\n3) The usual culprits are large database queries, backup jobs and log flushing — move deferrable work off peak;\n4) If it is sustained rather than a spike, consider SSDs or spreading hot data across disks.",
    "1) 用 ip -s link show \u003c网卡\u003e 确认 RX dropped/errors 的增长；\n2) 用 ethtool -S \u003c网卡\u003e 看具体丢包原因（rx_no_buffer、rx_missed_errors 等）；\n3) 环形缓冲区不足可用 ethtool -G \u003c网卡\u003e rx \u003c更大值\u003e 调大，并检查 net.core.netdev_max_backlog；\n4) 若伴随 CPU 软中断偏高（top 里 %si），开启 RPS/RSS 把中断分散到多核。": "1) Run ip -s link show \u003cnic\u003e to confirm RX dropped/errors are growing;\n2) Run ethtool -S \u003cnic\u003e to see the specific drop reason (rx_no_buffer, rx_missed_errors, etc.);\n3) If ring buffers are too small, raise them with ethtool -G \u003cnic\u003e rx \u003clarger value\u003e and check net.core.netdev_max_backlog;\n4) If softirq CPU is also high (%si in top), enable RPS/RSS to spread interrupts across cores.",
    "1) 用 ip -s link show \u003c网卡\u003e 确认 TX dropped/errors 的增长；\n2) 用 ethtool -S \u003c网卡\u003e 看具体计数，并用 tc -s qdisc show dev \u003c网卡\u003e 看队列是否溢出；\n3) 发送队列不足可调大 txqueuelen 与 ethtool -G \u003c网卡\u003e tx；\n4) 确认是否已跑满带宽——是则限流或扩带宽，不是则排查网卡驱动与对端设备。": "1) Run ip -s link show \u003cnic\u003e to confirm TX dropped/errors are growing;\n2) Run ethtool -S \u003cnic\u003e for the specific counters and tc -s qdisc show dev \u003cnic\u003e to see whether the queue is overflowing;\n3) If the transmit queue is too small, raise txqueuelen and ethtool -G \u003cnic\u003e tx;\n4) Check whether the link is saturated — if so throttle or add bandwidth, otherwise investigate the NIC driver and peer device.",
    "1) 用 ps -ef | grep \u003c进程关键字\u003e 确认确实不在，排除采集侧的匹配规则写错；\n2) 查该进程的日志与 dmesg -T | grep -i 'killed process'，区分自身崩溃、被 OOM 杀死还是人工停止；\n3) 按服务自身流程拉起，并确认 systemd 的 Restart 策略是否生效；\n4) 若反复退出，先定位根因再拉起，避免陷入重启循环。": "1) Run ps -ef | grep \u003cprocess keyword\u003e to confirm it is really gone and rule out a wrong match rule on the collector side;\n2) Check that process's log and dmesg -T | grep -i 'killed process' to tell a crash, an OOM kill and a manual stop apart;\n3) Start it through the service's own procedure and confirm the systemd Restart policy is effective;\n4) If it keeps exiting, find the root cause before starting it again to avoid a restart loop.",
    "1) 用 ps -eo ppid | sort | uniq -c | sort -rn | head 找出 fork 子进程最多的父进程；\n2) 确认是正常的多进程模型（如 php-fpm、nginx worker）还是失控 fork；\n3) 失控的先停掉该父进程止血，再修复代码里的 fork 逻辑；\n4) 正常业务增长则调整 worker 数配置并评估是否需要扩容。": "1) Run ps -eo ppid | sort | uniq -c | sort -rn | head to find the parent forking the most children;\n2) Determine whether this is a normal multi-process model (php-fpm, nginx workers) or a runaway fork;\n3) For a runaway, stop that parent to stop the bleeding, then fix the fork logic in the code;\n4) For genuine growth, tune the worker count and evaluate whether to scale out.",
    "1) 用 ss -s 确认各状态连接数分布；\n2) 用 ss -tan state time-wait | awk '{print $4}' | cut -d: -f1 | sort | uniq -c | sort -rn 看集中在哪个目标；\n3) 根因通常是短连接过多——在应用侧改用长连接或连接池，这是最有效的手段；\n4) 应急可开启 net.ipv4.tcp_tw_reuse=1 并调大 net.ipv4.ip_local_port_range（不要开 tcp_tw_recycle，NAT 环境下会丢包）。": "1) Run ss -s to see the connection state distribution;\n2) Run ss -tan state time-wait | awk '{print $4}' | cut -d: -f1 | sort | uniq -c | sort -rn to see which peer they concentrate on;\n3) The root cause is usually too many short-lived connections — switching the application to long-lived connections or a pool is by far the most effective fix;\n4) As an emergency measure enable net.ipv4.tcp_tw_reuse=1 and widen net.ipv4.ip_local_port_range (do not enable tcp_tw_recycle — it drops packets behind NAT).",
    "1) 用 sysctl net.netfilter.nf_conntrack_count 与 nf_conntrack_max 确认当前水位；\n2) 用 conntrack -L | awk '{print $4}' | sort | uniq -c | sort -rn 看连接集中在哪类协议或来源；\n3) 应急调大：sysctl -w net.netfilter.nf_conntrack_max=\u003c更大值\u003e，并同步调大 nf_conntrack_buckets；\n4) 同时缩短 nf_conntrack_tcp_timeout_time_wait 等超时，或对不需要跟踪的流量用 iptables raw 表 NOTRACK。": "1) Run sysctl net.netfilter.nf_conntrack_count and nf_conntrack_max to see the current level;\n2) Run conntrack -L | awk '{print $4}' | sort | uniq -c | sort -rn to see which protocol or source the entries concentrate on;\n3) As an emergency measure raise it: sysctl -w net.netfilter.nf_conntrack_max=\u003clarger value\u003e, raising nf_conntrack_buckets to match;\n4) Also shorten timeouts such as nf_conntrack_tcp_timeout_time_wait, or mark traffic that needs no tracking NOTRACK in the iptables raw table.",
    "1) 用 top -H 或 pidstat 1 找出占用最高的进程与线程；\n2) 用 perf top 或对应语言的 profiler（Java 用 arthas/async-profiler，Go 用 pprof）定位热点函数；\n3) 区分是业务量自然增长还是代码异常（死循环、正则回溯、频繁 GC）；\n4) 业务量增长则扩容或限流，代码问题则修复后发布。": "1) Run top -H or pidstat 1 to find the busiest processes and threads;\n2) Use perf top or a language profiler (arthas/async-profiler for Java, pprof for Go) to locate the hot functions;\n3) Tell natural traffic growth apart from a code problem (infinite loop, regex backtracking, frequent GC);\n4) For growth, scale out or rate limit; for a code problem, fix and release.",
    "1) 用 uptime 看 1/5/15 分钟负载趋势，判断是突发还是持续；\n2) 用 top 看 %wa（IO 等待）——高说明瓶颈在磁盘而非 CPU，按磁盘 IO 处理；\n3) 用 vmstat 1 看 r（运行队列）与 b（阻塞）列区分 CPU 密集与 IO 阻塞；\n4) 定位到具体进程后按 CPU 或 IO 的路径继续排查，必要时扩容。": "1) Run uptime to see the 1/5/15 minute load trend and judge spike versus sustained;\n2) Run top and look at %wa (IO wait) — high means the bottleneck is disk rather than CPU, so follow the disk IO path;\n3) Run vmstat 1 and use the r (run queue) and b (blocked) columns to separate CPU-bound from IO-blocked;\n4) Once you have the specific process, continue down the CPU or IO path and scale out if needed.",
    "CPU使用率": "CPU Usage",
    "CPU使用率历史趋势": "CPU Usage History",
    "CPU使用率详情": "CPU Usage Details",
    "CPU利用率": "CPU Usage",
    "CPU利用率%": "CPU Usage %",
    "CPU利用率较高": "CPU usage high",
    "CPU空闲率": "CPU Idle",
    "CPU详情": "CPU Details",
    "CPU负载": "CPU Load",
    "Conntrack使用率": "Conntrack Usage",
    "Conntrack表的使用率超过80%": "Conntrack table usage exceeds 80%",
    "FD使用率": "FD Usage",
    "HTTP地址探测失败": "HTTP probe failed",
    "IO/Merged次数": "IO/Merged Count",
    "IO使用率历史趋势": "IO Usage History",
    "IO比较繁忙": "IO busy",
    "NTP偏移": "NTP Offset",
    "NTP时间偏移": "NTP Time Offset",
    "NTP时间偏移太大": "NTP time offset too large",
    "PING地址探测失败": "PING probe failed",
    "Process 总量": "Total Processes",
    "SWAP内存使用": "SWAP Memory Usage",
    "TLB效率": "TLB Efficiency",
    "TME_WAIT状态的连接超过2万，可能需要关注": "TIME_WAIT connections exceed 20000, worth attention",
    "Time Wait 状态的连接数": "Time Wait Connections",
    "`dmesg -T` 有时看到 conntrack table full 的报错，大概率就是 conntrack 限制太小了，需要调整内核参数": "`dmesg -T` sometimes reports conntrack table full, which most likely means the conntrack limit is too small and the kernel parameter needs to be adjusted",
    "inode 使用率（最大的25个）": "inode Usage (Top 25)",
    "inode分区使用率": "inode Partition Usage",
    "nf_conntrack 使用率": "nf_conntrack Usage",
    "swap 使用率历史趋势": "Swap Usage History",
    "swap使用过高，会影响系统io性能，如果内存够用但swap使用很高，可以调小swappiness的值": "High swap usage hurts system IO performance. If memory is sufficient but swap usage is high, consider lowering the swappiness value",
    "上下文切换/中断": "Context Switches / Interrupts",
    "以现在的硬件配置，通常句柄的 ulimit 应该比较大，如果低于 4096，大概率是忘记修改配置了，需要注意。这个数据是 Categraf 的 procstat 插件采集的。": "With today's hardware the file handle ulimit should normally be fairly large. A value below 4096 most likely means the configuration was forgotten, so pay attention. This data is collected by the procstat plugin of Categraf.",
    "其他指标": "Other Metrics",
    "内存使用率": "Memory Usage",
    "内存使用率历史趋势": "Memory Usage History",
    "内存利用率": "Memory Usage",
    "内存利用率%": "Memory Usage %",
    "内存利用率比较高": "Memory usage high",
    "内存详情": "Memory Details",
    "内核态内存使用": "Kernel Memory Usage",
    "写文件错误数总和": "Total File Write Errors",
    "出入流量大小": "Inbound/Outbound Traffic",
    "单机概况": "Single Node Overview",
    "启动时长": "Uptime",
    "大于0，说明有进程内存不够用了，需要考虑扩容或升级配置了": "A value greater than 0 means some process ran out of memory; consider scaling out or upgrading the configuration",
    "大于200G的盘，空间不足了": "Disk larger than 200G is running out of space",
    "如果cpu使用率超过50%，可以通过top命令查看机器上是否有异常进程，如果没有异常进程，则说明服务需要扩容或者机器需要升级配置了": "If CPU usage exceeds 50%, use the top command to check whether there are abnormal processes on the machine. If there are none, the service needs to scale out or the machine needs a configuration upgrade",
    "如果内存使用率超过50%，则需要扩容或者升级配置了": "If memory usage exceeds 50%, scale out or upgrade the configuration",
    "如果没有启用 swap，这个图表是 No Data，是符合预期的": "If swap is not enabled, this chart shows No Data, which is expected",
    "如果超过80%，建议把文件描述符的最大个数调大，或者扩容": "If it exceeds 80%, increase the maximum number of file descriptors or scale out",
    "存活机器数量": "Alive Machines",
    "小于200G的盘，空间不足了": "Disk smaller than 200G is running out of space",
    "总内存": "Total Memory",
    "所有机器": "All Machines",
    "操作系统文件句柄使用率": "OS File Handle Usage",
    "数据源": "Data Source",
    "文件句柄使用率超过90%": "File handle usage exceeds 90%",
    "时序库": "Time Series Database",
    "机器": "Machine",
    "机器列表": "Machine List",
    "机器台账表格视图（使用 Categraf 作为采集器）": "Machine Inventory Table View (Categraf as collector)",
    "机器常用指标（使用 NodeExporter 作为采集器）": "Common Machine Metrics (NodeExporter as collector)",
    "机器常用指标（如果只想看当前业务组内的机器修改大盘变量 ident 的变量类型为机器标识即可）": "Common Machine Metrics (to only see machines of the current business group, change the dashboard variable ident to the host-ident type)",
    "机器监控 - agent失联超过60秒": "Machine monitoring - agent lost for more than 60 seconds",
    "机器监控 - 时间偏移超过3秒，请注意": "Machine monitoring - time offset exceeds 3 seconds, attention",
    "机器表格样例": "Machine Table Example",
    "机器进程数量统计（使用 Categraf 作为采集器）": "Machine Process Count (Categraf as collector)",
    "根分区使用率": "Root Partition Usage",
    "根分区利用率%": "Root Partition Usage %",
    "每个CPU Core的平均负载任务超过3": "Average load per CPU core exceeds 3",
    "每秒OOM次数": "OOM Count per Second",
    "每秒读写数据大小": "Read/Write Bytes per Second",
    "熵池大小": "Entropy Pool Size",
    "熵池太小 ，程序使用随机函数会阻塞，可以安装 rng-tools 工具增加熵池大小，可参考\n\u003ca href=\"https://codeantenna.com/a/Ab6aMd3NSA\" target=\"_blank\"\u003ehttps://codeantenna.com/a/Ab6aMd3NSA\u003c/a\u003e ": "If the entropy pool is too small, programs using random functions will block. You can install the rng-tools package to increase the entropy pool size, see\n\u003ca href=\"https://codeantenna.com/a/Ab6aMd3NSA\" target=\"_blank\"\u003ehttps://codeantenna.com/a/Ab6aMd3NSA\u003c/a\u003e ",
    "用户态内存使用": "User Memory Usage",
    "硬盘 ReadOnly": "Disk ReadOnly",
    "硬盘使用率（最大的25个）": "Disk Usage (Top 25)",
    "硬盘出错，0是正常，非0是异常": "Disk errors, 0 is normal, non-zero is abnormal",
    "硬盘监控 - inode使用率超过90%": "Disk monitoring - inode usage exceeds 90%",
    "硬盘监控 - 大于200G的盘，空间不足了": "Disk monitoring - disk larger than 200G is running out of space",
    "硬盘监控 - 小于200G的盘，空间不足了": "Disk monitoring - disk smaller than 200G is running out of space",
    "硬盘监控 - 设备有报错": "Disk monitoring - device errors reported",
    "硬盘监控 - 预测硬盘会在未来4小时内写满": "Disk monitoring - disk predicted to be full within 4 hours",
    "硬盘设备错误": "Disk Device Errors",
    "磁盘分区使用率": "Disk Partition Usage",
    "磁盘详情": "Disk Details",
    "系统指标": "System Metrics",
    "系统有OOM产生": "OOM occurred on the system",
    "网卡监控 - 入方向有丢包": "NIC monitoring - inbound packet loss",
    "网卡监控 - 入方向有错包": "NIC monitoring - inbound packet errors",
    "网卡监控 - 出方向有丢包": "NIC monitoring - outbound packet loss",
    "网卡监控 - 出方向有错包": "NIC monitoring - outbound packet errors",
    "网络丢包": "Network Packet Loss",
    "网络地址探活失败": "Network probe failed",
    "网络流量-入向": "Network Traffic - Inbound",
    "网络流量-出向": "Network Traffic - Outbound",
    "网络详情": "Network Details",
    "网络错包": "Network Packet Errors",
    "详情": "Details",
    "进程句柄数限制（低于4096要注意）": "Process File Handle Limit (below 4096 needs attention)",
    "进程数": "Process Count",
    "进程数超过2000，可以考虑扩容了": "Process count exceeds 2000, consider scaling out",
    "进程监控-有进程数为0，某进程可能挂了": "Process monitoring - a process count is 0, the process may be down",
    "进程监控-进程句柄限制过小": "Process monitoring - process file handle limit too small",
    "进程监控-进程总量超过600": "Process monitoring - total processes exceed 600"
}
