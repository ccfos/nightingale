{
    "1) 在各 mon 节点用 chronyc tracking 确认偏移量与上游源状态；\n2) 确认所有 mon 节点用的是同一组 NTP 源；\n3) chronyd 未运行的启动并设开机自启，偏移过大的用 chronyc makestep 强制对时；\n4) 对完时后用 ceph -s 确认 clock skew 告警消失。": "1) On each mon node run chronyc tracking to check the offset and upstream source state;\n2) Confirm every mon node uses the same set of NTP sources;\n3) Start chronyd where it is not running and enable it at boot; where the offset is large, use chronyc makestep to step the clock;\n4) After correcting the time, confirm with ceph -s that the clock skew warning is gone.",
    "1) 用 ceph -s 与 ceph health detail 看集群整体状态和具体告警项；\n2) 用 ceph pg dump_stuck 与 ceph pg \u003cpgid\u003e query 定位卡住的 PG 及其原因；\n3) 多数情况根因是 OSD down 或磁盘满——先恢复 OSD 或释放空间，PG 会自动回到 active+clean；\n4) 恢复期间用 ceph -w 持续观察，避免同时做其他会加重负载的操作。": "1) Run ceph -s and ceph health detail for the overall state and the specific warnings;\n2) Use ceph pg dump_stuck and ceph pg \u003cpgid\u003e query to locate the stuck PGs and why;\n3) In most cases the root cause is a down OSD or a full disk — restore the OSD or free space and the PGs return to active+clean automatically;\n4) Watch with ceph -w during recovery and avoid other operations that add load.",
    "1) 用 ceph health detail 找出不一致的 PG，再用 rados list-inconsistent-obj \u003cpgid\u003e 看具体对象；\n2) 用 ceph pg repair \u003cpgid\u003e 触发修复，Ceph 会用权威副本覆盖；\n3) 修复前确认坏的是哪一份——若主副本本身损坏，需先确认权威副本可靠；\n4) 反复出现不一致的 OSD 要查磁盘 SMART，多为坏道，应换盘。": "1) Use ceph health detail to find the inconsistent PGs, then rados list-inconsistent-obj \u003cpgid\u003e for the specific objects;\n2) Run ceph pg repair \u003cpgid\u003e to trigger repair — Ceph overwrites from the authoritative copy;\n3) Before repairing, confirm which copy is bad: if the primary itself is corrupt, verify the authoritative copy is trustworthy first;\n4) An OSD that repeatedly produces inconsistencies should have its SMART checked — it is usually bad sectors and the disk should be replaced.",
    "1) 用 ceph health detail 看具体是哪些 WARN 项；\n2) 常见为 OSD 单点 down、PG 未 clean、时钟偏移、pool 未设置 pg_autoscale；\n3) 逐项按提示处理，Ceph 的 health detail 通常已给出明确指引；\n4) 处理完确认 ceph -s 回到 HEALTH_OK。": "1) Run ceph health detail to see exactly which WARN items are present;\n2) Common ones are a single down OSD, PGs not clean, clock skew and pools without pg_autoscale;\n3) Work through them following the guidance — Ceph's health detail usually gives explicit instructions;\n4) Once done, confirm ceph -s returns to HEALTH_OK.",
    "1) 用 ceph osd df 找出空间不足的 OSD；\n2) 释放空间或扩容 OSD 是根本解法；\n3) 应急可调高 osd_backfill_full_ratio 让回填继续，但要确认磁盘确实还有余量；\n4) 回填完成前避免再有 OSD 下线，否则冗余会进一步降低。": "1) Run ceph osd df to find the OSDs short on space;\n2) Freeing space or adding OSDs is the real fix;\n3) As an emergency measure raise osd_backfill_full_ratio so backfill continues, but only if the disks genuinely have headroom;\n4) Avoid taking further OSDs offline before backfill completes, or redundancy drops further.",
    "1) 用 ceph osd df 看各 OSD 使用率分布，确认是个别 OSD 偏高还是整体接近满；\n2) 个别偏高的用 ceph balancer status 确认均衡器是否开启，未开启则 ceph balancer on；\n3) 整体接近满的必须扩容 OSD，或先删除无用的 pool/快照释放空间；\n4) 应急可临时调高 mon_osd_full_ratio 争取时间，但这是危险操作，必须同时进行扩容。": "1) Run ceph osd df to see the utilization spread and whether it is one OSD or the cluster as a whole;\n2) For a single hot OSD, check ceph balancer status and run ceph balancer on if it is disabled;\n3) If the cluster as a whole is near full you must add OSDs, or first delete unused pools/snapshots to free space;\n4) Temporarily raising mon_osd_full_ratio buys time but is a dangerous operation and must be paired with real expansion.",
    "1) 用 ceph osd perf 看各 OSD 的 commit/apply 延迟，定位是个别还是普遍；\n2) 个别 OSD 高延迟的，到该节点用 iostat -x 1 与 smartctl 检查磁盘健康与是否已打满；\n3) 普遍高延迟的确认是否正在做大规模 recovery/backfill，可调低 osd_max_backfills 降低影响；\n4) 磁盘故障的按换盘流程处理。": "1) Run ceph osd perf to see commit/apply latency per OSD and whether it is isolated or widespread;\n2) For a single slow OSD, go to that node and use iostat -x 1 and smartctl to check disk health and saturation;\n3) If it is widespread, check whether a large recovery/backfill is running and lower osd_max_backfills to reduce the impact;\n4) For a failing disk, follow the replacement procedure.",
    "1) 用 ceph osd tree 确认是哪个 OSD 被降权、当前权重多少；\n2) 确认是人工 reweight 还是 reweight-by-utilization 自动调整；\n3) 若故障已恢复，用 ceph osd reweight \u003cid\u003e 1.0 恢复权重让数据回填；\n4) 若是为了平衡容量而降权，评估是否该改用 balancer 模块统一处理。": "1) Run ceph osd tree to see which OSD is down-weighted and to what;\n2) Determine whether it was a manual reweight or an automatic reweight-by-utilization;\n3) If the fault is resolved, run ceph osd reweight \u003cid\u003e 1.0 to restore the weight and let data backfill;\n4) If it was down-weighted to balance capacity, consider using the balancer module instead.",
    "1) 用 ceph osd tree 确认是哪个 OSD、在哪个节点；\n2) 到该节点用 systemctl status ceph-osd@\u003cid\u003e 与其日志定位原因（磁盘故障、OOM、进程崩溃）；\n3) 磁盘正常的直接拉起 OSD；磁盘故障的走换盘流程并 ceph osd out 让数据迁走；\n4) 恢复期间用 ceph -s 观察 recovery 进度，避免同时下线更多 OSD。": "1) Run ceph osd tree to identify the OSD and its node;\n2) On that node use systemctl status ceph-osd@\u003cid\u003e and its logs to find the cause (disk failure, OOM, process crash);\n3) If the disk is fine, just start the OSD; if the disk failed, follow the replacement procedure and ceph osd out to migrate data away;\n4) Watch recovery progress with ceph -s and avoid taking more OSDs offline.",
    "1) 用 ceph pg \u003cpgid\u003e query 看 down 的原因，重点看 blocked_by 指向哪些 OSD；\n2) 把这些 OSD 恢复上线是最直接的解法——通常是磁盘或主机故障；\n3) OSD 确实无法恢复时，需要评估 osd lost 操作的数据丢失风险后再执行；\n4) 全程记录操作，涉及数据丢失的决策要有人复核。": "1) Run ceph pg \u003cpgid\u003e query to see why it is down, focusing on which OSDs blocked_by points to;\n2) Bringing those OSDs back online is the most direct fix — usually a disk or host failure;\n3) If an OSD genuinely cannot be recovered, assess the data loss risk of an osd lost operation before running it;\n4) Record every step: decisions involving data loss should be reviewed by a second person.",
    "1) 用 df -h 确认 /var/lib/ceph/mon 所在分区的余量；\n2) mon 目录膨胀最常见的原因是集群长期不 clean 导致 osdmap 无法裁剪——先解决集群 health 问题；\n3) 用 ceph tell mon.\u003cid\u003e compact 触发 rocksdb 压缩释放空间；\n4) 长期需给 mon 单独分配更大的磁盘。": "1) Run df -h to check the free space on the partition holding /var/lib/ceph/mon;\n2) The most common reason the mon directory grows is a cluster that has been unclean for a long time, preventing osdmap trimming — fix the cluster health first;\n3) Run ceph tell mon.\u003cid\u003e compact to trigger rocksdb compaction and reclaim space;\n4) Longer term, give mon its own larger disk.",
    "1) 确认 ceph exporter / mgr 所在节点是否存活，进程是否运行；\n2) 用 ceph -s 在任一 mon 节点手工确认集群本身是否健康；\n3) mgr 挂了会导致大量指标缺失，用 ceph mgr fail 触发切换到备用 mgr；\n4) 恢复采集后确认历史数据缺口，注意盲区期间没有告警。": "1) Confirm the node running ceph exporter / mgr is alive and the process is running;\n2) Run ceph -s on any mon node to confirm by hand whether the cluster itself is healthy;\n3) A failed mgr causes many metrics to disappear — run ceph mgr fail to switch to the standby mgr;\n4) After collection resumes, note the data gap and remember there were no alerts during the blind window.",
    "1) 立即用 ceph health detail 看具体是哪一项 ERR；\n2) 常见为 PG inactive/incomplete、OSD 大量 down、或 full osd 导致集群只读；\n3) 先恢复 down 的 OSD 或释放已满 OSD 的空间，这是绝大多数 ERR 的根因；\n4) 恢复期间不要执行扩缩容或调整 crush map，避免加重数据迁移。": "1) Immediately run ceph health detail to see which ERR item it is;\n2) Common ones are inactive/incomplete PGs, many down OSDs, or a full OSD forcing the cluster read-only;\n3) Restore the down OSDs or free space on the full OSD first — this is the root cause of the vast majority of ERRs;\n4) Do not scale or adjust the crush map during recovery, as it worsens data migration."
}
