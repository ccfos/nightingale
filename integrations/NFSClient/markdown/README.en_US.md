# NFS Client

forked from telegraf/inputs.nfsclient

## Disabling this plugin

- Option 1: rename the `input.nfsclient` directory to something that does not start with `input.`
- Option 2: leave the configuration in nfsclient.toml empty
