{
    "1) 先确认该集群本来就应持续写入，否则关闭此规则；\n2) 检查上游写入端（Filebeat/Logstash/应用 bulk 客户端）是否存活、日志中是否有 bulk 被拒；\n3) 用 GET _cat/thread_pool/write?v 看 write 线程池的 rejected 是否在增长；\n4) 确认索引未被置为只读（磁盘 watermark 会触发 read_only_allow_delete）。": "1) First confirm this cluster is genuinely expected to receive continuous writes, otherwise disable this rule;\n2) Check that the upstream writers (Filebeat/Logstash/application bulk clients) are alive and whether their logs show rejected bulk requests;\n3) Run GET _cat/thread_pool/write?v to see whether the write thread pool's rejected counter is growing;\n4) Confirm the indices have not been made read-only (a disk watermark triggers read_only_allow_delete).",
    "1) 对照 elasticsearch_jvm_gc_collection_seconds_count 看 old GC 是否变频繁；\n2) 用 GET _cat/indices?v\u0026s=store.size:desc 排查近期是否有索引数据量暴涨；\n3) 用 GET _nodes/stats/indices/fielddata 检查 fielddata 与查询缓存占用；\n4) 提前规划扩容或清理，避免涨到 90% 触发 Full GC。": "1) Compare against elasticsearch_jvm_gc_collection_seconds_count to see whether old GC is becoming more frequent;\n2) Run GET _cat/indices?v\u0026s=store.size:desc to check whether any index has grown sharply recently;\n3) Run GET _nodes/stats/indices/fielddata to inspect fielddata and query cache usage;\n4) Plan capacity or cleanup ahead of time so it does not climb to 90% and trigger full GC.",
    "1) 用 GET _cat/allocation?v 定位磁盘吃紧的节点；\n2) 删除或归档过期索引（DELETE \u003cindex\u003e），或用 ILM 把冷数据迁到冷节点；\n3) 磁盘释放后手工解除只读：PUT _all/_settings {\"index.blocks.read_only_allow_delete\":null}；\n4) 长期需扩容磁盘或增加数据节点。": "1) Run GET _cat/allocation?v to find the nodes that are short on disk;\n2) Delete or archive expired indices (DELETE \u003cindex\u003e), or use ILM to move cold data to cold nodes;\n3) After freeing disk, lift the read-only block by hand: PUT _all/_settings {\"index.blocks.read_only_allow_delete\":null};\n4) Longer term, expand the disk or add data nodes.",
    "1) 用 GET _cat/allocation?v 看各节点磁盘水位；\n2) 清理过期索引或调整 ILM 策略，提前把数据滚动到冷节点；\n3) 评估 cluster.routing.allocation.disk.watermark.* 阈值是否与实际容量匹配；\n4) 提前安排扩容，避免跌到 flood-stage 后索引被置为只读。": "1) Run GET _cat/allocation?v to check the disk watermark on each node;\n2) Clean up expired indices or adjust the ILM policy to roll data to cold nodes earlier;\n3) Review whether the cluster.routing.allocation.disk.watermark.* thresholds match your actual capacity;\n4) Schedule expansion early so it never drops to flood stage and turns indices read-only.",
    "1) 用 GET _cat/recovery?v\u0026active_only=true 看恢复进度是否还在推进；\n2) 卡住不动时用 GET _cluster/allocation/explain 看原因（磁盘不足、节点带宽打满）；\n3) 恢复限速可临时调高 indices.recovery.max_bytes_per_sec；\n4) 确认没有节点在反复掉线导致分片反复重建。": "1) Run GET _cat/recovery?v\u0026active_only=true to see whether recovery is still making progress;\n2) If it is stuck, run GET _cluster/allocation/explain to find the reason (insufficient disk, saturated node bandwidth);\n3) Temporarily raise indices.recovery.max_bytes_per_sec if the recovery throttle is the bottleneck;\n4) Confirm no node is repeatedly dropping out and causing shards to be rebuilt over and over.",
    "1) 用 GET _cat/recovery?v\u0026active_only=true 看迁移是否还在推进；\n2) 停滞则用 GET _cluster/allocation/explain 看是否被磁盘 watermark 或 allocation filter 挡住；\n3) 确认是否有人手工触发了大规模 reroute，或有节点在上下线；\n4) 必要时临时调高 cluster.routing.allocation.node_concurrent_recoveries 与恢复限速。": "1) Run GET _cat/recovery?v\u0026active_only=true to see whether the relocation is still progressing;\n2) If it has stalled, run GET _cluster/allocation/explain to check whether a disk watermark or an allocation filter is blocking it;\n3) Confirm whether someone triggered a large reroute by hand, or whether nodes are joining and leaving;\n4) If needed, temporarily raise cluster.routing.allocation.node_concurrent_recoveries and the recovery throttle.",
    "1) 用 GET _cluster/allocation/explain 直接拿到未分配的原因；\n2) 节点掉线则先把节点恢复回集群；\n3) 副本数超过可用节点数则调小 index.number_of_replicas；\n4) 磁盘触发 watermark 则先清理磁盘，再执行 POST _cluster/reroute?retry_failed=true。": "1) Run GET _cluster/allocation/explain to get the exact reason the shards are unassigned;\n2) If a node dropped out, bring it back into the cluster first;\n3) If the replica count exceeds the number of available nodes, lower index.number_of_replicas;\n4) If a disk watermark was hit, free disk space first and then run POST _cluster/reroute?retry_failed=true.",
    "1) 用 GET _cluster/allocation/explain 看副本分片无法分配的原因；\n2) 单节点集群把索引副本数设为 0：PUT \u003cindex\u003e/_settings {\"index.number_of_replicas\":0}；\n3) 多节点集群检查是否有节点掉线、或磁盘触发了 watermark 导致不再分配；\n4) 处理完确认 health 回到 green。": "1) Run GET _cluster/allocation/explain to find why the replica shards cannot be allocated;\n2) On a single-node cluster, set the index replica count to 0: PUT \u003cindex\u003e/_settings {\"index.number_of_replicas\":0};\n3) On a multi-node cluster, check whether a node dropped out or a disk watermark is blocking allocation;\n4) Once handled, confirm the health goes back to green.",
    "1) 用 GET _cluster/health?level=indices 定位处于 red 的索引；\n2) 用 GET _cluster/allocation/explain 看主分片无法分配的具体原因；\n3) 常见原因是节点掉线，先把节点拉回集群让分片自动恢复；\n4) 节点数据确实损坏时，用 POST _snapshot/\u003crepo\u003e/\u003csnap\u003e/_restore 从快照恢复该索引。": "1) Run GET _cluster/health?level=indices to find the indices that are red;\n2) Run GET _cluster/allocation/explain to see exactly why the primary shard cannot be allocated;\n3) The usual cause is a node dropping out — bring the node back so shards recover automatically;\n4) If the node's data is genuinely corrupted, restore the index from a snapshot with POST _snapshot/\u003crepo\u003e/\u003csnap\u003e/_restore.",
    "1) 用 GET _cluster/pending_tasks 看积压的具体任务和优先级；\n2) 用 GET _cat/master?v 确认 master 是否在频繁切换；\n3) 用 GET _cat/shards 统计分片总数，排查是否因大量小索引导致集群状态过大；\n4) 收敛分片数量（合并小索引、调整 ILM），必要时给 master 节点单独扩容。": "1) Run GET _cluster/pending_tasks to see the queued tasks and their priorities;\n2) Run GET _cat/master?v to confirm the master is not flapping;\n3) Run GET _cat/shards to count total shards and check whether a large number of tiny indices is bloating the cluster state;\n4) Reduce the shard count (merge small indices, tune ILM) and, if needed, scale the master nodes separately.",
    "1) 用 GET _nodes/stats/jvm 确认是哪个节点堆压力大；\n2) 用 GET _cat/indices?v\u0026s=store.size:desc 找占用大的索引，关闭或归档不用的；\n3) 用 GET _tasks?actions=*search*\u0026detailed 定位大聚合或深分页查询并 cancel；\n4) 长期把堆调整为物理内存的一半（不超过 31G），或增加节点分摊。": "1) Run GET _nodes/stats/jvm to identify which node is under heap pressure;\n2) Run GET _cat/indices?v\u0026s=store.size:desc to find the largest indices and close or archive the unused ones;\n3) Run GET _tasks?actions=*search*\u0026detailed to find large aggregations or deep pagination queries and cancel them;\n4) Longer term, set the heap to half of physical memory (never above 31G), or add nodes to spread the load.",
    "1m/5m/15m系统负载": "1m/5m/15m System Load",
    "GC运行次数": "GC Run Count",
    "GC运行耗时（秒）": "GC Run Time (s)",
    "JVM申请/限制内存（byte）": "JVM Committed/Max Memory (byte)",
    "fielddata cache内存占用（byte）": "Fielddata Cache Memory Usage (byte)",
    "fielddata cache平均每秒内存剔除次数": "Fielddata Cache Evictions per Second",
    "query cache内存占用（byte）": "Query Cache Memory Usage (byte)",
    "query cache平均每秒内存剔除次数": "Query Cache Evictions per Second",
    "segment个数": "Segment Count",
    "segment内存占用（byte）": "Segment Memory Usage (byte)",
    "translog大小（byte）": "Translog Size (byte)",
    "主分片doc value内存占用（byte）": "Primary Shard Doc Value Memory Usage (byte)",
    "主分片fixed bit set内存占用（byte）": "Primary Shard Fixed Bit Set Memory Usage (byte)",
    "主分片normalization factor内存占用（byte）": "Primary Shard Normalization Factor Memory Usage (byte)",
    "主分片point内存占用（byte）": "Primary Shard Point Memory Usage (byte)",
    "主分片segment容量": "Primary Shard Segment Size",
    "主分片segment数": "Primary Shard Segment Count",
    "主分片term内存占用（byte）": "Primary Shard Term Memory Usage (byte)",
    "主分片文档数": "Primary Shard Doc Count",
    "主分片索引写入数据量（byte）": "Primary Shard Index Write Volume (byte)",
    "主分片索引容量（byte）": "Primary Shard Index Size (byte)",
    "分片field内存占用（byte）": "Shard Field Memory Usage (byte)",
    "创建中分片数量\nCount of shards that are being freshly created": "Initializing Shards\nCount of shards that are being freshly created",
    "合并操作耗时（秒）": "Merge Time (s)",
    "平均CPU使用率": "Average CPU Usage",
    "平均JVM内存使用率": "Average JVM Memory Usage",
    "平均每秒删除文档数": "Deleted Docs per Second",
    "平均每秒合并文档大小": "Merged Doc Size per Second",
    "平均每秒合并文档数": "Merged Docs per Second",
    "平均每秒索引文档数": "Indexed Docs per Second",
    "所有分片doc value内存占用（byte）": "All Shards Doc Value Memory Usage (byte)",
    "所有分片field内存占用（byte）": "All Shards Field Memory Usage (byte)",
    "所有分片fixed bit set内存占用（byte）": "All Shards Fixed Bit Set Memory Usage (byte)",
    "所有分片normalization factor内存占用（byte）": "All Shards Normalization Factor Memory Usage (byte)",
    "所有分片point内存占用（byte）": "All Shards Point Memory Usage (byte)",
    "所有分片segment容量": "All Shards Segment Size",
    "所有分片segment总数": "All Shards Total Segment Count",
    "所有分片term内存占用（byte）": "All Shards Term Memory Usage (byte)",
    "所有分片version map内存占用（byte）": "All Shards Version Map Memory Usage (byte)",
    "所有分片索引写入数据量（byte）": "All Shards Index Write Volume (byte)",
    "所有分片索引容量（byte）": "All Shards Index Size (byte)",
    "排队等待线程任务数量": "Queued Thread Tasks",
    "暂缓分配的分片数量，当节点丢失，会发生选主和数据拷贝。为了较少网络抖动等原因导致的重分配情况，配置delay参数，该值为等待delay到期将被重分配的分片数量\nShards delayed to reduce reallocation overhead": "Number of shards whose allocation is delayed. When a node is lost, primary election and data copying take place. To reduce reallocation caused by network jitter and similar issues, the delay parameter is configured; this value is the number of shards waiting for the delay to expire before being reallocated\nShards delayed to reduce reallocation overhead",
    "未分配的分片数量\nThe number of shards that exist in the cluster state, but cannot be found in the cluster itself": "Unassigned Shards\nThe number of shards that exist in the cluster state, but cannot be found in the cluster itself",
    "查询操作耗时（秒）": "Query Time (s)",
    "活跃主分片数\nThe number of primary shards in your cluster. This is an aggregate total across all indices.": "Active Primary Shards\nThe number of primary shards in your cluster. This is an aggregate total across all indices.",
    "活跃分片数\nAggregate total of all shards across all indices, which includes replica shards": "Active Shards\nAggregate total of all shards across all indices, which includes replica shards",
    "活跃线程数": "Active Threads",
    "磁盘使用率": "Disk Usage",
    "等待执行的集群变更任务数量": "Pending Cluster Tasks",
    "索引存储限制耗时（秒）": "Index Throttle Time (s)",
    "索引操作耗时（秒）": "Indexing Time (s)",
    "线程池complete次数": "Thread Pool Completed Count",
    "线程池reject次数": "Thread Pool Rejected Count",
    "绿色：健康，黄色：存在副本分片异常，红色：存在主分片异常": "Green: healthy, Yellow: some replica shards abnormal, Red: some primary shards abnormal",
    "网络流量（byte）": "Network Traffic (byte)",
    "老版本的filter cache内存剔除次数": "Legacy Filter Cache Evictions",
    "节点文档总数，不包含已删除文档和子文档以及刚索引的文档": "Total documents on the node, excluding deleted documents, nested documents and documents that were just indexed",
    "节点断路器阻断总数": "Node Circuit Breaker Tripped Total",
    "迁移中分片数量\nThe number of shards that are currently moving from one node to another node.": "Relocating Shards\nThe number of shards that are currently moving from one node to another node.",
    "进程CPU占比": "Process CPU Percent",
    "进程内存占用/限制/峰值（byte）": "Process Memory Used/Limit/Peak (byte)",
    "集群数据节点数量": "Cluster Data Node Count",
    "集群断路器阻断总数": "Cluster Circuit Breaker Tripped Total",
    "集群节点数量": "Cluster Node Count",
    "预估内存大小和限制内存大小": "Estimated Memory Size and Memory Limit"
}
