{
    "1) 从告警标签取 backend 与 server，登录该后端主机确认应用进程与端口存活；\n2) 在 HAProxy 主机上 curl 该后端地址，区分是应用故障还是网络不通；\n3) 应用故障则按应用自身的恢复流程处理，恢复后确认 HAProxy 已自动把它重新加回（haproxy_server_up 回到 1）；\n4) 用 echo 'show stat' | socat stdio /var/run/haproxy.sock 确认同 backend 还剩几台可用，余量不足时立即扩容。": "1) Take backend and server from the alert labels and log in to that backend host to confirm the application process and port are alive;\n2) curl the backend address from the HAProxy host to tell an application failure apart from a network problem;\n3) For an application failure, follow that application's own recovery procedure, then confirm HAProxy has automatically added it back (haproxy_server_up returns to 1);\n4) Run echo 'show stat' | socat stdio /var/run/haproxy.sock to see how many servers are still available in the backend, and scale out immediately if headroom is low.",
    "1) 从告警标签取 backend 名，先确认是所有 server 都在报错还是集中在某一台（对比 haproxy_server_http_responses_total{code=\"5xx\"}）；\n2) 集中在单台则把该 server 从 backend 摘除止血；\n3) 全量报错则登录后端应用查日志定位根因（依赖超时、连接池耗尽、发布引入的 bug）；\n4) 若与近期发布时间吻合，优先回滚而不是继续排查。": "1) Take the backend name from the alert labels and first determine whether every server is erroring or only one (compare haproxy_server_http_responses_total{code=\"5xx\"});\n2) If it is one server, remove it from the backend to stop the bleeding;\n3) If all of them are erroring, check the backend application logs for the root cause (dependency timeouts, exhausted connection pool, a bug from a release);\n4) If it lines up with a recent release, roll back first rather than keep investigating.",
    "1) 从告警标签取 backend 名，对比 haproxy_backend_http_connect_time_average_seconds 与 http_queue_time_average_seconds，区分是建连慢、排队久还是后端处理慢；\n2) 排队久按请求排队处理（调 maxconn 或扩容）；\n3) 后端处理慢则去应用侧排查慢查询、GC、下游依赖超时；\n4) 定位期间可临时调大 timeout server 避免大量请求被截断，但不要当成长期方案。": "1) Take the backend name from the alert labels and compare haproxy_backend_http_connect_time_average_seconds with http_queue_time_average_seconds to tell slow connects, long queueing and slow backend processing apart;\n2) For long queueing, handle it as request queueing (raise maxconn or scale out);\n3) For slow processing, investigate on the application side (slow queries, GC, downstream timeouts);\n4) You may raise timeout server temporarily so requests are not cut off while you investigate, but do not treat it as a long-term fix.",
    "1) 从告警标签取 backend 名，对照 haproxy_backend_http_response_time_average_seconds 判断是后端变慢还是并发上涨；\n2) 后端变慢则去后端应用侧排查（慢查询、GC、依赖超时）；\n3) 并发上涨则检查 backend 与 server 上的 maxconn 是否设置过小，适当调大并 reload；\n4) 后端容量确实不足时，扩容 server 数量。": "1) Take the backend name from the alert labels and compare with haproxy_backend_http_response_time_average_seconds to tell a slower backend apart from rising concurrency;\n2) If the backend got slower, investigate on the application side (slow queries, GC, dependency timeouts);\n3) If concurrency rose, check whether maxconn on the backend and its servers is set too low, raise it and reload;\n4) If backend capacity is genuinely insufficient, add more servers.",
    "1) 从告警标签取 backend 名，执行 echo 'show stat' | socat stdio /var/run/haproxy.sock 查看该 backend 下每台 server 的状态与下线原因；\n2) 逐台在 HAProxy 主机上 curl 后端地址，确认是后端集体故障还是 HAProxy 到后端的网络问题；\n3) 后端正常则检查 backend 的 check 配置（探测路径、超时、rise/fall）是否过于激进；\n4) 短期内无法恢复时，把流量切到备用 backend 或降级页面。": "1) Take the backend name from the alert labels and run echo 'show stat' | socat stdio /var/run/haproxy.sock to see the state and down reason of every server in that backend;\n2) curl each backend address from the HAProxy host to tell a collective backend failure apart from a HAProxy-to-backend network problem;\n3) If the backends are healthy, check whether the backend's check configuration (probe path, timeout, rise/fall) is too aggressive;\n4) If it cannot be recovered quickly, switch traffic to a standby backend or a degraded page.",
    "1) 从告警标签取 backend 名，用 echo 'show stat' | socat stdio /var/run/haproxy.sock 看是哪台 server 的连接错误在涨；\n2) 在 HAProxy 主机上 telnet 或 curl 该后端地址，确认是端口不通、连接被拒还是建连超时；\n3) 后端进程正常则排查中间网络、安全组，以及后端的 backlog/somaxconn 是否被打满；\n4) 检查 backend 的 timeout connect 是否设置过短导致误判。": "1) Take the backend name from the alert labels and run echo 'show stat' | socat stdio /var/run/haproxy.sock to see which server's connection errors are growing;\n2) telnet or curl that backend address from the HAProxy host to tell a closed port, a refused connection and a connect timeout apart;\n3) If the backend process is healthy, check the network in between, the security groups, and whether the backend's backlog/somaxconn is saturated;\n4) Check whether the backend's timeout connect is set too short and causing false positives.",
    "1) 从告警标签取 frontend 名，确认是业务量自然增长还是遭遇异常流量（慢连接、爬虫）；\n2) 正常增长则调大 frontend 的 maxconn 与 global maxconn，同步确认系统 ulimit -n 与内存足够后 reload；\n3) 异常流量则在 frontend 上加 stick-table 限流或直接封禁来源 IP；\n4) 检查是否有大量连接处于长时间空闲，可用 timeout client 收敛。": "1) Take the frontend name from the alert labels and determine whether this is natural traffic growth or abnormal traffic (slow connections, crawlers);\n2) For natural growth, raise the frontend maxconn and the global maxconn, confirm the system ulimit -n and memory are sufficient, then reload;\n3) For abnormal traffic, add a stick-table rate limit on the frontend or block the source IPs outright;\n4) Check whether many connections are sitting idle for a long time and tighten timeout client.",
    "1) 登录主机执行 systemctl status haproxy 确认进程是否存活；\n2) 进程存活则用 curl -s http://\u003chost\u003e:\u003cport\u003e/stats\\;csv 手工验证 stats 接口，排查是否被 ACL 或防火墙挡住；\n3) 进程不在则先执行 haproxy -c -f /etc/haproxy/haproxy.cfg 校验配置再启动，并看 /var/log/haproxy.log 定位退出原因；\n4) 确认上层 VIP（keepalived 等）已完成切换，否则手工把流量切到备节点。": "1) Log in to the host and run systemctl status haproxy to confirm the process is alive;\n2) If it is alive, verify the stats endpoint by hand with curl -s http://\u003chost\u003e:\u003cport\u003e/stats\\;csv and check whether an ACL or firewall is blocking it;\n3) If it is not running, validate the config first with haproxy -c -f /etc/haproxy/haproxy.cfg, then start it and check /var/log/haproxy.log for the exit reason;\n4) Confirm the VIP layer (keepalived or similar) has failed over, otherwise move traffic to the standby node by hand.",
    "HAProxy backend 5xx 比例过高": "HAProxy backend 5xx ratio too high",
    "HAProxy backend 出现连接错误": "HAProxy backend connection errors",
    "HAProxy backend 响应时间过高": "HAProxy backend response time too high",
    "HAProxy backend 整体不可用": "HAProxy backend entirely unavailable",
    "HAProxy backend 请求排队": "HAProxy backend requests queueing",
    "HAProxy frontend 会话数逼近上限": "HAProxy frontend sessions approaching the limit",
    "HAProxy 后端服务器不可用": "HAProxy backend server unavailable",
    "HAProxy 实例不可达": "HAProxy instance unreachable"
}
