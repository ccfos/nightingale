interval = 15

[[instances]]
urls = [
     "http://<automq-or-otel-collector>:8890/metrics"
]

# AutoMQ metrics already contain an instance label; do not overwrite it.
url_label_key = "otel_collector"
url_label_value = "{{.Host}}"
labels = { source = "automq" }
