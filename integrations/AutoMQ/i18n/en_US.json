{
    "1) 从告警标签取 error 字段确认错误类型；\n2) NOT_LEADER_OR_FOLLOWER 类多为分区 leader 正在切换，确认集群是否在做再平衡；\n3) REQUEST_TIMED_OUT 类看 broker 负载与磁盘 IO；\n4) 认证授权类错误核对客户端的 ACL 与凭据配置。": "1) Take the error field from the alert labels to identify the error type;\n2) NOT_LEADER_OR_FOLLOWER usually means partition leaders are moving — check whether the cluster is rebalancing;\n3) For REQUEST_TIMED_OUT, check broker load and disk IO;\n4) For authentication/authorization errors, verify the client's ACLs and credentials.",
    "1) 从告警标签取 operation_name 确认是哪类操作失败（上传、下载、删除）；\n2) 在 broker 日志里找对应的 S3 错误码，区分权限（403）、限流（503 SlowDown）、网络超时；\n3) 限流的联系对象存储侧提配额，或降低写入并发；\n4) 权限类的核对 AK/SK 与 bucket 策略，网络类的检查到对象存储的连通性与带宽。": "1) Take operation_name from the alert labels to identify which operation is failing (upload, download, delete);\n2) Find the matching S3 error code in the broker log and separate permissions (403), throttling (503 SlowDown) and network timeouts;\n3) For throttling, ask the object storage side to raise the quota or reduce write concurrency;\n4) For permissions, verify the AK/SK and bucket policy; for network issues, check connectivity and bandwidth to the object store.",
    "1) 从告警标签取 topic 与 consumer_group，用 kafka-consumer-groups.sh --describe --group \u003cgroup\u003e 看各分区 LAG 分布；\n2) 积压集中在个别分区说明数据倾斜，检查生产端分区键；\n3) 均匀积压则是消费者处理慢——先看消费端日志有无异常重试，再扩 consumer 实例（不超过分区数）；\n4) 确认消费者没有全部掉线。": "1) Take topic and consumer_group from the alert labels and run kafka-consumer-groups.sh --describe --group \u003cgroup\u003e to see the LAG distribution per partition;\n2) Lag concentrated on a few partitions means data skew — check the producer's partition key;\n3) Evenly spread lag means slow consumers: check the consumer logs for abnormal retries, then add consumer instances (never beyond the partition count);\n4) Confirm the consumers have not all dropped out.",
    "1) 在各 broker 上确认进程存活与到 controller quorum 的网络连通性；\n2) KRaft 模式下用 kafka-metadata-quorum.sh --bootstrap-server \u003cbroker\u003e describe --status 看 quorum 状态；\n3) 为 0 时先恢复宕掉的 controller 节点，恢复多数派后会自动选举；\n4) 大于 1 时排查网络分区，确认后停掉少数派一侧让集群重新收敛。": "1) On each broker confirm the process is alive and can reach the controller quorum;\n2) In KRaft mode run kafka-metadata-quorum.sh --bootstrap-server \u003cbroker\u003e describe --status to check the quorum;\n3) When the count is 0, restore the failed controller nodes first — the cluster elects automatically once the majority is back;\n4) When it is above 1, investigate the network partition and then stop the minority side so the cluster reconverges.",
    "1) 找出消费流量最大的 topic 与消费组；\n2) 确认是否有消费组在做全量回溯消费（从头重新消费），这是出向流量突增的常见原因；\n3) 确认消费者数量是否远超实际需要，重复消费同一 topic 的消费组是否可以合并；\n4) 按需扩容 broker 或提升网卡带宽。": "1) Identify the topics and consumer groups with the highest consume traffic;\n2) Check whether a consumer group is doing a full replay from the beginning — this is the most common cause of an outbound traffic spike;\n3) Check whether there are far more consumers than needed, and whether groups consuming the same topic can be merged;\n4) Add brokers or NIC bandwidth as needed.",
    "1) 检查生产端的 batch.size 与 linger.ms——过小会导致每条消息一个请求；\n2) 调大 linger.ms 让生产端攒批发送，能大幅降低请求数；\n3) 确认是否有生产者未开启压缩（compression.type）；\n4) 结合 broker CPU 判断是否需要扩容。": "1) Check the producer's batch.size and linger.ms — values that are too small mean one request per message;\n2) Raising linger.ms lets the producer batch and dramatically reduces the request count;\n3) Check whether producers have compression enabled (compression.type);\n4) Combine with broker CPU to judge whether to scale out.",
    "1) 用 kafka-topics.sh --bootstrap-server \u003cbroker\u003e --describe 统计各 topic 的分区数，找出分区数明显偏大的 topic；\n2) 确认分区数是否与实际吞吐匹配——很多 topic 的分区是拍脑袋定的；\n3) 分区只能增不能减，需要收敛的只能新建 topic 后迁移数据；\n4) 清理长期无流量的废弃 topic，这是最快见效的。": "1) Run kafka-topics.sh --bootstrap-server \u003cbroker\u003e --describe to count partitions per topic and find the ones that are clearly oversized;\n2) Check whether the partition count matches actual throughput — many topics are sized by guesswork;\n3) Partitions can only be added, never removed, so shrinking means creating a new topic and migrating;\n4) Delete long-idle abandoned topics — that is the quickest win.",
    "1) 用 kafka-topics.sh 结合 topic 级流量指标找出流量最大的 topic；\n2) 确认是业务量自然增长还是有客户端在异常重发；\n3) 生产端未开压缩的开启 compression.type=lz4/zstd，通常能立竿见影降低带宽；\n4) 确认后按需扩容 broker 或提升网卡带宽。": "1) Use kafka-topics.sh together with per-topic traffic metrics to find the highest-traffic topic;\n2) Determine whether it is natural growth or a client resending abnormally;\n3) If producers have no compression, enable compression.type=lz4/zstd — that usually cuts bandwidth immediately;\n4) Once confirmed, add brokers or NIC bandwidth as needed.",
    "1) 用 ss -tn state established '( sport = :9092 )' | awk '{print $5}' | cut -d: -f1 | sort | uniq -c | sort -rn 看连接来自哪些客户端；\n2) 区分是客户端实例增多还是连接泄漏（未复用 producer/consumer）；\n3) 应急调大 broker 的文件句柄上限；\n4) 根因侧让客户端复用连接，并确认没有短连接式的反复建连。": "1) Run ss -tn state established '( sport = :9092 )' | awk '{print $5}' | cut -d: -f1 | sort | uniq -c | sort -rn to see which clients the connections come from;\n2) Tell more client instances apart from a connection leak (producers/consumers not being reused);\n3) As an emergency measure raise the broker's file handle limit;\n4) For the root cause, make clients reuse connections and confirm there is no short-lived reconnect pattern.",
    "1) 看 broker 的磁盘 IO 与 page cache 命中——Fetch 慢通常是读到了冷数据要落盘；\n2) 确认是否有消费者在从很早的 offset 重新消费，把冷数据大量读入；\n3) 检查网络带宽是否打满；\n4) 持续存在则考虑扩容 broker 或增加内存提升 page cache 命中率。": "1) Check broker disk IO and page cache hit rate — slow Fetch usually means cold data being read from disk;\n2) Check whether a consumer is replaying from a very old offset and pulling large amounts of cold data;\n3) Check whether network bandwidth is saturated;\n4) If it persists, add brokers or memory to raise the page cache hit rate.",
    "1) 看 broker 的磁盘写入延迟与网络带宽是否打满；\n2) 确认 acks 配置——acks=all 时延迟受最慢副本影响，检查是否有副本落后；\n3) 用 kafka-topics.sh --describe --under-replicated-partitions 确认副本同步是否正常；\n4) 存储侧瓶颈明显的扩容 broker 或换更快的磁盘。": "1) Check broker disk write latency and whether network bandwidth is saturated;\n2) Check the acks setting — with acks=all latency follows the slowest replica, so look for lagging replicas;\n3) Run kafka-topics.sh --describe --under-replicated-partitions to confirm replication is healthy;\n4) If the storage side is clearly the bottleneck, add brokers or move to faster disks.",
    "1) 确认消费者数量与 fetch.min.bytes / fetch.max.wait.ms 配置——这两个值过小会导致空转式高频拉取；\n2) 调大 fetch.min.bytes 让消费者攒批拉取，能显著降低请求数；\n3) 确认是否有消费者处于空转（无数据仍高频拉取）；\n4) 结合 broker CPU 判断是否需要扩容。": "1) Check the consumer count together with fetch.min.bytes / fetch.max.wait.ms — values that are too small cause busy high-frequency polling;\n2) Raising fetch.min.bytes makes consumers batch their fetches and significantly reduces request count;\n3) Check whether consumers are spinning (polling frequently with no data);\n4) Combine with broker CPU to judge whether to scale out.",
    "Kafka Broker 连接数过多": "Kafka broker has too many connections",
    "Kafka Fetch 请求高延迟": "Kafka fetch request latency high",
    "Kafka Partition 数量过多": "Kafka partition count too high",
    "Kafka Produce 请求高延迟": "Kafka produce request latency high",
    "Kafka active controller 数目异常": "Kafka active controller count abnormal",
    "Kafka 每秒 Fetch 请求超过1000": "Kafka fetch requests exceed 1000 per second",
    "Kafka 每秒 Produce 请求超过1000": "Kafka produce requests exceed 1000 per second",
    "Kafka 消费延迟较高": "Kafka consumer lag high",
    "Kafka 请求 S3 失败率较高": "Kafka S3 request failure rate high",
    "Kafka 请求错误率过高": "Kafka request error rate too high",
    "Kafka 集群入向网络流量较高": "Kafka cluster inbound network traffic high",
    "Kafka 集群出向网络流量较高": "Kafka cluster outbound network traffic high",
    "下钻": "Drill Down"
}
