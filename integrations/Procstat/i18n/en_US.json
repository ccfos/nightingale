{
    "1) 用 cat /proc/\u003cpid\u003e/limits 确认 Max open files 的软硬上限；\n2) systemd 服务在 unit 里加 LimitNOFILE=65535 后 daemon-reload 并重启；\n3) 非 systemd 的在启动脚本加 ulimit -n 65535，或改 /etc/security/limits.conf；\n4) 注意软上限改完必须重启进程才生效。": "1) Run cat /proc/\u003cpid\u003e/limits to see the soft and hard Max open files;\n2) For systemd services add LimitNOFILE=65535 to the unit, then daemon-reload and restart;\n3) For non-systemd add ulimit -n 65535 to the startup script or edit /etc/security/limits.conf;\n4) Note the process must be restarted for a changed soft limit to take effect.",
    "1) 用 ps -ef | grep \u003c进程关键字\u003e 确认确实不在，排除采集侧匹配规则写错；\n2) 查该进程日志与 dmesg -T | grep -i 'killed process'，区分崩溃、被 OOM 杀死、人工停止；\n3) 按服务自身流程拉起，确认 systemd Restart 策略生效；\n4) 反复退出的先定位根因再拉起，避免重启循环。": "1) Run ps -ef | grep \u003cprocess keyword\u003e to confirm it is really gone and rule out a wrong match rule on the collector side;\n2) Check its log and dmesg -T | grep -i 'killed process' to tell a crash, an OOM kill and a manual stop apart;\n3) Start it through the service's own procedure and confirm the systemd Restart policy works;\n4) If it keeps exiting, find the root cause before starting it again to avoid a restart loop."
}
