{
    "1) 从告警标签取 cluster，在任一存活节点执行 emqx ctl cluster status 确认哪些节点 stopped；\n2) 登录停止的节点按单节点故障排查（进程、内存、磁盘、日志）；\n3) 节点短期无法恢复的，执行 emqx ctl cluster force-leave \u003cnode\u003e 把它移出集群，避免路由表持续尝试同步；\n4) 恢复期间关注剩余节点的连接数与内存，必要时先扩容再让设备重连。": "1) Take cluster from the alert labels and run emqx ctl cluster status on any surviving node to see which nodes are stopped;\n2) Log in to the stopped node and troubleshoot it as a single-node failure (process, memory, disk, logs);\n3) If the node cannot be recovered soon, run emqx ctl cluster force-leave \u003cnode\u003e to remove it so the routing table stops trying to sync with it;\n4) During recovery watch the connection count and memory on the remaining nodes and scale out before letting devices reconnect if needed.",
    "1) 从告警标签取 node，在 Dashboard 的规则引擎页面查看各规则的失败计数，定位失败的规则与动作；\n2) 查看 log/emqx.log 里该动作的报错，区分是下游不可达、超时还是数据格式不匹配；\n3) 下游不可达的先修下游并确认桥接的连接池与超时配置合理；\n4) 数据格式问题的检查规则 SQL 的字段映射，修改后用 Dashboard 的测试功能验证再发布。": "1) Take node from the alert labels and check the per-rule failure counters on the rule engine page of the Dashboard to identify the failing rule and action;\n2) Read that action's error in log/emqx.log to distinguish an unreachable downstream, a timeout, and a data format mismatch;\n3) For an unreachable downstream, fix it first and confirm the bridge's connection pool and timeout settings are sensible;\n4) For format problems, review the field mapping in the rule SQL, verify with the Dashboard's test feature, and then publish.",
    "1) 从告警标签取 node，执行 emqx ctl listeners 查看各监听器的当前连接与最大连接配置；\n2) 执行 cat /proc/$(pgrep -f beam.smp)/limits | grep 'open files' 确认实际 fd 上限；\n3) 调大 systemd 的 LimitNOFILE 与 emqx.conf 中 node.max_ports，重启后生效，注意评估内存（每连接约占几十 KB）；\n4) 若是异常客户端疯狂重连占用连接，先在网关侧限流并检查客户端的重连退避策略。": "1) Take node from the alert labels and run emqx ctl listeners to see the current and maximum connections per listener;\n2) Run cat /proc/$(pgrep -f beam.smp)/limits | grep 'open files' to confirm the actual fd limit;\n3) Raise LimitNOFILE in systemd and node.max_ports in emqx.conf and restart to apply, re-checking memory (each connection costs tens of KB);\n4) If misbehaving clients are consuming connections by reconnecting furiously, rate-limit them at the gateway and review the client reconnect backoff.",
    "1) 从告警标签取 node，执行 emqx ctl metrics | grep dropped 区分丢弃原因（queue_full / no_subscribers / expired）；\n2) queue_full 的说明订阅端消费太慢，先排查消费者，再评估调大 mqueue_max_len；\n3) no_subscribers 的确认发布端主题拼写是否正确、订阅端是否已断开；\n4) expired 的检查消息的 Message Expiry Interval 与实际投递时延是否匹配。": "1) Take node from the alert labels and run emqx ctl metrics | grep dropped to separate the drop reasons (queue_full / no_subscribers / expired);\n2) queue_full means subscribers are consuming too slowly; investigate the consumers first, then consider raising mqueue_max_len;\n3) For no_subscribers, confirm the publisher's topic spelling and whether the subscriber has disconnected;\n4) For expired, check that the Message Expiry Interval matches the real delivery latency.",
    "1) 从告警标签取 node，执行 emqx ctl vm memory 查看各部分内存占用（processes/binary/ets）；\n2) binary 占比高通常是消息积压在会话队列里，检查是否有消费不及时的订阅者，用 emqx ctl topics list 确认热点主题；\n3) processes 占比高多为会话数过多，按 Erlang 进程告警的思路处理持久会话；\n4) 应急可调低 mqueue_max_len 限制单会话队列长度，避免个别慢消费者拖垮整个节点。": "1) Take node from the alert labels and run emqx ctl vm memory to see the breakdown (processes/binary/ets);\n2) A high binary share usually means messages piling up in session queues; look for subscribers that are not consuming fast enough and confirm the hot topics with emqx ctl topics list;\n3) A high processes share usually means too many sessions; handle it as for the Erlang process alert;\n4) As a stopgap lower mqueue_max_len to cap per-session queue length so a single slow consumer cannot drag the node down.",
    "1) 从告警标签取 node，执行 emqx ctl vm 查看 process/used 与 process/limit 的实时值；\n2) 对比连接数增长趋势，确认是正常业务增长还是会话泄漏（大量 clean_session=false 的持久会话不断累积）；\n3) 持久会话堆积的检查 session 过期时间配置，并用 emqx ctl sessions list 抽查；\n4) 确实需要更大容量的调大 emqx.conf 中 node.process_limit 并重启，同时评估内存。": "1) Take node from the alert labels and run emqx ctl vm to see the live process/used and process/limit values;\n2) Compare against connection growth to decide whether this is normal growth or a session leak (persistent sessions with clean_session=false accumulating);\n3) For accumulating persistent sessions, review the session expiry configuration and spot-check with emqx ctl sessions list;\n4) If you genuinely need more capacity, raise node.process_limit in emqx.conf and restart, re-checking memory.",
    "1) 从告警标签取 node，查看 log/emqx.log 中的认证失败记录，确认失败的 clientid 与来源 IP 分布；\n2) 来源集中且 clientid 规律的多为认证后端故障，检查 emqx ctl authn list 配置的数据源（MySQL/Redis/HTTP）是否可达；\n3) 来源分散且 clientid 随机的判定为暴力破解，在防火墙或网关侧限流并开启黑名单；\n4) 确认是凭据批量过期导致的，先临时放行再推动设备侧更新凭据。": "1) Take node from the alert labels and read the authentication failure entries in log/emqx.log to see the clientid and source IP distribution;\n2) Concentrated sources with regular clientids usually mean a broken authentication backend; check that the data source configured in emqx ctl authn list (MySQL/Redis/HTTP) is reachable;\n3) Scattered sources with random clientids indicate brute forcing; rate-limit at the firewall or gateway and enable a blacklist;\n4) If a batch of credentials expired at once, allow them through temporarily and push the device side to update.",
    "1) 从告警标签取 node，登录该主机执行 emqx ctl status 与 systemctl status emqx 确认进程状态；\n2) 进程存活但状态异常的查看 log/emqx.log.1 尾部，常见为 Erlang 虚拟机内存耗尽或磁盘写满导致节点自保；\n3) 进程不在则先执行 df -h 与 free -g 排除资源问题再启动；\n4) 恢复后执行 emqx ctl cluster status 确认该节点已重新加入集群，并观察连接数是否回流。": "1) Take node from the alert labels, log in to that host, and run emqx ctl status and systemctl status emqx to check the process;\n2) If the process is alive but the status is abnormal, read the tail of log/emqx.log.1; an exhausted Erlang VM heap or a full disk driving the node into self-protection are the usual causes;\n3) If the process is gone, rule out resource problems with df -h and free -g before starting it;\n4) After recovery run emqx ctl cluster status to confirm the node rejoined the cluster and watch the connection count return.",
    "1) 先确认是否是计划内发布或扩缩容；\n2) 非计划则查看 log/emqx.log.1 与 journalctl -u emqx --since '-30min' 定位退出原因；\n3) 执行 dmesg -T | grep -i 'killed process' 排除被 OOM 杀掉的可能，是则按内存告警处理；\n4) 重启后关注连接数回升曲线与 CPU，必要时在网关侧对重连做限速，避免风暴打垮刚恢复的节点。": "1) First confirm whether this was a planned release or a scaling operation;\n2) If unplanned, check log/emqx.log.1 and journalctl -u emqx --since '-30min' for the cause;\n3) Run dmesg -T | grep -i 'killed process' to rule out an OOM kill, and handle it as a memory alert if that is the case;\n4) After the restart watch the connection recovery curve and CPU, and rate-limit reconnections at the gateway if needed so the storm does not take down the node again.",
    "EMQX Erlang 进程数逼近上限": "EMQX Erlang process count approaching the limit",
    "EMQX 客户端认证失败激增": "EMQX client authentication failures spiking",
    "EMQX 消息丢弃激增": "EMQX message drops spiking",
    "EMQX 节点不在运行状态": "EMQX node not running",
    "EMQX 节点内存使用率过高": "EMQX node memory usage too high",
    "EMQX 节点近期发生过重启": "EMQX node restarted recently",
    "EMQX 规则引擎动作失败率过高": "EMQX rule engine action failure rate too high",
    "EMQX 连接数逼近文件描述符上限": "EMQX connections approaching the file descriptor limit",
    "EMQX 集群存在已停止的节点": "EMQX cluster has a stopped node"
}
