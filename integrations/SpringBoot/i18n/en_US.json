{
    "1) 从告警标签取 instance 定位实例，到日志系统里按 level=ERROR 检索该时间窗的异常栈；\n2) 按异常类型归类，区分是偶发（下游抖动、超时重试）还是稳定复现（代码 bug、配置错误）；\n3) 稳定复现的定位代码修复；偶发的确认重试与降级逻辑是否生效；\n4) 若 ERROR 量突增且与发布时间吻合，优先回滚。": "1) Take instance from the alert labels, then search your log system for ERROR-level stack traces in that time window;\n2) Group by exception type to tell an intermittent problem (downstream jitter, timeout retries) from a reproducible one (code bug, misconfiguration);\n3) Fix reproducible ones in code; for intermittent ones confirm retry and fallback logic actually works;\n4) If the ERROR volume spiked in line with a release, roll back first.",
    "1) 从告警标签取 uri 定位接口，再到应用日志里按该路径搜异常栈；\n2) 区分是全量失败还是部分失败——部分失败常见于某个下游实例异常或数据边界条件；\n3) 若与近期发布时间吻合，优先回滚而不是继续排查；\n4) 确认下游依赖（数据库、缓存、第三方接口）是否同时异常。": "1) Take uri from the alert labels to identify the endpoint, then search the application log for stack traces on that path;\n2) Tell total failure from partial failure — partial failure usually means one bad downstream instance or a data edge case;\n3) If it lines up with a recent release, roll back rather than keep investigating;\n4) Check whether downstream dependencies (database, cache, third-party APIs) are failing at the same time.",
    "1) 从告警标签取 uri 定位是哪个接口；\n2) 用 arthas 的 trace 命令或 APM 链路追踪，看耗时集中在哪一段（数据库、下游 RPC、还是本地计算）；\n3) 数据库慢的按慢查询路径排查补索引；下游慢的确认对方状态并加熔断降级；\n4) 确认线程池是否已被慢请求占满导致排队放大，必要时隔离线程池。": "1) Take uri from the alert labels to identify the endpoint;\n2) Use arthas trace or APM distributed tracing to see which segment consumes the time (database, downstream RPC, or local computation);\n3) For a slow database, follow the slow-query path and add indexes; for a slow downstream, check its state and add a circuit breaker or fallback;\n4) Check whether the thread pool is saturated by slow requests and amplifying the queue — isolate the pool if so.",
    "1) 用 jcmd \u003cpid\u003e VM.native_memory summary（需开 NativeMemoryTracking）或 arthas 的 memory 命令看各区占用；\n2) Metaspace 持续增长通常是类加载器泄漏——常见于反复热部署、动态代理、脚本引擎；\n3) 应急调大 -XX:MaxMetaspaceSize 并重启；\n4) 根因侧排查是否有自定义 ClassLoader 未释放，用 jmap -clstats 看类加载器数量是否异常。": "1) Run jcmd \u003cpid\u003e VM.native_memory summary (requires NativeMemoryTracking) or the arthas memory command to see each region's usage;\n2) Steadily growing Metaspace usually means a class loader leak — common with repeated hot deploys, dynamic proxies and script engines;\n3) As an emergency measure raise -XX:MaxMetaspaceSize and restart;\n4) For the root cause, look for a custom ClassLoader that is never released, and check jmap -clstats for an abnormal class loader count.",
    "1) 看 jvm_gc_pause_seconds 确认 Full GC 是否变频繁、停顿多长；\n2) 用 jmap -histo:live \u003cpid\u003e 或 arthas 的 heapdump 抓堆，按对象数排序找出异常增长的类；\n3) 区分是业务量增长（调大 -Xmx）还是内存泄漏（必须在代码里修复，扩堆只是拖延）；\n4) 应急可重启实例释放，但要先抓好堆快照，否则现场就丢了。": "1) Check jvm_gc_pause_seconds to see whether full GC became more frequent and how long the pauses are;\n2) Capture a heap with jmap -histo:live \u003cpid\u003e or the arthas heapdump, then sort by object count to find the abnormally growing classes;\n3) Tell traffic growth (raise -Xmx) apart from a memory leak (must be fixed in code — a bigger heap only postpones it);\n4) You can restart the instance to recover, but capture the heap snapshot first or you lose the evidence.",
    "JVM仪表盘": "JVM Dashboard",
    "springboot HEAP内存使用率大于20%": "SpringBoot heap memory usage exceeds 20%",
    "springboot HEAP内存使用率大于85%": "SpringBoot heap memory usage above 85%",
    "springboot HTTP请求延迟大于10s": "SpringBoot HTTP request latency exceeds 10s",
    "springboot HTTP错误数": "SpringBoot HTTP errors",
    "springboot NOHEAP内存使用率大于30%": "SpringBoot non-heap memory usage exceeds 30%",
    "springboot 事件错误数": "SpringBoot event errors",
    "springboot 非堆内存使用率大于85%": "SpringBoot non-heap memory usage above 85%"
}
