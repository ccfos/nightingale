{
    "1) 用 SELECT * FROM V$RECOVERY_FILE_DEST 确认 FRA 的配额与已用空间；\n2) 用 RMAN 的 crosscheck archivelog all; delete expired archivelog all; 清理已备份的归档；\n3) 确认归档备份任务是否正常执行——归档堆积通常是备份失败导致的；\n4) 长期需调大 db_recovery_file_dest_size 或把归档目录迁到更大的存储。": "1) Run SELECT * FROM V$RECOVERY_FILE_DEST to check the FRA quota and used space;\n2) Use RMAN's crosscheck archivelog all; delete expired archivelog all; to clear already-backed-up archives;\n3) Confirm the archive backup job is running — a pile-up of archives is usually caused by failed backups;\n4) Longer term, raise db_recovery_file_dest_size or move the archive destination to larger storage.",
    "1) 用 SELECT COUNT(*) FROM V$PROCESS 与 SHOW PARAMETER processes 对比当前水位与上限；\n2) 用 SELECT machine, program, COUNT(*) FROM V$SESSION GROUP BY 1,2 ORDER BY 3 DESC 找出连接数异常的应用；\n3) 区分业务增长与应用连接池泄漏（大量 INACTIVE 会话堆积）；\n4) 应急可调大 processes 参数（需重启实例），根因侧收敛应用连接池上限。": "1) Compare SELECT COUNT(*) FROM V$PROCESS against SHOW PARAMETER processes to see the current level versus the limit;\n2) Run SELECT machine, program, COUNT(*) FROM V$SESSION GROUP BY 1,2 ORDER BY 3 DESC to find the application with an abnormal connection count;\n3) Tell business growth apart from an application connection pool leak (many INACTIVE sessions piling up);\n4) As an emergency measure raise the processes parameter (requires an instance restart); for the root cause, cap the application connection pool.",
    "1) 用 SELECT sid, serial#, sql_id, event, seconds_in_wait FROM V$SESSION WHERE status='ACTIVE' AND type='USER' ORDER BY seconds_in_wait DESC 看活跃会话在等什么；\n2) 等待事件集中在某类（如 enq: TX - row lock contention）说明有锁冲突，顺着 blocking_session 找源头；\n3) 用 sql_id 查 V$SQL 定位慢 SQL，必要时 ALTER SYSTEM KILL SESSION 止血；\n4) 定位后补索引或改写 SQL，并排查上游是否有突发流量。": "1) Run SELECT sid, serial#, sql_id, event, seconds_in_wait FROM V$SESSION WHERE status='ACTIVE' AND type='USER' ORDER BY seconds_in_wait DESC to see what the active sessions are waiting on;\n2) If the wait events cluster on one type (such as enq: TX - row lock contention) there is lock contention — follow blocking_session to the source;\n3) Use the sql_id against V$SQL to find the slow statement, and ALTER SYSTEM KILL SESSION to stop the bleeding if needed;\n4) Once located, add indexes or rewrite the SQL, and check upstream for a traffic spike.",
    "1) 用 SELECT tablespace_name, bytes/1024/1024 mb FROM DBA_DATA_FILES 与 DBA_FREE_SPACE 确认是哪个表空间、数据文件是否已达最大；\n2) 应急扩容：ALTER DATABASE DATAFILE '\u003cpath\u003e' RESIZE \u003c更大值\u003e，或 ADD DATAFILE 新增文件；\n3) 确认是否有大表或历史数据可归档删除，删除后需 shrink 或 move 才能真正释放；\n4) 检查数据文件是否开了 AUTOEXTEND 及其 MAXSIZE 设置。": "1) Use SELECT tablespace_name, bytes/1024/1024 mb FROM DBA_DATA_FILES together with DBA_FREE_SPACE to identify the tablespace and whether the data file has reached its maximum;\n2) Expand it: ALTER DATABASE DATAFILE '\u003cpath\u003e' RESIZE \u003clarger value\u003e, or ADD DATAFILE;\n3) Check whether large tables or historical data can be archived and deleted — after deletion you still need shrink or move to actually release space;\n4) Check whether the data file has AUTOEXTEND enabled and what its MAXSIZE is.",
    "1) 登录主机确认 pmon 进程与监听器状态（ps -ef | grep pmon、lsnrctl status）；\n2) 进程存活则用 sqlplus 手工连一次，区分实例夯住与监听/账号问题；\n3) 进程不在则看 alert_\u003cSID\u003e.log 尾部定位原因（ORA-00600、归档满、磁盘满）再拉起；\n4) 有 Data Guard 的立即评估是否需要 failover。": "1) Log in to the host and check the pmon process and listener state (ps -ef | grep pmon, lsnrctl status);\n2) If they are alive, connect by hand with sqlplus to tell a hung instance from a listener or account problem;\n3) If not running, read the tail of alert_\u003cSID\u003e.log to identify the cause (ORA-00600, full FRA, full disk) before starting it;\n4) If Data Guard is in place, immediately assess whether to fail over.",
    "Oracle: 数据库归档日志量大于100": "Oracle: archived log volume exceeds 100",
    "Oracle: 数据库总连接数大于500": "Oracle: total database connections exceed 500",
    "Oracle: 数据库活跃连接数大于100": "Oracle: active database connections exceed 100",
    "Oracle: 监控数据采集失败，可能已经挂了": "Oracle: metric collection failed, instance may be down",
    "Oracle: 表空间使用率大于95%": "Oracle: tablespace usage exceeds 95%"
}
