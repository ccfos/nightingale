{
    "1) SHOW FULL PROCESSLIST 找当前长时间运行的 SQL，必要时 KILL \u003cid\u003e 先止血；\n2) 用 mysqldumpslow -s t -t 10 \u003cslow_log\u003e 或 pt-query-digest 定位 TOP 慢 SQL；\n3) 对定位到的 SQL 执行 EXPLAIN 看执行计划，补索引或改写；\n4) 若是 long_query_time 设置过低导致的噪音，调整该参数而不是忽略告警。": "1) Run SHOW FULL PROCESSLIST to find long-running SQL and KILL \u003cid\u003e to stop the bleeding if needed;\n2) Use mysqldumpslow -s t -t 10 \u003cslow_log\u003e or pt-query-digest to find the top slow queries;\n3) Run EXPLAIN on the queries you found to inspect the plan, then add indexes or rewrite them;\n4) If the noise comes from long_query_time being set too low, tune that parameter instead of ignoring the alert.",
    "1) SHOW FULL PROCESSLIST 按来源主机统计连接，找出连接数异常的应用；\n2) 区分是业务突增还是应用连接池泄漏（大量长时间 Sleep 的连接堆积）；\n3) 应急可调大 max_connections，同时核对内存余量避免 OOM；\n4) 根因侧收敛应用连接池上限、缩短 wait_timeout。": "1) Run SHOW FULL PROCESSLIST and group connections by source host to find the application with an abnormal connection count;\n2) Tell a traffic spike apart from an application connection pool leak (many long-lived Sleep connections piling up);\n3) As an emergency measure raise max_connections, checking free memory at the same time to avoid an OOM;\n4) For the root cause, cap the application connection pool and shorten wait_timeout.",
    "1) SHOW FULL PROCESSLIST 看堆积的线程集中在哪条 SQL、哪张表；\n2) SHOW ENGINE INNODB STATUS 查是否存在锁等待或死锁；\n3) 对元凶 SQL 执行 KILL 止血后补索引或限流；\n4) 排查上游是否有突发流量或慢依赖导致连接迟迟不释放。": "1) Run SHOW FULL PROCESSLIST to see which SQL statement and which table the piled-up threads are concentrated on;\n2) Run SHOW ENGINE INNODB STATUS to check for lock waits or deadlocks;\n3) KILL the offending SQL to stop the bleeding, then add indexes or apply rate limiting;\n4) Check whether an upstream traffic spike or a slow dependency is keeping connections from being released.",
    "1) SHOW SLAVE STATUS\\G 看 Last_SQL_Error 定位卡住的事件；\n2) 若是可安全跳过的冲突（如重复主键），核对数据一致后执行 SET GLOBAL sql_slave_skip_counter=1 再 START SLAVE SQL_THREAD；\n3) 若主从数据已经分叉，用备份重建从库，不要盲目跳过；\n4) 恢复前先把读流量从该从库摘走。": "1) Run SHOW SLAVE STATUS\\G on the replica and read Last_SQL_Error to identify the event it is stuck on;\n2) If it is a conflict that can be safely skipped (such as a duplicate primary key), verify the data is consistent, then run SET GLOBAL sql_slave_skip_counter=1 followed by START SLAVE SQL_THREAD;\n3) If the primary and replica data have already diverged, rebuild the replica from a backup instead of blindly skipping;\n4) Take read traffic off this replica before you start.",
    "1) SHOW SLAVE STATUS\\G 看 Seconds_Behind_Master 与 SQL 线程当前正在执行的事件；\n2) 到主库用 SHOW PROCESSLIST 找是否有大事务或批量 DML 正在灌数据；\n3) 从库若是单线程回放，开启并行复制（slave_parallel_workers / slave_parallel_type=LOGICAL_CLOCK）；\n4) 延迟持续扩大时先把读流量切回主库或其他从库。": "1) Run SHOW SLAVE STATUS\\G and read Seconds_Behind_Master together with the event the SQL thread is currently applying;\n2) On the primary, run SHOW PROCESSLIST to check for a large transaction or a bulk DML loading data;\n3) If the replica applies single-threaded, enable parallel replication (slave_parallel_workers / slave_parallel_type=LOGICAL_CLOCK);\n4) If the lag keeps growing, move read traffic back to the primary or to another replica.",
    "1) 在从库执行 SHOW SLAVE STATUS\\G 看 Last_IO_Error；\n2) 常见原因是主库连不上、复制账号失效、或所需 binlog 已被 purge——分别处理网络、用 CHANGE MASTER TO 重设账号、或用备份重建从库；\n3) 处理完执行 START SLAVE IO_THREAD 并确认 Slave_IO_Running=Yes；\n4) 恢复前先把读流量从该从库摘走。": "1) Run SHOW SLAVE STATUS\\G on the replica and read Last_IO_Error;\n2) The usual causes are the primary being unreachable, an invalid replication account, or the required binlog having been purged — fix the network, reset the account with CHANGE MASTER TO, or rebuild the replica from a backup respectively;\n3) Then run START SLAVE IO_THREAD and confirm Slave_IO_Running=Yes;\n4) Take read traffic off this replica before you start.",
    "1) 查看 error log（SHOW VARIABLES LIKE 'log_error' 指向的文件）确认是崩溃、被 OOM kill 还是人工重启；\n2) 若是 OOM，核对 innodb_buffer_pool_size 与机器物理内存后调小；\n3) 若非计划内重启，检查 systemd 或容器编排是否在反复拉起；\n4) 重启后确认主从复制已自动恢复、应用连接池已重连。": "1) Check the error log (the file pointed to by SHOW VARIABLES LIKE 'log_error') to tell a crash, an OOM kill and a manual restart apart;\n2) If it was an OOM, compare innodb_buffer_pool_size against the machine's physical memory and lower it;\n3) If the restart was unplanned, check whether systemd or the container orchestrator is repeatedly restarting it;\n4) After the restart, confirm replication resumed automatically and application connection pools reconnected.",
    "1) 用 SHOW GLOBAL STATUS LIKE 'Open_files' 和 SHOW VARIABLES LIKE 'open_files_limit' 确认当前水位；\n2) 调大 my.cnf 中的 open_files_limit，并同步放开系统 ulimit -n 与 systemd 的 LimitNOFILE；\n3) 检查 table_open_cache 是否设置过大导致句柄膨胀；\n4) 排查是否有大量分区表或小表被同时访问。": "1) Run SHOW GLOBAL STATUS LIKE 'Open_files' and SHOW VARIABLES LIKE 'open_files_limit' to confirm the current level;\n2) Raise open_files_limit in my.cnf and raise the system ulimit -n and the systemd LimitNOFILE to match;\n3) Check whether table_open_cache is set too large and inflating the handle count;\n4) Check whether a large number of partitioned or small tables are being accessed at once.",
    "1) 登录主机用 systemctl status mysqld 或 docker ps 确认进程是否存活；\n2) 进程存活则用 mysql -h\u003chost\u003e -P\u003cport\u003e -u\u003cuser\u003e -p 手工连一次，区分是实例夯住还是网络/账号问题；\n3) 进程不在则先看 error log 尾部定位崩溃原因（OOM、磁盘满、数据文件损坏）再拉起，不要盲目重启；\n4) 有主从的立即评估是否需要切主，并确认应用已摘除该节点。": "1) Log in to the host and run systemctl status mysqld or docker ps to confirm the process is alive;\n2) If it is alive, connect by hand with mysql -h\u003chost\u003e -P\u003cport\u003e -u\u003cuser\u003e -p to tell a hung instance apart from a network or account problem;\n3) If it is not running, read the tail of the error log to identify the cause (OOM, full disk, corrupted data files) before starting it — do not blindly restart;\n4) If replication is in use, immediately assess whether to fail over, and confirm the application has taken this node out of rotation.",
    "1) 调大 innodb_log_buffer_size（通常从默认 16M 翻倍到 64M~256M）；\n2) 检查 innodb_log_file_size 是否过小导致频繁 checkpoint；\n3) 用 iostat -x 1 看 redo log 所在磁盘的 util 是否已经打满；\n4) 排查是否有大事务一次性写入过多数据，拆成小批次提交。": "1) Raise innodb_log_buffer_size (typically doubling the 16M default up to 64M~256M);\n2) Check whether innodb_log_file_size is too small and causing frequent checkpoints;\n3) Run iostat -x 1 to see whether the disk holding the redo log is already at full utilization;\n4) Check for large transactions writing too much data at once and split them into smaller batches.",
    "MySQL 仪表盘（使用 address 筛选，用于中心端 Categraf 采集远端多个 mysql 实例的场景）": "MySQL Dashboard (filtered by address, for a central Categraf scraping multiple remote MySQL instances)",
    "MySQL 仪表盘（使用 instance 筛选，需要采集时自行打上 instance 标签）": "MySQL Dashboard (filtered by instance, requires attaching an instance label at collection time)",
    "MySQL 仪表盘，适用于 Categraf 采集本机 MySQL 的场景": "MySQL Dashboard, for Categraf scraping the local MySQL instance"
}
