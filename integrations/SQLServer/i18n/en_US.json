{
    "1) 从告警标签取 database_name 与 file_type，file_type=LOG 的优先处理，日志写慢会直接卡住所有提交；\n2) 主机侧用 iostat -x 1 看该磁盘的 await 与 %util，确认是否已打满；\n3) 日志文件与数据文件放在同一块盘的，把日志迁到独立的低延迟磁盘；\n4) 检查是否有备份、DBCC CHECKDB、索引重建等重 IO 任务与业务高峰重叠，调整到低峰执行。": "1) Take database_name and file_type from the alert labels and handle file_type=LOG first, since slow log writes stall every commit;\n2) On the host run iostat -x 1 and check that disk's await and %util to see whether it is saturated;\n3) If the log file shares a disk with the data files, move the log to a dedicated low-latency disk;\n4) Check whether backups, DBCC CHECKDB, or index rebuilds overlap the business peak and move them to off-peak hours.",
    "1) 从告警标签取 database_name 与 logical_filename，定位是哪个库的哪个文件慢；\n2) 在主机侧执行 iostat -x 1（Linux）或用性能监视器看 Avg. Disk sec/Read，确认是存储本身慢还是 SQL Server 读放大；\n3) 存储正常则按 Page Life Expectancy 的思路优化查询、补索引，减少物理读；\n4) 存储确实慢的，考虑把该文件迁到 SSD、拆分文件组分散 IO，或排查是否与备份任务错峰不当。": "1) Take database_name and logical_filename from the alert labels to identify the slow file;\n2) On the host run iostat -x 1 (Linux) or watch Avg. Disk sec/Read in Performance Monitor to tell slow storage from read amplification inside SQL Server;\n3) If the storage is fine, optimise queries and add indexes as for page life expectancy to reduce physical reads;\n4) If the storage really is slow, consider moving the file to SSD, splitting filegroups to spread IO, or checking whether backup jobs overlap the business peak.",
    "1) 从告警标签取 instance（数据库名），执行 SELECT name, log_reuse_wait_desc FROM sys.databases WHERE name = '\u003c库名\u003e' 看日志为什么不能重用；\n2) log_reuse_wait_desc 为 LOG_BACKUP 的立刻执行 BACKUP LOG [库名] TO DISK='...'，这是最常见的原因；\n3) 为 ACTIVE_TRANSACTION 的执行 DBCC OPENTRAN 找出长事务并评估是否 KILL；\n4) 为 REPLICATION 或 AVAILABILITY_REPLICA 的去查复制/AlwaysOn 的同步是否卡住，先修复同步链路。": "1) Take instance (the database name) from the alert labels and run SELECT name, log_reuse_wait_desc FROM sys.databases WHERE name = '\u003cdb\u003e' to see why the log cannot be reused;\n2) For LOG_BACKUP, run BACKUP LOG [db] TO DISK='...' immediately; this is by far the most common cause;\n3) For ACTIVE_TRANSACTION, run DBCC OPENTRAN to find the long transaction and decide whether to KILL it;\n4) For REPLICATION or AVAILABILITY_REPLICA, investigate whether replication/AlwaysOn synchronisation is stuck and repair the replication link first.",
    "1) 从告警标签取 volume_mount_point，执行 SELECT DB_NAME(database_id), name, size*8/1024 AS size_mb FROM sys.master_files ORDER BY size DESC 找出占用最大的文件；\n2) 日志文件异常大的先执行 BACKUP LOG 截断再 DBCC SHRINKFILE，注意完整恢复模式下不备份日志是缩不掉的；\n3) 数据文件确实需要这么大的，清理历史分区或归档冷数据；\n4) 应急可先把 tempdb 或备份文件迁到其他卷腾出空间，长期需要扩容磁盘。": "1) Take volume_mount_point from the alert labels and run SELECT DB_NAME(database_id), name, size*8/1024 AS size_mb FROM sys.master_files ORDER BY size DESC to find the largest files;\n2) For an oversized log file, run BACKUP LOG to truncate it first and then DBCC SHRINKFILE; under the full recovery model it cannot shrink without a log backup;\n3) If the data files genuinely need that space, clean up historical partitions or archive cold data;\n4) As an emergency measure move tempdb or backup files to another volume, but plan a disk expansion.",
    "1) 在数据库主机执行 systemctl status mssql-server（Linux）或 Get-Service MSSQLSERVER（Windows）确认服务状态；\n2) 服务在跑则用 sqlcmd -S \u003chost\u003e,1433 -U \u003cuser\u003e 手工连一次，区分是网络不通、端口没监听还是账号被锁；\n3) 服务没起来则查看 /var/opt/mssql/log/errorlog（Linux）或 SQL Server 错误日志，常见原因是磁盘满、tempdb 无法创建、证书过期；\n4) 恢复后执行 SELECT name, state_desc FROM sys.databases 确认所有库都回到 ONLINE。": "1) On the database host run systemctl status mssql-server (Linux) or Get-Service MSSQLSERVER (Windows) to check the service;\n2) If the service is running, connect by hand with sqlcmd -S \u003chost\u003e,1433 -U \u003cuser\u003e to separate a network problem, a port not listening, and a locked account;\n3) If the service is down, check /var/opt/mssql/log/errorlog (Linux) or the SQL Server error log; a full disk, tempdb creation failure, or an expired certificate are common causes;\n4) After recovery run SELECT name, state_desc FROM sys.databases to confirm every database is back ONLINE.",
    "1) 执行 SELECT * FROM sys.dm_os_performance_counters WHERE counter_name LIKE '%Page life%' 确认实时值；\n2) 执行 SELECT TOP 20 * FROM sys.dm_exec_query_stats ORDER BY total_logical_reads DESC，找出逻辑读最大的语句，通常是缺索引导致的全表扫描；\n3) 用 sys.dm_db_missing_index_details 拿到建议索引并评估后创建，这比加内存见效快；\n4) 确认查询已优化仍不足的，检查 max server memory 配置是否过小或被其他进程挤占，再考虑扩内存。": "1) Run SELECT * FROM sys.dm_os_performance_counters WHERE counter_name LIKE '%Page life%' to confirm the live value;\n2) Run SELECT TOP 20 * FROM sys.dm_exec_query_stats ORDER BY total_logical_reads DESC to find the statements with the highest logical reads, usually full scans caused by missing indexes;\n3) Take the suggestions from sys.dm_db_missing_index_details, review them, and create the indexes; this pays off faster than adding memory;\n4) If queries are already optimised and it is still low, check whether max server memory is set too small or is being squeezed by other processes before adding RAM.",
    "1) 执行 SELECT blocking_session_id, session_id, wait_type, wait_time, wait_resource FROM sys.dm_exec_requests WHERE blocking_session_id \u003c\u003e 0 找出阻塞链的源头；\n2) 对源头 session 用 DBCC INPUTBUFFER(\u003cspid\u003e) 或 sys.dm_exec_sql_text 看它在执行什么；\n3) 确认是失控的长事务后执行 KILL \u003cspid\u003e 先止血；\n4) 事后检查该事务是否缺少索引导致锁范围过大，或应用侧忘记提交事务，必要时评估启用 READ_COMMITTED_SNAPSHOT 降低读写互斥。": "1) Run SELECT blocking_session_id, session_id, wait_type, wait_time, wait_resource FROM sys.dm_exec_requests WHERE blocking_session_id \u003c\u003e 0 to find the head of the blocking chain;\n2) For the head session use DBCC INPUTBUFFER(\u003cspid\u003e) or sys.dm_exec_sql_text to see what it is running;\n3) Once confirmed as a runaway long transaction, run KILL \u003cspid\u003e to stop the bleeding;\n4) Afterwards check whether that transaction lacks an index (widening the lock range) or the application forgot to commit, and consider enabling READ_COMMITTED_SNAPSHOT to reduce reader/writer conflict.",
    "1) 执行 SELECT name, state_desc FROM sys.databases WHERE state_desc \u003c\u003e 'ONLINE' 确认是哪个库；\n2) 查看 SQL Server 错误日志里该库最近的报错，确认是 IO 错误还是日志损坏；\n3) 优先用最近一次可用备份做还原（RESTORE DATABASE），这是数据损失最小的路径；\n4) 无可用备份时才考虑 EMERGENCY 模式 + DBCC CHECKDB(..., REPAIR_ALLOW_DATA_LOSS)，该操作会丢数据，务必先做文件级快照。": "1) Run SELECT name, state_desc FROM sys.databases WHERE state_desc \u003c\u003e 'ONLINE' to identify the database;\n2) Look for the most recent errors for that database in the SQL Server error log to tell an IO error from log corruption;\n3) Restoring from the most recent good backup (RESTORE DATABASE) is the path with the least data loss and should be preferred;\n4) Only if no backup exists should you use EMERGENCY mode plus DBCC CHECKDB(..., REPAIR_ALLOW_DATA_LOSS); that operation loses data, so take a file-level snapshot first.",
    "1) 执行 SELECT name, state_desc FROM sys.databases WHERE state_desc = 'OFFLINE' 列出离线的库；\n2) 确认是否是计划内的维护或迁移操作，核对变更单；\n3) 非计划的执行 ALTER DATABASE [库名] SET ONLINE 尝试上线，失败则看错误日志里的具体原因；\n4) 上线后执行 DBCC CHECKDB([库名]) WITH NO_INFOMSGS 确认数据一致性。": "1) Run SELECT name, state_desc FROM sys.databases WHERE state_desc = 'OFFLINE' to list the offline databases;\n2) Confirm against the change record whether this is planned maintenance or a migration;\n3) If unplanned, try ALTER DATABASE [db] SET ONLINE and read the error log for the exact reason if it fails;\n4) Once online run DBCC CHECKDB([db]) WITH NO_INFOMSGS to confirm data consistency.",
    "1) 执行 SELECT session_id, requested_memory_kb, granted_memory_kb, wait_time_ms FROM sys.dm_exec_query_memory_grants 查看谁在等、谁占着；\n2) 对占用巨大的会话用 sys.dm_exec_sql_text 取出语句，通常是大排序或大哈希连接，缺索引会显著放大内存需求；\n3) 应急可 KILL 掉占用最大的会话释放内存；\n4) 长期按 dm_db_missing_index_details 建索引、拆分大查询，或调大 max server memory。": "1) Run SELECT session_id, requested_memory_kb, granted_memory_kb, wait_time_ms FROM sys.dm_exec_query_memory_grants to see who is waiting and who is holding;\n2) For sessions with huge grants, pull the statement with sys.dm_exec_sql_text; it is usually a large sort or hash join, and a missing index amplifies the memory requirement significantly;\n3) As an emergency measure KILL the largest consumer to release memory;\n4) Longer term add the indexes suggested by dm_db_missing_index_details, split large queries, or raise max server memory.",
    "1) 执行 system_health 扩展事件查询死锁图：SELECT XEvent.query('.') FROM (SELECT CAST(target_data AS XML) AS TargetData FROM sys.dm_xe_session_targets ...) 取出 xml_deadlock_report；\n2) 从死锁图里看两个事务的加锁顺序与涉及的索引；\n3) 最常见的修法是统一业务代码里多表更新的顺序，以及给参与的查询补索引缩小锁范围；\n4) 短期可在应用侧对死锁错误号 1205 做自动重试，但不能替代根因修复。": "1) Query the system_health extended event session for the deadlock graph: SELECT XEvent.query('.') FROM (SELECT CAST(target_data AS XML) AS TargetData FROM sys.dm_xe_session_targets ...) and extract xml_deadlock_report;\n2) Read the lock acquisition order and the indexes involved from the graph;\n3) The usual fix is to make multi-table updates follow the same order everywhere in the application code, plus indexes on the participating queries to narrow the lock range;\n4) In the short term the application can retry on error 1205, but that does not replace fixing the root cause.",
    "SQL Server 事务日志使用率过高": "SQL Server transaction log usage too high",
    "SQL Server 发生死锁": "SQL Server deadlock occurred",
    "SQL Server 存在 OFFLINE 状态的数据库": "SQL Server has a database in OFFLINE state",
    "SQL Server 存在 SUSPECT 状态的数据库": "SQL Server has a database in SUSPECT state",
    "SQL Server 存在等待内存授予的查询": "SQL Server has queries waiting for memory grants",
    "SQL Server 实例不可达": "SQL Server instance unreachable",
    "SQL Server 数据卷剩余空间不足": "SQL Server data volume free space low",
    "SQL Server 数据文件写延迟过高": "SQL Server data file write latency too high",
    "SQL Server 数据文件读延迟过高": "SQL Server data file read latency too high",
    "SQL Server 阻塞进程数过高": "SQL Server blocked process count too high",
    "SQL Server 页面生命周期过短": "SQL Server page life expectancy too low",
    "当前数据库连接": "Current Database Connections",
    "硬盘空闲空间": "Disk Free Space"
}
