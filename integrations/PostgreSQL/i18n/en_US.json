{
    "1) 先排除刚重启导致的缓存冷启动（对照实例 uptime）；\n2) 用 pg_stat_statements 按 shared_blks_read 排序找出读盘最多的 SQL，优先补索引消除全表扫描；\n3) 对比 shared_buffers 与实例内存，通常调到物理内存的 25%；\n4) 确认是否有大表全量扫描或批处理任务把缓存冲掉，可错峰执行。": "1) First rule out a cold cache after a restart (check the instance uptime);\n2) Use pg_stat_statements sorted by shared_blks_read to find the SQL doing the most disk reads, and add indexes to eliminate sequential scans first;\n3) Compare shared_buffers against instance memory — 25% of physical memory is the usual target;\n4) Check whether a full table scan or a batch job is flushing the cache, and move it off peak hours.",
    "1) 先确认 SHOW track_io_timing 为 on，否则该指标恒为 0，需先开启才有意义；\n2) 用 pg_stat_statements 按 shared_blks_read 排序找出读盘最多的 SQL，补索引或改写；\n3) 对比 shared_buffers 与实例内存，通常设为物理内存的 25% 左右；\n4) 用 iostat -x 1 确认数据目录所在磁盘是否已经打满。": "1) First confirm SHOW track_io_timing is on, otherwise this metric stays at 0 and the rule is meaningless until you enable it;\n2) Use pg_stat_statements sorted by shared_blks_read to find the SQL doing the most disk reads, then add indexes or rewrite them;\n3) Compare shared_buffers against instance memory — usually about 25% of physical memory;\n4) Run iostat -x 1 to confirm whether the disk holding the data directory is saturated.",
    "1) 先确认 SHOW track_io_timing 为 on，否则该指标恒为 0；\n2) 看 pg_stat_bgwriter 的 buffers_backend 是否远大于 buffers_checkpoint——若是说明 shared_buffers 偏小或 bgwriter 太保守；\n3) 适当调大 checkpoint_timeout 与 checkpoint_completion_target，把刷盘摊平避免 IO 尖峰；\n4) 用 iostat -x 1 确认磁盘是否已打满，必要时换更快的存储。": "1) First confirm SHOW track_io_timing is on, otherwise this metric stays at 0;\n2) Check whether buffers_backend in pg_stat_bgwriter is much larger than buffers_checkpoint — if so, shared_buffers is too small or the bgwriter is too conservative;\n3) Raise checkpoint_timeout and checkpoint_completion_target to spread the flushing out and avoid IO spikes;\n4) Run iostat -x 1 to confirm whether the disk is saturated, and move to faster storage if needed.",
    "1) 在 postgresql.conf 里确认 log_lock_waits=on、deadlock_timeout 合理，然后从日志里取死锁双方的完整 SQL；\n2) 查 pg_locks 与 pg_stat_activity 关联，确认是哪两张表、哪种锁互相等待；\n3) 根因通常是不同事务以相反顺序更新同几行——统一业务侧的加锁顺序即可消除；\n4) 长事务会放大死锁概率，检查是否有事务开启后长时间不提交。": "1) In postgresql.conf, confirm log_lock_waits=on and a sane deadlock_timeout, then take the full SQL of both sides of the deadlock from the log;\n2) Join pg_locks with pg_stat_activity to identify which two tables and which lock modes are waiting on each other;\n3) The root cause is usually two transactions updating the same rows in opposite order — enforcing a single locking order in the application removes it;\n4) Long transactions amplify deadlocks, so check for transactions that stay open without committing.",
    "1) 登录主机用 systemctl status postgresql 或 docker ps 确认进程是否存活；\n2) 进程存活则用 psql -h \u003chost\u003e -p \u003cport\u003e -U \u003cuser\u003e 手工连一次，区分实例夯住与网络/pg_hba 认证问题；\n3) 进程不在则看 $PGDATA/log 下的日志尾部定位原因（OOM、磁盘满、WAL 目录写满）再拉起；\n4) 有流复制备库的立即评估是否需要提升备库（pg_ctl promote）。": "1) Log in to the host and run systemctl status postgresql or docker ps to confirm the process is alive;\n2) If it is alive, connect by hand with psql -h \u003chost\u003e -p \u003cport\u003e -U \u003cuser\u003e to tell a hung instance apart from a network or pg_hba authentication problem;\n3) If it is not running, read the tail of the log under $PGDATA/log to identify the cause (OOM, full disk, full WAL directory) before starting it;\n4) If a streaming replica exists, immediately assess whether to promote it (pg_ctl promote).",
    "Postgresql缓存命中率低于50%": "PostgreSQL cache hit ratio below 50%",
    "buffers_checkpoint: checkpoint写数据块数量\nbuffers_clean：bgwriter写入数据块的数量\nbuffers_backend:通过backend写入的块数量, 此值过大，代表shared buffer没有维护好，后端进程不得不自己去进行刷盘的动作\nbuffers_backend_fsync: backend需要fsync的次数\n": "buffers_checkpoint: number of buffers written by checkpoints\nbuffers_clean: number of buffers written by the bgwriter\nbuffers_backend: number of buffers written directly by a backend. A high value means the shared buffer is not well tuned and backend processes have to flush to disk themselves\nbuffers_backend_fsync: number of times a backend had to execute fsync\n",
    "checkpoint 写文件时间分布": "Checkpoint Write Time Distribution",
    "checkpoint_write_time: 从shared buffer 中write到page cache花费的时间\ncheckpoint_sync_time  : checkpoint调用fsync将脏数据刷到磁盘花费的时间，如果这个值很长，容易造成IO抖动，需要增加checkpoint_timeout或者checkpoint_completion_target": "checkpoint_write_time: time spent writing from the shared buffer to the page cache\ncheckpoint_sync_time: time spent by the checkpoint calling fsync to flush dirty data to disk. A long value easily causes IO jitter, so increase checkpoint_timeout or checkpoint_completion_target",
    "checkpoint分布": "Checkpoint Distribution",
    "posgresql读取时间过高": "PostgreSQL read time too high",
    "postgresql_checkpoints_timed：指超过checkpoint_timeout的时间后触发的检查点次数\npostgresql_checkpoints_req：手动触发checkpoint或者因为WAL文件数量达到max_wal_size时也会增加，如果这个值大于postgresql_checkpoints_timed说明checkpoint_timeout设置的不合理": "postgresql_checkpoints_timed: number of checkpoints triggered after checkpoint_timeout elapsed\npostgresql_checkpoints_req: increases when a checkpoint is triggered manually or when the WAL size reaches max_wal_size. If this value is greater than postgresql_checkpoints_timed, checkpoint_timeout is set improperly",
    "postgresql写入时间过高": "PostgreSQL write time too high",
    "postgresql有死锁": "PostgreSQL deadlocks detected",
    "returned ,代表执行查询遍历行数\nfetched，代表返回给客户端的行数\nreturned 远大于 fetched，代表查询效率低，存在全表扫描，应增加索引进行优化": "returned: number of rows scanned by queries\nfetched: number of rows returned to the client\nIf returned is much greater than fetched, query efficiency is low and full table scans exist, so add indexes to optimize",
    "rollback 过多，则需要检查业务逻辑，表面业务逻辑存在问题": "Too many rollbacks means the business logic needs to be checked, as it indicates a problem in the business logic",
    "temp_files较高说明存在很多排序，hash,或者聚合这种操作，可以增大work_mem减少临时文件的产生，并且同时这些操作的性能也会有较大的提升": "A high temp_files value means there are many sort, hash or aggregate operations. Increasing work_mem reduces temporary files and also greatly improves the performance of these operations",
    "与恢复冲突取消的查询次数，只会在备机上发生": "Number of queries canceled due to recovery conflicts, which only happens on standby servers",
    "事务统计": "Transaction Statistics",
    "冲突数": "Conflicts",
    "命中率低于99%，就需要适当调大shared buffer": "If the hit ratio is below 99%, increase the shared buffer accordingly",
    "数据块写入分布": "Buffer Write Distribution",
    "数据库中花费在写数据文件的时间，pg中脏页一般写入page cache，如果这个值较高，则说明cache较小，操作系统的cache需要更积极的写入": "Time spent writing data files. In PG dirty pages are normally written to the page cache; a high value means the cache is small and the OS cache has to flush more aggressively",
    "数据库中花费在读取文件的时间，这个值很高说明内存较小，需要频繁从磁盘读入数据文件": "Time spent reading data files. A high value means memory is insufficient and data files are frequently read from disk",
    "数据库中花费在读取文件的时间，这个值很高说明内存较小，需要频繁从磁盘读入数据文件\n数据库中花费在写数据文件的时间，pg中脏页一般写入page cache，如果这个值较高，则说明cache较小，操作系统的cache需要更积极的写入": "Time spent reading data files. A high value means memory is insufficient and data files are frequently read from disk\nTime spent writing data files. In PG dirty pages are normally written to the page cache; a high value means the cache is small and the OS cache has to flush more aggressively",
    "数据库读写时间统计": "Database Read/Write Time Statistics",
    "数据更新统计": "Data Update Statistics",
    "数据查询统计": "Data Query Statistics",
    "文档": "Documentation",
    "死锁数": "Deadlocks",
    "死锁的数量，如果这个值很大说明业务逻辑有问题": "Number of deadlocks. A large value indicates a problem in the business logic",
    "生成临时文件统计": "Temporary File Statistics",
    "缓存命中率": "Cache Hit Ratio",
    "连接数": "Connections"
}
