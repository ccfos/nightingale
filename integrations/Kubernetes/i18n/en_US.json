{
    "(container_memory_rss + container_memory_cache + container_memory_swap + kernel memory)/ (memory limit), 没有数据表明容器配置没有设置mem limit": "(container_memory_rss + container_memory_cache + container_memory_swap + kernel memory) / (memory limit); no data means the container has no mem limit configured",
    "1) kubectl describe \u003ckind\u003e \u003cname\u003e -n \u003cns\u003e 对比 generation 与 observedGeneration；\n2) 检查 kube-controller-manager 是否健康——控制器不工作是最常见原因；\n3) 看 controller-manager 日志中与该对象相关的报错；\n4) 确认 apiserver 与 etcd 正常，控制器恢复后 generation 会自动追平。": "1) Run kubectl describe \u003ckind\u003e \u003cname\u003e -n \u003cns\u003e and compare generation with observedGeneration;\n2) Check whether kube-controller-manager is healthy — a stalled controller is the most common cause;\n3) Look for errors about this object in the controller-manager log;\n4) Confirm apiserver and etcd are healthy; once the controller recovers, generation catches up automatically.",
    "1) kubectl describe deploy \u003cname\u003e -n \u003cns\u003e 看 Conditions 与 Events；\n2) kubectl get pod -n \u003cns\u003e -l \u003cselector\u003e 看未就绪 Pod 的状态与原因；\n3) 常见为资源不足无法调度、镜像拉取失败、就绪探针不通过；\n4) 按 Pod 的具体状态排查，资源不足的扩容节点或调低 requests。": "1) Run kubectl describe deploy \u003cname\u003e -n \u003cns\u003e for Conditions and Events;\n2) Run kubectl get pod -n \u003cns\u003e -l \u003cselector\u003e to see the state and reason of the unready Pods;\n3) Common causes are insufficient resources for scheduling, image pull failures, and failing readiness probes;\n4) Follow the Pod's specific state; for resource shortage, add nodes or lower requests.",
    "1) kubectl describe ds \u003cname\u003e -n \u003cns\u003e 看 Events 与期望/实际数量；\n2) 对比节点的污点与 DaemonSet 的容忍配置——污点不匹配是最常见原因；\n3) 检查 nodeSelector/affinity 是否把节点排除在外；\n4) 资源不足导致的调低 DaemonSet 的 requests 或释放节点资源。": "1) Run kubectl describe ds \u003cname\u003e -n \u003cns\u003e for Events and desired/current counts;\n2) Compare node taints against the DaemonSet's tolerations — a taint mismatch is the most common cause;\n3) Check whether nodeSelector/affinity excludes those nodes;\n4) For resource shortage, lower the DaemonSet's requests or free node resources.",
    "1) kubectl describe hpa \u003cname\u003e -n \u003cns\u003e 看 Conditions 里的具体原因；\n2) ScalingLimited 说明触及了 min/max 边界；FailedGetScale 说明目标工作负载有问题；\n3) 指标获取失败的检查 metrics-server 或自定义指标适配器是否健康；\n4) 资源不足导致新副本 Pending 的，扩容节点。": "1) Run kubectl describe hpa \u003cname\u003e -n \u003cns\u003e and read the reason in Conditions;\n2) ScalingLimited means it hit the min/max boundary; FailedGetScale means the target workload has a problem;\n3) For metric failures, check whether metrics-server or the custom metrics adapter is healthy;\n4) If new replicas stay Pending for lack of resources, add nodes.",
    "1) kubectl describe hpa \u003cname\u003e -n \u003cns\u003e 看当前指标值与目标值的差距；\n2) 确认集群是否还有资源支撑更多副本（节点余量）；\n3) 有余量的调大 maxReplicas；没余量的先扩容节点；\n4) 同时评估单副本的处理能力是否偏低，优化应用比无限扩副本更有效。": "1) Run kubectl describe hpa \u003cname\u003e -n \u003cns\u003e to see the gap between the current and target metric;\n2) Confirm the cluster still has resources for more replicas (node headroom);\n3) With headroom, raise maxReplicas; without it, add nodes first;\n4) Also assess whether per-replica capacity is too low — optimising the application beats scaling replicas indefinitely.",
    "1) kubectl describe node \u003cnode\u003e 看 Conditions 与 Events，确认是 MemoryPressure、DiskPressure 还是 kubelet 失联；\n2) 登录节点确认 kubelet 与容器运行时进程状态（systemctl status kubelet containerd）；\n3) kubelet 日志用 journalctl -u kubelet -n 200 定位；\n4) 节点短期无法恢复的执行 kubectl drain 让 Pod 尽快迁走，避免等待驱逐超时。": "1) Run kubectl describe node \u003cnode\u003e for Conditions and Events to tell MemoryPressure, DiskPressure and a lost kubelet apart;\n2) Log in to the node and check kubelet and the container runtime (systemctl status kubelet containerd);\n3) Use journalctl -u kubelet -n 200 to locate the problem;\n4) If the node cannot recover soon, run kubectl drain so the Pods move promptly instead of waiting for the eviction timeout.",
    "1) kubectl describe node \u003cnode\u003e 看 Conditions 的变更时间线；\n2) 登录节点看 kubelet 日志，常见根因是资源压力、容器运行时卡顿、或到 apiserver 的网络不稳；\n3) 抖动期间建议先 kubectl cordon 该节点，阻止新 Pod 调度上去；\n4) 根因定位并修复后再 uncordon，不要在抖动中反复重启 kubelet。": "1) Run kubectl describe node \u003cnode\u003e and read the timeline of Condition changes;\n2) On the node, check the kubelet log — common root causes are resource pressure, a stalled container runtime, or an unstable network to apiserver;\n3) While it flaps, kubectl cordon the node so no new Pods land on it;\n4) Uncordon only after the root cause is fixed — do not repeatedly restart kubelet during the flapping.",
    "1) kubectl describe node | grep -A5 'Allocated resources' 汇总各节点的分配率；\n2) 这是容量规划信号而非即时故障——评估节点故障时是否还有冗余承接；\n3) 检查是否有 Pod 的 requests 设置得过高（远超实际用量），下调可释放大量可调度容量；\n4) 冗余确实不足的扩容节点。": "1) Run kubectl describe node | grep -A5 'Allocated resources' to sum up allocation per node;\n2) This is a capacity planning signal rather than an immediate outage — assess whether there is still headroom to absorb a node failure;\n3) Check for Pods with requests set far above actual usage; lowering them frees a lot of schedulable capacity;\n4) If headroom is genuinely insufficient, add nodes.",
    "1) kubectl describe pod \u003cpod\u003e -n \u003cns\u003e 看 Events 的时间戳，定位卡在拉镜像、挂载卷还是初始化容器；\n2) 拉镜像慢的检查镜像大小与仓库带宽，考虑预热镜像或用本地仓库；\n3) 挂载卷慢的排查 PV/CSI 驱动状态；\n4) 应用自身启动慢的优化启动逻辑，并把就绪探针的 initialDelaySeconds 调到合理值。": "1) Run kubectl describe pod \u003cpod\u003e -n \u003cns\u003e and use the Event timestamps to see whether it is stuck pulling the image, mounting a volume, or in an init container;\n2) For slow pulls, check image size and registry bandwidth, and consider pre-warming images or a local registry;\n3) For slow mounts, check the PV/CSI driver state;\n4) If the application itself starts slowly, optimise startup and set a sensible readiness probe initialDelaySeconds.",
    "1) kubectl describe pod \u003cpod\u003e -n \u003cns\u003e 看 Waiting 的 Reason；\n2) ImagePullBackOff 检查镜像名、tag 与拉取凭据（imagePullSecrets）；\n3) CreateContainerConfigError 检查引用的 ConfigMap/Secret 是否存在；\n4) CrashLoopBackOff 按崩溃循环路径处理，看 --previous 日志。": "1) Run kubectl describe pod \u003cpod\u003e -n \u003cns\u003e and read the Waiting Reason;\n2) For ImagePullBackOff, check the image name, tag and pull credentials (imagePullSecrets);\n3) For CreateContainerConfigError, check the referenced ConfigMap/Secret exists;\n4) For CrashLoopBackOff, follow the crash loop path and read the --previous logs.",
    "1) kubectl describe pv \u003cpv\u003e 与 kubectl describe pvc \u003cpvc\u003e -n \u003cns\u003e 看 Events 里的具体报错；\n2) 常见为存储后端不可用、容量不足、或 StorageClass 参数错误；\n3) 检查 CSI 驱动 Pod 是否健康（通常在 kube-system 下）；\n4) 后端存储故障的按存储系统自身流程处理，期间 Pod 会一直 Pending。": "1) Run kubectl describe pv \u003cpv\u003e and kubectl describe pvc \u003cpvc\u003e -n \u003cns\u003e and read the errors in Events;\n2) Common causes are an unavailable storage backend, insufficient capacity, or wrong StorageClass parameters;\n3) Check whether the CSI driver Pods are healthy (usually in kube-system);\n4) For a storage backend failure, follow that system's own procedure — Pods stay Pending meanwhile.",
    "1) kubectl describe quota -n \u003cns\u003e 看是哪一项（cpu/memory/pods/storage）触顶；\n2) kubectl get pod -n \u003cns\u003e -o custom-columns 统计各工作负载的 requests 占用，找出大户；\n3) 确认是否有已停止但未清理的资源仍占着配额；\n4) 确需更多配额的联系集群管理员调整 ResourceQuota。": "1) Run kubectl describe quota -n \u003cns\u003e to see which item (cpu/memory/pods/storage) topped out;\n2) Use kubectl get pod -n \u003cns\u003e -o custom-columns to sum requests per workload and find the biggest consumers;\n3) Check for stopped-but-uncleaned resources still holding quota;\n4) If more quota is genuinely needed, ask the cluster admin to adjust the ResourceQuota.",
    "1) kubectl exec 进入使用该卷的 Pod，用 df -i 确认；\n2) 逐层统计文件数找出小文件最多的目录；\n3) 清理无用的小文件（缓存碎片、临时文件）；\n4) 无法收敛的需要扩容或更换存储类型。": "1) kubectl exec into a Pod using the volume and run df -i;\n2) Count files level by level to find the directories with the most small files;\n3) Clean up useless small files (cache fragments, temporary files);\n4) If it cannot be reduced, expand the volume or change storage type.",
    "1) kubectl get apiservice 看哪个 APIService 处于 False 状态；\n2) kubectl describe apiservice \u003cname\u003e 看具体报错；\n3) 后端服务不可用的检查对应 Deployment/Service 是否健康（常见是 metrics-server）；\n4) 证书问题的核对 CA bundle 配置。": "1) Run kubectl get apiservice to see which APIService is False;\n2) Run kubectl describe apiservice \u003cname\u003e for the specific error;\n3) If the backing service is down, check its Deployment/Service health (commonly metrics-server);\n4) For certificate problems, verify the CA bundle configuration.",
    "1) kubectl get apiservice 看哪个处于 False 状态；\n2) kubectl describe apiservice \u003cname\u003e 看具体报错；\n3) 后端服务不可用的检查对应 Deployment/Service；\n4) 证书问题的核对 CA bundle 配置。": "1) Run kubectl get apiservice to see which one is False;\n2) Run kubectl describe apiservice \u003cname\u003e for the specific error;\n3) If the backing service is down, check its Deployment/Service;\n4) For certificate problems, verify the CA bundle configuration.",
    "1) kubectl get apiservice 确认是哪个 APIService；\n2) kubectl -n \u003cns\u003e get pod 检查其后端 Pod 是否健康；\n3) 查看该 Pod 日志定位启动或运行错误；\n4) 恢复后用 kubectl top node 之类命令验证功能已回来。": "1) Run kubectl get apiservice to identify the APIService;\n2) Run kubectl -n \u003cns\u003e get pod to check its backing Pods;\n3) Read that Pod's logs for startup or runtime errors;\n4) After recovery, verify with something like kubectl top node that the functionality is back.",
    "1) kubectl get job \u003cjob\u003e -n \u003cns\u003e 看失败次数，再 kubectl logs job/\u003cjob\u003e 看失败原因；\n2) kubectl describe job 看 Events，确认是否因 backoffLimit 耗尽而放弃；\n3) 修复根因后 kubectl delete job 再重新提交，或让 CronJob 下次调度重跑；\n4) 确认该任务失败是否有下游影响（数据未产出、清理未执行），需要时手工补做。": "1) Run kubectl get job \u003cjob\u003e -n \u003cns\u003e for the failure count, then kubectl logs job/\u003cjob\u003e for the reason;\n2) Run kubectl describe job for Events and whether it gave up after exhausting backoffLimit;\n3) After fixing the root cause, kubectl delete job and resubmit, or let the CronJob run again on schedule;\n4) Confirm whether the failure has downstream impact (missing output, skipped cleanup) and redo it manually if needed.",
    "1) kubectl get job \u003cjob\u003e -n \u003cns\u003e 看已运行时长与完成数；\n2) kubectl logs job/\u003cjob\u003e 看是否卡在某一步；\n3) 确认是任务本身耗时长还是卡死——卡死的 kubectl delete job 后修复再重跑；\n4) 给 Job 设置合理的 activeDeadlineSeconds，避免无限期挂起。": "1) Run kubectl get job \u003cjob\u003e -n \u003cns\u003e for elapsed time and completions;\n2) Run kubectl logs job/\u003cjob\u003e to see where it is stuck;\n3) Determine whether the task is genuinely long-running or hung — for a hang, kubectl delete job, fix it and rerun;\n4) Set a sensible activeDeadlineSeconds so Jobs cannot hang indefinitely.",
    "1) kubectl get node -o wide 看各节点 kubelet 版本，kubectl version 看控制面版本；\n2) 确认偏差是否在官方支持的版本偏斜范围内（kubelet 不得高于 apiserver，且落后不超过两个小版本）；\n3) 通常是升级过程未完成，把落后的节点升级到位；\n4) 升级前先确认工作负载对新版本 API 的兼容性。": "1) Run kubectl get node -o wide for kubelet versions and kubectl version for the control plane;\n2) Confirm the skew is within the officially supported window (kubelet must not exceed apiserver and must not lag by more than two minor versions);\n3) This usually means an unfinished upgrade — bring the lagging nodes up to date;\n4) Before upgrading, confirm workload compatibility with the new API versions.",
    "1) kubectl get pod -A -o wide --field-selector spec.nodeName=\u003cnode\u003e | wc -l 确认实际数量；\n2) 对照 kubelet 的 maxPods 配置（默认 110）；\n3) 用 kubectl drain 或调整调度策略把部分 Pod 迁到其他节点；\n4) 确需更高密度的调大 maxPods，但要同步评估节点的 CPU/内存/PLEG 压力。": "1) Run kubectl get pod -A -o wide --field-selector spec.nodeName=\u003cnode\u003e | wc -l for the actual count;\n2) Compare against the kubelet maxPods setting (110 by default);\n3) Use kubectl drain or scheduling policy to move some Pods to other nodes;\n4) If you genuinely need higher density, raise maxPods but reassess node CPU/memory/PLEG pressure at the same time.",
    "1) kubectl get pod -n \u003cns\u003e -l \u003cselector\u003e -o wide 看被误调度到哪些节点；\n2) 检查 DaemonSet 的 nodeSelector/affinity 与节点标签是否匹配；\n3) 通常是节点标签被改动或 DaemonSet 配置更新导致，修正后多余 Pod 会被自动清理；\n4) 未自动清理的手工删除对应 Pod。": "1) Run kubectl get pod -n \u003cns\u003e -l \u003cselector\u003e -o wide to see which nodes they landed on;\n2) Check whether the DaemonSet's nodeSelector/affinity matches the node labels;\n3) This usually follows a node label change or a DaemonSet update — after correcting it the extra Pods are cleaned up automatically;\n4) Delete any leftovers manually.",
    "1) kubectl get pod -n \u003cns\u003e -l \u003cselector\u003e 按序号看是哪个副本卡住；\n2) kubectl describe pod 排查该副本的 PVC 是否绑定成功——这是有状态服务最常见的卡点；\n3) 存储问题排查 PV/StorageClass 与 CSI 驱动；\n4) 应用自身问题看 kubectl logs，修复后 StatefulSet 会自动继续。": "1) Run kubectl get pod -n \u003cns\u003e -l \u003cselector\u003e and check by ordinal which replica is stuck;\n2) Run kubectl describe pod to check whether its PVC bound successfully — this is the most common sticking point for stateful services;\n3) For storage problems, check the PV/StorageClass and the CSI driver;\n4) For application problems check kubectl logs; once fixed, the StatefulSet continues automatically.",
    "1) kubectl get pod \u003cpod\u003e -n \u003cns\u003e -o wide 看 STATUS 与 READY，再 kubectl describe pod 看 Events；\n2) Pending 多为资源不足或调度约束（节点亲和、污点、PVC 未绑定）；ImagePullBackOff 查镜像地址与拉取凭据；\n3) Running 但未 Ready 的看就绪探针配置与 kubectl logs \u003cpod\u003e 里的启动报错；\n4) 确认副本数是否已不足以承载流量，不足的先扩容其他健康副本。": "1) Run kubectl get pod \u003cpod\u003e -n \u003cns\u003e -o wide for STATUS and READY, then kubectl describe pod for Events;\n2) Pending usually means insufficient resources or scheduling constraints (affinity, taints, unbound PVC); for ImagePullBackOff check the image address and pull credentials;\n3) Running but not Ready — check the readiness probe and kubectl logs \u003cpod\u003e for startup errors;\n4) Confirm whether the replica count is now too low to carry traffic, and scale healthy replicas first if so.",
    "1) kubectl get pvc -n \u003cns\u003e 定位对应的 PVC 与 PV；\n2) 进到使用该卷的 Pod 里用 du 找出占用最大的目录（kubectl exec）；\n3) 清理日志与临时数据；\n4) StorageClass 支持在线扩容的直接 kubectl edit pvc 调大 spec.resources.requests.storage，不支持的需要迁移数据。": "1) Run kubectl get pvc -n \u003cns\u003e to find the PVC and PV;\n2) kubectl exec into a Pod using the volume and use du to find the largest directories;\n3) Clean up logs and temporary data;\n4) If the StorageClass supports online expansion, kubectl edit pvc and raise spec.resources.requests.storage; otherwise migrate the data.",
    "1) kubectl logs -n \u003cns\u003e deploy/prometheus-operator 看具体报错；\n2) 权限类报错核对 Operator 的 ClusterRole 是否覆盖了所需资源；\n3) Reconcile/Sync 失败的检查对应的 CRD 对象是否配置有误（kubectl describe servicemonitor/prometheusrule）；\n4) 被拒绝的资源用 kubectl describe 看拒绝原因，通常是字段校验不通过。": "1) Run kubectl logs -n \u003cns\u003e deploy/prometheus-operator for the specific error;\n2) For permission errors, verify the Operator's ClusterRole covers the required resources;\n3) For Reconcile/Sync failures, check the corresponding CRD objects (kubectl describe servicemonitor/prometheusrule);\n4) For rejected resources, kubectl describe shows the rejection reason, usually failed field validation.",
    "1) kubectl logs \u003cpod\u003e -c config-reloader -n \u003cns\u003e 看具体报错；\n2) 常见为配置文件语法错误导致 reload 被拒绝；\n3) 用 promtool check config / amtool check-config 校验配置；\n4) 修正后确认 reload 成功，并在 Prometheus 的 /config 页核对配置已更新。": "1) Run kubectl logs \u003cpod\u003e -c config-reloader -n \u003cns\u003e for the specific error;\n2) It is usually a config syntax error causing the reload to be rejected;\n3) Validate with promtool check config / amtool check-config;\n4) After fixing, confirm the reload succeeded and verify the new config on Prometheus's /config page.",
    "1) kubectl logs \u003cpod\u003e -n \u003cns\u003e --previous 看上一次退出前的日志，这是最关键的一步；\n2) kubectl describe pod 看退出码与 Events：137 通常是 OOM 或被 kill，1/2 多为应用自身启动失败；\n3) 配置或依赖问题（连不上数据库、ConfigMap/Secret 缺失）先修依赖再拉起；\n4) OOM 的对照 resources.limits.memory 与实际用量调整，或修复应用内存泄漏。": "1) Run kubectl logs \u003cpod\u003e -n \u003cns\u003e --previous to see the log before the last exit — this is the key step;\n2) Run kubectl describe pod for the exit code and Events: 137 usually means OOM or killed, 1/2 usually means the application failed to start;\n3) For config or dependency problems (unreachable database, missing ConfigMap/Secret), fix the dependency first;\n4) For OOM, compare resources.limits.memory against actual usage, or fix the application's memory leak.",
    "1) kubectl logs 或实例日志里找配置解析报错的具体行；\n2) 用 promtool check config prometheus.yml 本地校验；\n3) K8s 上通常由 Operator 生成配置，检查 ServiceMonitor/PrometheusRule 是否有非法字段；\n4) 修正后确认 /-/reload 成功且 /config 页显示新配置。": "1) Find the config parse error line in kubectl logs or the instance log;\n2) Validate locally with promtool check config prometheus.yml;\n3) On Kubernetes the config is usually generated by the Operator — check ServiceMonitor/PrometheusRule for invalid fields;\n4) After fixing, confirm /-/reload succeeded and the /config page shows the new configuration.",
    "1) kubectl rollout status deploy/\u003cname\u003e -n \u003cns\u003e 确认卡在第几个副本；\n2) kubectl describe pod 看新版本 Pod 为何未就绪（镜像、探针、资源）；\n3) 短期无法修复的用 kubectl rollout undo deploy/\u003cname\u003e 回滚到上一版本；\n4) 修复后重新发布，并检查 maxSurge/maxUnavailable 配置是否合理。": "1) Run kubectl rollout status deploy/\u003cname\u003e -n \u003cns\u003e to see which replica it is stuck on;\n2) Run kubectl describe pod to see why the new Pod is not ready (image, probe, resources);\n3) If it cannot be fixed quickly, kubectl rollout undo deploy/\u003cname\u003e to roll back;\n4) After fixing, redeploy and review whether maxSurge/maxUnavailable are sensible.",
    "1) kubectl rollout status ds/\u003cname\u003e -n \u003cns\u003e 看卡在哪些节点；\n2) kubectl describe pod 排查新版本 Pod 为何未就绪；\n3) 短期无法修复的用 kubectl rollout undo ds/\u003cname\u003e 回滚；\n4) 检查 updateStrategy 的 maxUnavailable 配置是否过于保守。": "1) Run kubectl rollout status ds/\u003cname\u003e -n \u003cns\u003e to see which nodes are stuck;\n2) Run kubectl describe pod to see why the new Pod is not ready;\n3) If it cannot be fixed quickly, kubectl rollout undo ds/\u003cname\u003e;\n4) Check whether the updateStrategy maxUnavailable is too conservative.",
    "1) kubectl rollout status sts/\u003cname\u003e -n \u003cns\u003e 看卡在第几个副本；\n2) StatefulSet 按序更新，一个副本卡住会阻塞后续全部——重点看当前正在更新的那个；\n3) kubectl describe pod 排查其未就绪原因（PVC、探针、镜像）；\n4) 无法快速修复的用 kubectl rollout undo 回滚。": "1) Run kubectl rollout status sts/\u003cname\u003e -n \u003cns\u003e to see which replica it is stuck on;\n2) StatefulSets update in order, so one stuck replica blocks all the rest — focus on the one currently updating;\n3) Run kubectl describe pod to find why it is not ready (PVC, probe, image);\n4) If it cannot be fixed quickly, kubectl rollout undo.",
    "1) kubectl top node 与 kubectl top pod -A --sort-by=memory 定位占用最高的 Pod；\n2) 检查是否有 Pod 未设 memory limit 在无限增长；\n3) 用 kubectl drain 迁走部分 Pod 缓解，注意先确认目标节点有余量；\n4) 检查 kubelet 的驱逐阈值配置是否合理，集群整体紧张的扩容节点。": "1) Run kubectl top node and kubectl top pod -A --sort-by=memory to find the biggest consumers;\n2) Check for Pods without a memory limit growing without bound;\n3) Use kubectl drain to move some Pods off, confirming the target nodes have headroom;\n4) Review the kubelet eviction thresholds, and add nodes if the cluster is tight overall.",
    "1) kubectl top node 确认整体分布，kubectl top pod -A --sort-by=cpu 找出占用最高的 Pod；\n2) 确认该节点是否被调度了过多高负载 Pod，必要时用 kubectl drain 迁走部分；\n3) 检查 Pod 的 requests 是否设置过低导致超卖；\n4) 集群整体水位高的扩容节点。": "1) Run kubectl top node for the overall picture and kubectl top pod -A --sort-by=cpu for the biggest consumers;\n2) Check whether too many high-load Pods landed on this node and drain some if needed;\n3) Check whether Pod requests are set too low, causing overcommit;\n4) If the cluster is running hot overall, add nodes.",
    "1) kubectl top pod \u003cpod\u003e -n \u003cns\u003e --containers 对照 resources.limits.cpu；\n2) 注意 Java 等运行时若未感知 cgroup limit 会开过多线程加剧限流，确认 JVM 版本与参数；\n3) 调大 limits.cpu，或改用 HPA 水平扩容分摊；\n4) limit 设置远高于 request 时容易被超卖挤占，评估是否把两者设置得更接近。": "1) Run kubectl top pod \u003cpod\u003e -n \u003cns\u003e --containers and compare against resources.limits.cpu;\n2) Note that runtimes like Java that do not detect the cgroup limit may spawn too many threads and worsen throttling — check the JVM version and flags;\n3) Raise limits.cpu, or scale out horizontally with an HPA;\n4) A limit far above the request is easily squeezed by overcommit — consider bringing them closer together.",
    "1) kubectl top pod \u003cpod\u003e -n \u003cns\u003e --containers 确认实际用量与 limit 的比值；\n2) 对照 CPUThrottlingHigh 指标确认是否已经在被限流；\n3) 业务量增长的调大 resources.limits.cpu 或做水平扩容（HPA）；\n4) 用量异常的在应用侧抓 CPU profile 定位热点，避免只靠加配额掩盖问题。": "1) Run kubectl top pod \u003cpod\u003e -n \u003cns\u003e --containers to compare actual usage against the limit;\n2) Check the CPUThrottlingHigh metric to confirm whether it is already being throttled;\n3) For genuine growth, raise resources.limits.cpu or scale out with an HPA;\n4) For abnormal usage, capture a CPU profile in the application rather than papering over it with more quota.",
    "1) kubectl top pod \u003cpod\u003e -n \u003cns\u003e --containers 确认用量与 limit；\n2) 对照历史曲线判断是持续爬升（疑似泄漏）还是业务高峰的正常波动；\n3) 正常波动的调大 resources.limits.memory 并预留余量；\n4) 持续爬升的在应用侧抓堆定位泄漏，扩配额只是拖延。": "1) Run kubectl top pod \u003cpod\u003e -n \u003cns\u003e --containers to compare usage against the limit;\n2) Check the history curve to tell a steady climb (likely a leak) from a normal traffic peak;\n3) For normal peaks, raise resources.limits.memory with headroom;\n4) For a steady climb, capture a heap profile in the application — raising the quota only postpones it.",
    "1) systemctl --failed 列出失败单元；\n2) journalctl -u \u003cunit\u003e -n 100 --no-pager 定位原因；\n3) K8s 节点上重点关注 kubelet、containerd、kube-proxy 这几个关键单元；\n4) 修复后 systemctl reset-failed 清理状态并确认恢复。": "1) Run systemctl --failed to list the failed units;\n2) Use journalctl -u \u003cunit\u003e -n 100 --no-pager to find the cause;\n3) On Kubernetes nodes pay special attention to kubelet, containerd and kube-proxy;\n4) After fixing, run systemctl reset-failed and confirm recovery.",
    "1) 从告警标签定位是哪个组件/实例在报错；\n2) 看该组件日志中的具体错误码：401/403 是 RBAC 权限问题，429 是被限流，5xx 是 apiserver 侧问题；\n3) 权限问题核对 ServiceAccount 的 Role/ClusterRoleBinding；\n4) 限流问题确认该组件是否在做高频 LIST，优化为 Watch 或加 resourceVersion。": "1) Use the alert labels to identify the component/instance that is erroring;\n2) Read the specific error code in that component's log: 401/403 is an RBAC problem, 429 is throttling, 5xx is an apiserver-side problem;\n3) For permissions, verify the ServiceAccount's Role/ClusterRoleBinding;\n4) For throttling, check whether the component is doing high-frequency LIST calls and switch to Watch or add a resourceVersion.",
    "1) 先 ping / ssh 确认机器本身是否存活，机器宕机按宕机处理；\n2) 机器存活则排查节点到 apiserver 的网络与证书（kubelet 日志里会有明确报错）；\n3) 确认该节点上是否有单副本的关键服务，有的话优先在其他节点拉起；\n4) 恢复后确认节点重新 Ready 且 Pod 状态正常。": "1) First confirm the machine is alive with ping/ssh — if it is down, handle it as a host outage;\n2) If it is alive, investigate the node's network and certificates to apiserver (the kubelet log states the error clearly);\n3) Check whether any single-replica critical service runs on that node and start it elsewhere first;\n4) After recovery, confirm the node is Ready again and Pod states are normal.",
    "1) 在 /status 页看查询引擎的并发与队列指标；\n2) 用 topk 类查询或 query log 找出最重的查询；\n3) 常见根因是高基数标签与过大的时间范围——收窄范围、减少 group by 维度；\n4) 优化后仍高的扩容 Prometheus 或做联邦/分片。": "1) Check the query engine concurrency and queue metrics on the /status page;\n2) Use topk-style queries or the query log to find the heaviest queries;\n3) The usual root causes are high-cardinality labels and over-wide time ranges — narrow the range and reduce group by dimensions;\n4) If it stays high after tuning, scale Prometheus or use federation/sharding.",
    "1) 在 /targets 页找出被拒绝的目标与具体原因；\n2) 到目标侧确认为何标签数异常多，通常是把高基数信息（用户ID、请求ID）打成了标签；\n3) 在目标侧修正指标设计，或用 metric_relabel_configs 丢弃多余标签；\n4) 确需更多标签的调大 label_limit，但要先评估基数影响。": "1) Find the rejected targets and the specific reason on the /targets page;\n2) On the target side, work out why the label count is so high — usually high-cardinality data (user ID, request ID) was made into labels;\n3) Fix the metric design at the source, or drop the excess labels with metric_relabel_configs;\n4) If more labels are genuinely needed, raise label_limit after assessing the cardinality impact.",
    "1) 在 /targets 页找出被拒绝的目标与样本数；\n2) 手工 curl /metrics | wc -l 确认实际规模；\n3) 高基数指标是主因，在目标侧减少标签维度，或用 metric_relabel_configs 丢弃不需要的指标；\n4) 确需更大限制的调大 sample_limit，但要评估存储与查询压力。": "1) Find the rejected targets and their sample counts on the /targets page;\n2) Run curl /metrics | wc -l by hand to confirm the actual scale;\n3) High-cardinality metrics are the main cause — reduce label dimensions at the source or drop unneeded metrics with metric_relabel_configs;\n4) If a higher limit is genuinely needed, raise sample_limit after assessing storage and query impact.",
    "1) 在 /targets 页找出被拒绝的目标；\n2) 手工 curl 该目标的 /metrics 看实际大小，确认是否指标数量异常膨胀；\n3) 常见根因是高基数指标，在目标侧治理或用 metric_relabel_configs 丢弃；\n4) 确需更大限制的调大 body_size_limit。": "1) Find the rejected targets on the /targets page;\n2) curl that target's /metrics by hand to see the actual size and whether the metric count has ballooned;\n3) High-cardinality metrics are the usual cause — fix at the source or drop with metric_relabel_configs;\n4) If a higher limit is genuinely needed, raise body_size_limit.",
    "1) 在 /targets 页找出被重复抓取的目标（同一实例出现在多个 job 下）；\n2) 检查 ServiceMonitor/PodMonitor 的 selector 是否过于宽泛导致重叠；\n3) 收敛 selector 让每个目标只被一个 job 抓取；\n4) 修正后确认丢弃计数不再增长。": "1) Find the duplicated targets on the /targets page (the same instance appearing under several jobs);\n2) Check whether ServiceMonitor/PodMonitor selectors are too broad and overlapping;\n3) Tighten the selectors so each target is scraped by exactly one job;\n4) After fixing, confirm the discard counter stops growing.",
    "1) 在 /targets 页确认当前目标数与限制；\n2) 检查 ServiceMonitor 的 selector 是否过于宽泛，抓取了不需要的目标；\n3) 收敛 selector 或按业务拆分到多个 Prometheus 实例；\n4) 确需更多目标的调大 target_limit 并评估资源。": "1) Confirm the current target count against the limit on the /targets page;\n2) Check whether the ServiceMonitor selector is too broad and picking up unwanted targets;\n3) Tighten the selector or split across multiple Prometheus instances by business domain;\n4) If more targets are genuinely needed, raise target_limit and size the resources accordingly.",
    "1) 在 /targets 页确认是否所有目标都 down；\n2) 检查服务发现是否正常（/service-discovery 页）；\n3) 配置错误导致目标为空的按配置问题处理；\n4) 这是监控体系的完全失明，应最高优先级处理。": "1) Check on the /targets page whether every target is down;\n2) Check that service discovery is working (the /service-discovery page);\n3) If a config error left the target list empty, treat it as a configuration problem;\n4) This is total monitoring blindness and should be the highest priority.",
    "1) 在控制面节点确认 kube-apiserver 进程/静态 Pod 状态与日志；\n2) 多副本部署确认是否只挂了部分实例，负载均衡是否已把故障实例摘除；\n3) 常见根因是 etcd 不可用、证书过期、资源耗尽；\n4) 优先恢复 etcd 与证书，apiserver 本身通常会自动恢复。": "1) On the control plane node, check the kube-apiserver process/static Pod state and logs;\n2) For multi-replica deployments, confirm whether only some instances are down and whether the load balancer removed the faulty ones;\n3) Common root causes are unavailable etcd, expired certificates and resource exhaustion;\n4) Restore etcd and certificates first — apiserver usually recovers on its own.",
    "1) 在控制面节点确认 kube-controller-manager 静态 Pod 或进程状态与日志；\n2) 多副本部署确认 leader 选举是否正常切换；\n3) 常见根因是 apiserver 不可用或证书过期；\n4) 恢复后确认副本数异常的工作负载开始自动修复。": "1) On the control plane node, check the kube-controller-manager static Pod or process state and logs;\n2) For multi-replica deployments, confirm leader election is failing over correctly;\n3) Common root causes are an unavailable apiserver or expired certificates;\n4) After recovery, confirm workloads with wrong replica counts start self-healing.",
    "1) 在控制面节点确认 kube-scheduler 静态 Pod 或进程状态与日志；\n2) 多副本部署的确认是否只是失去 leader，其他副本能否接管；\n3) 常见根因是 apiserver 不可用或证书问题；\n4) 恢复后确认 Pending 的 Pod 开始被调度。": "1) On the control plane node, check the kube-scheduler static Pod or process state and logs;\n2) For multi-replica deployments, confirm whether it merely lost leadership and another replica can take over;\n3) Common root causes are an unavailable apiserver or certificate problems;\n4) After recovery, confirm Pending Pods start being scheduled.",
    "1) 查看 apiserver 的 apiserver_flowcontrol 相关指标，确认是哪个 flow schema 在被限流；\n2) 定位发起大量请求的客户端（通常是某个 Operator 或脚本在做全量 LIST）；\n3) 对该客户端限流或优化其请求模式；\n4) 确需更高配额的调整 FlowSchema 与 PriorityLevelConfiguration。": "1) Check the apiserver_flowcontrol metrics to see which flow schema is being throttled;\n2) Identify the client making the flood of requests (usually an Operator or script doing full LIST calls);\n3) Rate limit that client or fix its request pattern;\n4) If more capacity is genuinely needed, tune the FlowSchema and PriorityLevelConfiguration.",
    "1) 检查 Alertmanager 实例是否健康、网络是否连通；\n2) 在 Prometheus 的 /status 页确认 Alertmanager 发现是否正常；\n3) 队列满的排查 Alertmanager 处理是否卡住，或告警量是否异常暴涨；\n4) 恢复后确认 Alertmanager 已收到告警，避免静默失败。": "1) Check whether the Alertmanager instances are healthy and reachable;\n2) Confirm Alertmanager discovery on the Prometheus /status page;\n3) If the queue is full, check whether Alertmanager processing is stuck or the alert volume spiked abnormally;\n4) After recovery, confirm Alertmanager actually received the alerts so nothing fails silently.",
    "1) 检查 textfile 目录下的 .prom 文件权限与格式；\n2) 写入方通常是 cron 脚本，确认脚本执行成功且用原子写（先写 .tmp 再 mv）；\n3) 格式错误的用 promtool check metrics 校验；\n4) 清理残留的临时文件，它们会导致解析失败。": "1) Check the permissions and format of the .prom files in the textfile directory;\n2) The writer is usually a cron script — confirm it succeeds and writes atomically (write .tmp then mv);\n3) Validate format errors with promtool check metrics;\n4) Clean up leftover temporary files, which break parsing.",
    "1) 用 chronyc tracking 确认偏移量与上游源状态；\n2) 确认 chronyd/ntpd 服务在运行且开机自启；\n3) 检查到 NTP 服务器的网络是否放行 123/UDP；\n4) 偏移过大用 chronyc makestep 强制对时，之后确认所有节点时间一致。": "1) Run chronyc tracking to check the offset and upstream source state;\n2) Confirm chronyd/ntpd is running and enabled at boot;\n3) Check that 123/UDP to the NTP server is allowed;\n4) For a large offset use chronyc makestep, then confirm all nodes agree on the time.",
    "1) 用 df -h 定位挂载点，用 du -x --max-depth=1 逐层找占用大的目录；\n2) 容器运行时目录（/var/lib/containerd 或 /var/lib/docker）用 crictl rmi --prune 清理无用镜像；\n3) 清理容器日志（/var/log/pods），并确认日志轮转配置生效；\n4) 用 lsof +L1 检查已删除但句柄未释放的文件。": "1) Run df -h to find the mount point and du -x --max-depth=1 to drill into the largest directories;\n2) For the container runtime directory (/var/lib/containerd or /var/lib/docker) run crictl rmi --prune to clean unused images;\n3) Clean container logs (/var/log/pods) and confirm log rotation is working;\n4) Use lsof +L1 for deleted files whose handles are still open.",
    "1) 用 df -i 确认挂载点；\n2) 逐层统计文件数找出小文件最多的目录；\n3) K8s 节点常见是容器层与日志碎片，清理无用镜像通常能释放大量 inode；\n4) 长期可提高 inode 密度或把小文件业务隔离到独立分区。": "1) Run df -i to find the mount point;\n2) Count files level by level to find the directories with the most small files;\n3) On Kubernetes nodes it is usually container layers and log fragments — pruning unused images typically frees a lot of inodes;\n4) Longer term, raise inode density or isolate small-file workloads to a dedicated partition.",
    "1) 用 dmesg -T | grep -i 'killed process' 确认被杀进程与当时内存；\n2) 对照 kubectl get pod -A 找出对应的 Pod，看是否是 limit 设置过低；\n3) 节点整体内存紧张的迁走部分 Pod 或扩容；\n4) 反复被杀的必须在应用侧定位内存泄漏，只调 limit 会反复复发。": "1) Run dmesg -T | grep -i 'killed process' to see which process was killed and how much memory it held;\n2) Match it against kubectl get pod -A to find the Pod and whether its limit is set too low;\n3) If the node is tight overall, move Pods off or add capacity;\n4) Repeated kills require finding the application memory leak — raising the limit alone will just recur.",
    "1) 用 dmesg -T | grep -i 'link is' 看抖动时间线；\n2) 检查网线/光模块，抖动多为物理层接触不良；\n3) 确认协商速率与双工模式和对端一致；\n4) 联系网络侧确认交换机端口状态，云环境下提工单。": "1) Run dmesg -T | grep -i 'link is' for the flap timeline;\n2) Check the cable and optical module — flapping usually means a bad physical connection;\n3) Confirm the negotiated speed and duplex match the peer;\n4) Ask the network team about the switch port state, or open a ticket in the cloud.",
    "1) 用 du -x --max-depth=1 找出增长最快的目录；\n2) 确认增长来源是正常业务还是异常（应用死循环打日志、备份堆积）；\n3) 清理无用镜像与容器日志；\n4) 增长确实来自业务的，安排扩容磁盘或迁移数据，不要等到写满。": "1) Run du -x --max-depth=1 to find the fastest-growing directory;\n2) Determine whether the growth is normal business or abnormal (an application looping on logs, backups piling up);\n3) Prune unused images and container logs;\n4) If the growth is genuine, plan disk expansion or data migration — do not wait until it is full.",
    "1) 用 free -h 与 swapon -s 确认 swap 使用情况；\n2) 用 smem 或 /proc/\u003cpid\u003e/status 的 VmSwap 找出换出最多的进程；\n3) K8s 节点原则上应关闭 swap，开启会掩盖真实的内存压力并让 QoS 失效；\n4) 根因是内存不足，按内存告警路径处理。": "1) Run free -h and swapon -s to check swap usage;\n2) Use smem or VmSwap in /proc/\u003cpid\u003e/status to find the processes swapped out most;\n3) Kubernetes nodes should generally have swap disabled — leaving it on masks real memory pressure and breaks QoS;\n4) The root cause is insufficient memory, so follow the memory alert path.",
    "1) 用 iftop 或 nethogs 定位占用带宽最高的进程/连接；\n2) K8s 节点上确认是否有 Pod 在做大量数据传输（备份、数据同步、镜像拉取）；\n3) 确认是否已跑满网卡带宽，是则限流或错峰；\n4) 异常流量的排查是否有配置错误导致的流量环路。": "1) Use iftop or nethogs to find the processes/connections consuming the most bandwidth;\n2) On Kubernetes nodes, check whether a Pod is doing bulk data transfer (backup, data sync, image pull);\n3) Confirm whether the NIC is saturated and throttle or reschedule if so;\n4) For abnormal traffic, check for a misconfiguration causing a traffic loop.",
    "1) 用 iftop/nethogs 定位占用带宽最高的连接；\n2) K8s 节点上确认是否有大流量 Pod 或正在拉取大镜像；\n3) 可错峰的任务调整执行时间；\n4) 确需更高带宽的升级网卡或迁移部分工作负载。": "1) Use iftop/nethogs to find the connections consuming the most bandwidth;\n2) On Kubernetes nodes, check for high-traffic Pods or large image pulls;\n3) Move deferrable jobs off peak;\n4) If more bandwidth is genuinely needed, upgrade the NIC or move some workloads.",
    "1) 用 iostat -x 1 看 r_await/w_await 与队列深度；\n2) 延迟高但 IOPS 不高的怀疑磁盘硬件问题，用 smartctl -a 检查；\n3) IOPS 也高的按磁盘饱和处理，定位高 IO 进程；\n4) 云盘的确认是否触及规格的 IOPS/吞吐上限被限流。": "1) Run iostat -x 1 and look at r_await/w_await and queue depth;\n2) High latency with low IOPS suggests a disk hardware problem — check with smartctl -a;\n3) If IOPS is also high, treat it as disk saturation and find the heavy IO process;\n4) For cloud disks, check whether the specification's IOPS/throughput ceiling triggered throttling.",
    "1) 用 iostat -x 1 确认是哪块盘、await 与 %util；\n2) 用 iotop -oP 找出 IO 最高的进程；\n3) K8s 节点常见是容器日志写入、镜像拉取解压、或某个 Pod 的数据写入；\n4) 可延后的任务错峰执行，持续高负载的换 SSD 或分散数据。": "1) Run iostat -x 1 to see which disk it is and the actual await and %util;\n2) Run iotop -oP to find the process doing the most IO;\n3) On Kubernetes nodes it is usually container log writes, image pull/unpack, or one Pod's data writes;\n4) Move deferrable work off peak; for sustained load, consider SSDs or spreading the data.",
    "1) 用 ip -s link show \u003c网卡\u003e 与 ethtool -S \u003c网卡\u003e 确认错包计数与类型；\n2) 确认协商速率与双工模式和对端一致；\n3) 检查网线/光模块，云环境下提工单给云厂商；\n4) 有冗余的先把流量切走。": "1) Run ip -s link show \u003cnic\u003e and ethtool -S \u003cnic\u003e for the error counters and types;\n2) Confirm the negotiated speed and duplex match the peer;\n3) Check the cable/optical module, or open a cloud provider ticket;\n4) If redundancy exists, move traffic away first.",
    "1) 用 kubeadm certs check-expiration（kubeadm 集群）或 openssl x509 -noout -dates 确认到期时间；\n2) kubeadm 集群用 kubeadm certs renew all 续期后重启控制面组件；\n3) kubelet 客户端证书通常自动轮换，未轮换的检查 rotateCertificates 配置与 CSR 审批是否正常；\n4) 续期后务必验证组件已加载新证书（重启对应进程），并复查所有节点。": "1) Run kubeadm certs check-expiration (kubeadm clusters) or openssl x509 -noout -dates to confirm the expiry;\n2) On kubeadm clusters run kubeadm certs renew all and restart the control plane components;\n3) kubelet client certificates usually rotate automatically — if not, check the rotateCertificates setting and CSR approval;\n4) After renewal, verify the components loaded the new certificate (restart them) and re-check every node.",
    "1) 用 kubectl get node -o wide 对比各节点内核版本；\n2) 确认差异是升级过程中的临时状态还是长期存在；\n3) 逐步把落后节点的内核升级对齐；\n4) 升级前确认容器运行时与 CNI 插件对目标内核版本的兼容性。": "1) Run kubectl get node -o wide to compare kernel versions;\n2) Determine whether the difference is temporary (mid-upgrade) or long standing;\n3) Bring the lagging nodes' kernels in line step by step;\n4) Before upgrading, confirm the container runtime and CNI plugin support the target kernel.",
    "1) 用 sysctl net.netfilter.nf_conntrack_count 与 nf_conntrack_max 确认水位；\n2) K8s 节点上 kube-proxy 的 iptables 模式会产生大量 conntrack 条目，Pod 密度高时尤其明显；\n3) 调大 nf_conntrack_max 并同步调大 buckets；\n4) 长期可评估切换到 IPVS 模式减少 conntrack 压力。": "1) Run sysctl net.netfilter.nf_conntrack_count and nf_conntrack_max to check the level;\n2) On Kubernetes nodes, kube-proxy in iptables mode creates many conntrack entries, especially at high Pod density;\n3) Raise nf_conntrack_max and the bucket count together;\n4) Longer term, consider switching to IPVS mode to reduce conntrack pressure.",
    "1) 用 top 或 vmstat 确认 st 列的持续水位；\n2) 这是宿主机层面的资源争抢，虚拟机内部无法根治；\n3) 云环境下联系云厂商或更换实例规格（独享型规格无 steal）；\n4) 自建虚拟化的检查宿主机超卖比例并迁移虚机。": "1) Use top or vmstat to confirm the sustained level of the st column;\n2) This is contention at the hypervisor level and cannot be fixed from inside the VM;\n3) In the cloud, contact the provider or move to a dedicated instance type (no steal);\n4) On self-hosted virtualization, check the host's overcommit ratio and migrate VMs.",
    "1) 用 uptime 看 1/5/15 分钟趋势判断是突发还是持续；\n2) top 里看 %wa，高说明瓶颈在磁盘而非 CPU；\n3) vmstat 1 用 r/b 列区分 CPU 密集与 IO 阻塞；\n4) K8s 节点上用 kubectl top pod 定位高负载 Pod 并考虑迁走。": "1) Run uptime to see the 1/5/15 minute trend and judge spike versus sustained;\n2) Check %wa in top — high means the bottleneck is disk rather than CPU;\n3) Use the r/b columns in vmstat 1 to separate CPU-bound from IO-blocked;\n4) On Kubernetes nodes use kubectl top pod to find the heavy Pods and consider moving them.",
    "1) 用 vmstat 1 看 si/so 列确认是否已在 swap；\n2) 用 sar -B 或 /proc/vmstat 看 pgmajfault 的增长；\n3) 定位占用最高的进程或 Pod 并评估是否可迁走；\n4) 确认 swap 配置——K8s 节点通常应关闭 swap，开启会掩盖内存问题。": "1) Run vmstat 1 and check the si/so columns for swapping;\n2) Use sar -B or /proc/vmstat to watch pgmajfault growth;\n3) Find the heaviest process or Pod and assess whether it can be moved;\n4) Review the swap setting — Kubernetes nodes normally have swap disabled, since enabling it masks memory problems.",
    "1) 用 vmstat 1 确认 cs 列的水位；\n2) 用 pidstat -w 1 找出切换最频繁的进程；\n3) 常见根因是线程数过多、锁竞争激烈、或大量短生命周期进程；\n4) 应用侧收敛线程池大小、优化锁粒度是根本解法。": "1) Run vmstat 1 and check the cs column;\n2) Run pidstat -w 1 to find the processes switching most;\n3) Common root causes are too many threads, heavy lock contention, or many short-lived processes;\n4) Reducing thread pool sizes and lock granularity in the application is the real fix.",
    "1) 登录节点执行 systemctl status kubelet 确认进程状态；\n2) journalctl -u kubelet -n 200 --no-pager 定位退出或报错原因；\n3) 常见为证书过期、配置错误、容器运行时不可用；\n4) 恢复后确认节点回到 Ready，并检查期间是否有 Pod 被误驱逐。": "1) On the node run systemctl status kubelet to check the process;\n2) Use journalctl -u kubelet -n 200 --no-pager to find the exit or error reason;\n3) Common causes are expired certificates, config errors and an unavailable container runtime;\n4) After recovery, confirm the node is Ready again and check whether Pods were wrongly evicted meanwhile.",
    "1) 登录节点用 crictl ps 与 crictl info 确认容器运行时响应速度；\n2) 检查节点上容器数量是否过多、磁盘 IO 是否打满——这两者是 PLEG 变慢的主因；\n3) 容器运行时卡顿的可尝试重启 containerd（会短暂影响该节点）；\n4) 持续存在的考虑降低该节点的 Pod 密度。": "1) On the node run crictl ps and crictl info to gauge container runtime responsiveness;\n2) Check whether the node has too many containers and whether disk IO is saturated — these are the main causes of slow PLEG;\n3) For a stalled runtime, consider restarting containerd (briefly affects that node);\n4) If it persists, reduce the Pod density on that node.",
    "1) 登录节点用 df -h 定位挂载点，通常是 /var/lib/docker 或 /var/lib/containerd；\n2) 执行 crictl rmi --prune 或 docker system prune 清理无用镜像层，这是最快见效的；\n3) 检查是否有 Pod 在往 emptyDir 或宿主机路径写大量数据；\n4) 清理后确认 kubelet 的 DiskPressure 污点已自动移除。": "1) On the node run df -h to find the mount point, usually /var/lib/docker or /var/lib/containerd;\n2) Run crictl rmi --prune or docker system prune to clean unused image layers — the fastest win;\n3) Check whether a Pod is writing large amounts of data to an emptyDir or hostPath;\n4) After cleaning, confirm the kubelet DiskPressure taint was removed automatically.",
    "1) 登录节点用 free -h 确认 available 与 swap；\n2) ps aux --sort=-%mem | head 找出占用最高的进程；\n3) K8s 节点上优先用 kubectl top pod -A --sort-by=memory 定位是哪个 Pod；\n4) 泄漏的在应用侧定位，容量不足的迁走部分 Pod 或扩容节点。": "1) On the node run free -h to check available memory and swap;\n2) Run ps aux --sort=-%mem | head for the biggest consumers;\n3) On Kubernetes nodes, prefer kubectl top pod -A --sort-by=memory to find the Pod;\n4) For a leak, fix it in the application; for capacity, move Pods off or add nodes.",
    "1) 看 Prometheus 日志中 remote_write 或 tsdb 相关报错；\n2) 远程写入失败的确认远端存储可用性与鉴权配置，并看是否触发了限流；\n3) 本地存储失败的检查磁盘空间与权限，TSDB 损坏时可能需要清理 WAL；\n4) 确认队列积压是否在下降，持续积压需要调大 queue_config 或降低写入量。": "1) Check the Prometheus log for remote_write or tsdb errors;\n2) For remote write failures, confirm remote storage availability and credentials, and check for throttling;\n3) For local storage failures, check disk space and permissions — a corrupted TSDB may require clearing the WAL;\n4) Confirm the queue backlog is draining; if not, raise queue_config or reduce the write volume.",
    "1) 看 apiserver 的错误码与延迟分布，确认是读还是写请求在劣化；\n2) 写请求劣化优先排查 etcd 的磁盘延迟——这是最常见根因；\n3) 读请求劣化排查是否有客户端在做大范围 LIST（无 resourceVersion 的全量列举）；\n4) 定位到异常客户端的对其限流，并考虑开启 APF（API Priority and Fairness）。": "1) Look at the apiserver error code and latency distribution to tell read from write degradation;\n2) For write degradation, check etcd disk latency first — that is the most common root cause;\n3) For read degradation, look for clients doing wide LIST calls (full enumeration without a resourceVersion);\n4) Rate limit the offending client and consider enabling APF (API Priority and Fairness).",
    "1) 看 kubelet 日志中 certificate rotation 相关报错；\n2) 检查 CSR 是否处于 Pending 未审批：kubectl get csr，必要时手工 kubectl certificate approve；\n3) 确认集群是否开启了 CSR 自动审批控制器（kube-controller-manager 的 --cluster-signing-cert-file 配置）；\n4) 修复后确认新 CSR 能被自动审批并签发。": "1) Check the kubelet log for certificate rotation errors;\n2) Check whether CSRs are stuck Pending: kubectl get csr, and approve manually with kubectl certificate approve if needed;\n3) Confirm the cluster has a CSR auto-approval controller (kube-controller-manager's --cluster-signing-cert-file setting);\n4) After fixing, confirm new CSRs are auto-approved and signed.",
    "1) 看日志中 compaction 相关报错；\n2) 最常见是磁盘空间不足——先确认剩余空间；\n3) 数据块损坏的可能需要删除损坏的 block 目录（会丢失该段数据）；\n4) 修复后确认后续 compaction 成功完成。": "1) Check the log for compaction errors;\n2) The most common cause is insufficient disk space — confirm the free space first;\n3) A corrupted block may need its directory deleted (losing that time range);\n4) After fixing, confirm subsequent compactions complete successfully.",
    "1) 看日志中 reload 相关报错与涉及的 block；\n2) 常见为数据块损坏或权限问题；\n3) 权限问题修正数据目录属主；损坏的 block 只能删除（会丢失该段数据）；\n4) 修复后重启 Prometheus 并确认数据可查。": "1) Check the log for reload errors and which block is involved;\n2) It is usually a corrupted block or a permissions problem;\n3) For permissions, fix the data directory owner; a corrupted block can only be deleted (losing that time range);\n4) After fixing, restart Prometheus and confirm the data is queryable.",
    "1) 看日志中 rule evaluation 相关的超时报错；\n2) 找出评估耗时最长的规则组，通常是包含重查询的记录规则；\n3) 拆分规则组、拉长 evaluation_interval，或优化规则里的查询；\n4) 资源不足的扩容 Prometheus。": "1) Check the log for rule evaluation timeout errors;\n2) Find the rule group with the longest evaluation time, usually recording rules with heavy queries;\n3) Split the rule group, lengthen evaluation_interval, or optimise the queries in the rules;\n4) If resources are short, scale Prometheus.",
    "1) 看日志中 target sync 的具体报错；\n2) 常见为 relabel 配置错误导致目标地址非法；\n3) 检查最近变更的 ServiceMonitor/PodMonitor 配置；\n4) 修正后在 /targets 页确认目标恢复正常。": "1) Check the log for the specific target sync error;\n2) It is usually a relabel misconfiguration producing an invalid target address;\n3) Review recently changed ServiceMonitor/PodMonitor configurations;\n4) After fixing, confirm the targets are healthy again on the /targets page.",
    "1) 看日志中失败规则的名称与错误；\n2) 常见为规则里的查询语法错误、引用了不存在的指标、或返回数据量过大；\n3) 用 promtool check rules 校验规则文件；\n4) 修正后确认 /rules 页该规则状态为 ok。": "1) Check the log for the failing rule name and error;\n2) Common causes are a query syntax error in the rule, a reference to a non-existent metric, or an oversized result;\n3) Validate the rule files with promtool check rules;\n4) After fixing, confirm the rule shows ok on the /rules page.",
    "1) 看日志中服务发现相关的报错；\n2) K8s SD 的检查 Prometheus 的 RBAC 权限是否覆盖所需资源；\n3) 其他 SD（Consul/文件等）的检查后端可达性与凭据；\n4) 修复后在 /service-discovery 页确认目标列表已更新。": "1) Check the log for service discovery errors;\n2) For Kubernetes SD, check that Prometheus's RBAC covers the required resources;\n3) For other SD types (Consul, file, …), check backend reachability and credentials;\n4) After fixing, confirm the target list is updated on the /service-discovery page.",
    "1) 确认是哪个目标在上报乱序数据；\n2) 常见根因是目标机器时钟不同步，或指标由多个来源汇聚后时间戳错乱；\n3) 先校准目标机器时钟（chronyc）；\n4) Pushgateway 类场景确认推送端的时间戳处理逻辑。": "1) Identify which target is reporting out-of-order data;\n2) The usual root causes are an unsynchronised clock on the target, or metrics aggregated from multiple sources with mixed timestamps;\n3) Correct the target machine's clock first (chronyc);\n4) For Pushgateway-style setups, review how the pusher handles timestamps.",
    "5分钟内apiserver的请求数统计": "Apiserver request count in the last 5 minutes",
    "CPU 使用量除以 CPU 请求量。若没有数据需要检查相应容器是否进行了 CPU Request 的配置。": "CPU usage divided by CPU request. If there is no data, check whether the container has a CPU Request configured.",
    "CPU 使用量除以 CPU 限制量。若没有数据需要检查相应容器是否进行了 CPU Limit 的配置。": "CPU usage divided by CPU limit. If there is no data, check whether the container has a CPU Limit configured.",
    "Container Network - 接收": "Container Network - Receive",
    "Container Network - 请求": "Container Network - Transmit",
    "DaemonSet未就绪Pod": "DaemonSet has unready Pods",
    "Deployment Pod 可用性不足70%（replica超过9个pod）": "Deployment Pod availability below 70% (replicas above 9 pods)",
    "Deployment 历史更新保留版本的数量": "Number of revision history versions retained by the Deployment",
    "Deployment副本异常": "Deployment replicas mismatch",
    "HPA扩容失败": "HPA scaling failed",
    "Job执行失败": "Job failed",
    "KubeAPIErrorBudgetBurn-S1-120秒": "KubeAPIErrorBudgetBurn-S1-120s",
    "KubeAPIErrorBudgetBurn-S1-900秒": "KubeAPIErrorBudgetBurn-S1-900s",
    "KubeAPIErrorBudgetBurn-S2-10800秒": "KubeAPIErrorBudgetBurn-S2-10800s",
    "KubeAPIErrorBudgetBurn-S2-3600秒": "KubeAPIErrorBudgetBurn-S2-3600s",
    "Node CPU使用率超过90%": "Node CPU usage exceeds 90%",
    "Node不可达": "Node unreachable",
    "Node内存使用率超过90%": "Node memory usage exceeds 90%",
    "Node状态异常": "Node not ready",
    "Node状态抖动": "Node readiness flapping",
    "Node磁盘使用率超过90%": "Node disk usage exceeds 90%",
    "Node运行太多Pod": "Node running too many Pods",
    "PLEG耗时高": "PLEG relisting duration high",
    "Pod启动耗时高": "Pod startup duration high",
    "Pod启动超时": "Pod startup timeout",
    "Pod处于Failed状态": "Pod in Failed state",
    "Pod数量": "Pod Count",
    "Pod状态异常": "Pod state abnormal",
    "Pod频繁重启": "Pod restarting frequently",
    "RSS + 部分活跃的 page cache（不能立即回收）， 更应该关注此指标": "RSS + part of the active page cache (cannot be reclaimed immediately); this is the metric to focus on",
    "StatefulSet副本异常": "StatefulSet replicas mismatch",
    "apiserver工作队列深度，越接近0越好": "Apiserver work queue depth; the closer to 0 the better",
    "apiserver的5xx 错误率(5xx请求数/总请求数)": "Apiserver 5xx error rate (5xx requests / total requests)",
    "apiserver的5xx错误率，按请求动作分类统计": "Apiserver 5xx error rate by request verb",
    "apiserver的cpu使用率": "Apiserver CPU usage",
    "apiserver的内存使用量": "Apiserver memory usage",
    "apiserver的响应延迟(非watch请求）": "Apiserver response latency (non-watch requests)",
    "apiserver的响应延迟，按请求动作分类统计": "Apiserver response latency by request verb",
    "apiserver的实例健康状态，0表示down，1表示up": "Apiserver instance health status; 0 means down, 1 means up",
    "conntrack数量接近极限": "Conntrack entries approaching the limit",
    "controller manager 90分位 任务出队到完成的耗时": "Controller manager P90 duration from work item dequeue to completion",
    "controller manager 90分位请求apiserver延迟，按请求类型统计": "Controller manager P90 apiserver request latency by request type",
    "controller manager cpu使用率": "Controller manager CPU usage",
    "controller manager workqueue 添加任务数的速率": "Controller manager workqueue add rate",
    "controller manager zone中的node数据": "Controller manager Node count per zone",
    "controller manager 主从状态": "Controller manager leader/follower status",
    "controller manager 任务进入队列的重试次数": "Controller manager work item enqueue retries",
    "controller manager 内存使用量": "Controller manager memory usage",
    "controller manager 绑定/非绑定的pvc数目": "Controller manager bound/unbound PVC count",
    "controller manager 绑定的pv数目": "Controller manager bound PV count",
    "controller manager 请求量 按返回码统计": "Controller manager request count by response code",
    "controller manager90分位请求延迟，按url统计": "Controller manager P90 request latency by URL",
    "controller manager90分位请求延迟，按实例统计": "Controller manager P90 request latency by instance",
    "controller manager健康状态": "Controller manager health status",
    "controller manager打开的fd数量": "Controller manager open FD count",
    "controller manager请求(apiserver)量": "Controller manager request count (apiserver)",
    "controller manager请求5xx，按实例统计": "Controller manager 5xx requests by instance",
    "controller manager请求5xx，按请求类型统计": "Controller manager 5xx requests by request type",
    "controller manager请求量，按请求类型统计": "Controller manager request count by request type",
    "core dns健康状态": "CoreDNS health status",
    "coredns cache 条目数": "CoreDNS cache entries",
    "coredns cpu使用率": "CoreDNS CPU usage",
    "coredns 内存使用量": "CoreDNS memory usage",
    "coredns 平均包大小": "CoreDNS average packet size",
    "coredns 请求量 按返回码统计": "CoreDNS request count by response code",
    "coredns 请求量，按请求类型统计": "CoreDNS request count by request type",
    "coredns缓存命中/缺失次数": "CoreDNS cache hits/misses",
    "coredns请求量": "CoreDNS request count",
    "coredns转发请求量": "CoreDNS forwarded request count",
    "etcd 90分位WAL fsync耗时": "etcd P90 WAL fsync latency",
    "etcd 90分位commit耗时": "etcd P90 commit latency",
    "etcd 90分位defrag耗时": "etcd P90 defrag latency",
    "etcd 90分位snapshot耗时": "etcd P90 snapshot latency",
    "etcd cpu使用率": "etcd CPU usage",
    "etcd defrag（压缩） 状态": "etcd defrag (compaction) status",
    "etcd leader切换统计": "etcd leader change count",
    "etcd snapshot 状态": "etcd snapshot status",
    "etcd snapshot（快照） 状态": "etcd snapshot status",
    "etcd 健康检查状态": "etcd health check status",
    "etcd 内存使用量": "etcd memory usage",
    "etcd 处理的请求数，按响应码统计": "etcd handled request count by response code",
    "etcd 存储quota/total/in use大小": "etcd storage quota/total/in use size",
    "etcd健康状态": "etcd health status",
    "etcd协商失败/pending次数": "etcd proposal failure/pending count",
    "etcd处理的请求数， 按实例统计": "etcd handled request count by instance",
    "etcd处理的请求数，按请求类型统计": "etcd handled request count by request type",
    "etcd打开的fd数量": "etcd open FD count",
    "etcd操作类型统计": "etcd operation count by type",
    "etcd读取索引失败 读取索引慢（过期索引或者超时）次数": "etcd read index failure and slow read index count (stale index or timeout)",
    "inode数量": "Inode Count",
    "ioawaiting task 数量": "IO awaiting task count",
    "kubelet故障": "Kubelet down",
    "pod存活率": "Pod availability rate",
    "pod进入调度队列的平均速率": "Average rate of Pods entering the scheduling queue",
    "ready的pod数 / 总的副本数": "Ready Pods / total replicas",
    "running task 数量": "Running task count",
    "scheduler 90分位请求延迟，按实例统计": "Scheduler P90 request latency by instance",
    "scheduler 90分位请求延迟，按请求类型统计": "Scheduler P90 request latency by request type",
    "scheduler cpu使用率": "Scheduler CPU usage",
    "scheduler 主从状态": "Scheduler leader/follower status",
    "scheduler 内存使用量": "Scheduler memory usage",
    "scheduler 请求量 按返回码统计": "Scheduler request count by response code",
    "scheduler健康状态": "Scheduler health status",
    "scheduler打开的fd数量": "Scheduler open FD count",
    "scheduler请求(apiserver)量": "Scheduler request count (apiserver)",
    "scheduler请求5xx，按实例统计": "Scheduler 5xx requests by instance",
    "scheduler请求5xx，按请求类型统计": "Scheduler 5xx requests by request type",
    "scheduler请求量，按请求类型统计": "Scheduler request count by request type",
    "sleeping task 数量": "Sleeping task count",
    "stopped task 数量": "Stopped task count",
    "uninterruptible task 数量": "Uninterruptible task count",
    "不可调度Node数量": "Unschedulable Node Count",
    "主机 CPU 负载高 (instance {{ $labels.instance }})": "Host high CPU load (instance {{ $labels.instance }})",
    "主机 inode 将在 24 小时内填满 (instance {{ $labels.instance }})": "Host inodes will fill up within 24 hours (instance {{ $labels.instance }})",
    "主机 systemd 服务崩溃 (instance {{ $labels.instance }})": "Host systemd service crashed (instance {{ $labels.instance }})",
    "主机上下文切换 (instance {{ $labels.instance }})": "Host context switching (instance {{ $labels.instance }})",
    "主机上下文切换异常": "Host context switching abnormal",
    "主机不足 inode (instance {{ $labels.instance }})": "Host out of inodes (instance {{ $labels.instance }})",
    "主机交换内存已满 (instance {{ $labels.instance }})": "Host swap is full (instance {{ $labels.instance }})",
    "主机交换内存快满了": "Host swap almost full",
    "主机入口网络吞吐量异常": "Host inbound network throughput abnormal",
    "主机内存不足": "Host out of memory",
    "主机内存有压力": "Host under memory pressure",
    "主机内核版本偏差": "Host kernel version deviation",
    "主机内核版本偏差 (instance {{ $labels.instance }})": "Host kernel version deviation (instance {{ $labels.instance }})",
    "主机出口网络吞吐量异常": "Host outbound network throughput abnormal",
    "主机异常磁盘写入延迟 (instance {{ $labels.instance }})": "Host unusual disk write latency (instance {{ $labels.instance }})",
    "主机异常磁盘写入率 (instance {{ $labels.instance }})": "Host unusual disk write rate (instance {{ $labels.instance }})",
    "主机异常磁盘读取延迟 (instance {{ $labels.instance }})": "Host unusual disk read latency (instance {{ $labels.instance }})",
    "主机异常磁盘读取率 (instance {{ $labels.instance }})": "Host unusual disk read rate (instance {{ $labels.instance }})",
    "主机异常网络吞吐量 入 (instance {{ $labels.instance }})": "Host unusual network throughput in (instance {{ $labels.instance }})",
    "主机异常网络吞吐量 出 (instance {{ $labels.instance }})": "Host unusual network throughput out (instance {{ $labels.instance }})",
    "主机时钟不同步": "Host clock not synchronising",
    "主机时钟不同步 (instance {{ $labels.instance }})": "Host clock not synchronising (instance {{ $labels.instance }})",
    "主机时钟偏差": "Host clock skew",
    "主机时钟偏差 (instance {{ $labels.instance }})": "Host clock skew (instance {{ $labels.instance }})",
    "主机磁盘写入延迟异常": "Host disk write latency abnormal",
    "主机磁盘写入速率异常": "Host disk write rate abnormal",
    "主机磁盘将在 24 小时内填满": "Host disk will fill up within 24 hours",
    "主机磁盘将在 24 小时内填满 (instance {{ $labels.instance }})": "Host disk will fill up within 24 hours (instance {{ $labels.instance }})",
    "主机磁盘空间不足": "Host out of disk space",
    "主机磁盘空间不足 (instance {{ $labels.instance }})": "Host out of disk space (instance {{ $labels.instance }})",
    "主机磁盘读取延迟异常": "Host disk read latency abnormal",
    "主机磁盘读取速率异常": "Host disk read rate abnormal",
    "主机系统服务崩溃": "Host system service crashed",
    "主机网络传输错误 (instance {{ $labels.instance }})": "Host network transmit errors (instance {{ $labels.instance }})",
    "主机网络出口错误": "Host network transmit errors",
    "主机网络接口饱和": "Host network interface saturated",
    "主机网络接口饱和 (instance {{ $labels.instance }})": "Host network interface saturated (instance {{ $labels.instance }})",
    "主机网络接收错误": "Host network receive errors",
    "主机网络接收错误 (instance {{ $labels.instance }})": "Host network receive errors (instance {{ $labels.instance }})",
    "主机连接限制 (instance {{ $labels.instance }})": "Host conntrack limit (instance {{ $labels.instance }})",
    "任务在队列中的90分位等待耗时": "P90 time work items spend waiting in the queue",
    "使用的节点Conntrack条目数量高": "Node conntrack entries usage high",
    "兼容阿里ACK和categraf采集的指标": "Compatible with metrics collected by Alibaba ACK and Categraf",
    "内存": "Memory",
    "内存使用量除以内存请求量。若没有数据需要检查相应容器是否进行了 Memory Request 的配置。": "Memory usage divided by memory request. If there is no data, check whether the container has a Memory Request configured.",
    "内存使用量除以内存限制量。若没有数据需要检查相应容器是否进行了 Memory Limit 的配置。": "Memory usage divided by memory limit. If there is no data, check whether the container has a Memory Limit configured.",
    "各个controller 队列深度": "Queue depth of each controller",
    "启动时长": "Uptime",
    "实际“工作集内存”，即真正被频繁访问、内核不愿回收的内存，OOM 风险监控": "The actual working set memory, i.e. memory that is frequently accessed and that the kernel is reluctant to reclaim; used to monitor OOM risk",
    "客户端证书续签错误": "Client certificate renewal errors",
    "客户端证书过期-S1": "Client certificate expiring - S1",
    "客户端证书过期-S2": "Client certificate expiring - S2",
    "容器 CPU 使用量相对于限制量/请求量的比例。若没有数据需要检查相应容器是否进行了 CPU Limit/Request 的配置。": "Container CPU usage as a ratio of its limit/request. If there is no data, check whether the container has a CPU Limit/Request configured.",
    "容器 WorkingSet 内存使用量相对于限制量/请求量的比例。若没有数据需要检查相应容器是否进行了 Memory Limit/Request 的配置。": "Container WorkingSet memory usage as a ratio of its limit/request. If there is no data, check whether the container has a Memory Limit/Request configured.",
    "容器1min CPU平均使用率，如果pod内有多个容器，会分别显示各个容器CPU使用率（pause容器默认不会显示）；如果容器配置了4核，使用率100%表示当前用满4核，容器配置2核，使用率100%表示当前用满2核，如果容器没有配置cpu limit，则不会显示该数值": "Average container CPU usage over 1 min. If a Pod has multiple containers, the CPU usage of each container is shown separately (the pause container is hidden by default). If a container is configured with 4 cores, 100% usage means all 4 cores are fully used; if it is configured with 2 cores, 100% usage means all 2 cores are fully used. If the container has no cpu limit configured, this value is not shown.",
    "容器1min CPU平均使用率，如果pod内有多个容器，会分别显示各个容器CPU使用率（pause容器默认不会显示）；如果容器配置了4核，使用率400%表示当前用满4核，容器配置2核，使用率200%表示当前用满2核。": "Average container CPU usage over 1 min. If a Pod has multiple containers, the CPU usage of each container is shown separately (the pause container is hidden by default). If a container is configured with 4 cores, 400% usage means all 4 cores are fully used; if it is configured with 2 cores, 200% usage means all 2 cores are fully used.",
    "容器1min 内核态CPU平均使用率，如果pod内有多个容器，会分别显示各个容器内核态CPU使用率（pause容器默认不会显示）；如果容器配置了4核，使用率400%表示当前内核态用满4核，容器配置2核，使用率200%表示当前内核态用满2核。": "Average container system-mode CPU usage over 1 min. If a Pod has multiple containers, the system-mode CPU usage of each container is shown separately (the pause container is hidden by default). If a container is configured with 4 cores, 400% usage means all 4 cores are fully used in system mode; if it is configured with 2 cores, 200% usage means all 2 cores are fully used in system mode.",
    "容器1min 内核态CPU平均使用率，如果pod内有多个容器，会分别显示各个容器用户态CPU使用率（pause容器默认不会显示）；如果容器配置了4核，使用率400%表示当前用户态用满4核，容器配置2核，使用率200%表示当前用户态用满2核。": "Average container system-mode CPU usage over 1 min. If a Pod has multiple containers, the user-mode CPU usage of each container is shown separately (the pause container is hidden by default). If a container is configured with 4 cores, 400% usage means all 4 cores are fully used in user mode; if it is configured with 2 cores, 200% usage means all 2 cores are fully used in user mode.",
    "容器CPU Limit": "Container CPU Limit",
    "容器CPU Limit，2代表容器CPU Limit为2核， 0.2代表容器CPU Limit为0.2核， 没有数据表明没有设置Limit值": "Container CPU Limit; 2 means the container CPU Limit is 2 cores, 0.2 means 0.2 cores, and no data means no Limit is set",
    "容器CPU load 10": "Container CPU Load 10",
    "容器CPU使用率": "Container CPU Usage",
    "容器CPU使用率超过80%": "Container CPU usage exceeds 80%",
    "容器CPU归一化后使用率": "Container Normalized CPU Usage",
    "容器CPU归一化后使用率(已使用/Limit)": "Container Normalized CPU Usage (Used / Limit)",
    "容器I/O": "Container I/O",
    "容器RSS内存使用": "Container RSS Memory Usage",
    "容器RSS内存占用大小。": "Container RSS memory footprint.",
    "容器RSS内存占用大小，最能反映应用的“真实内存使用”\n1. 通常用于排查内存泄漏（RSS 持续增加往往代表泄漏）\n2. 是 OS 无法轻易回收的部分": "Container RSS memory footprint, which best reflects the real memory usage of an application\n1. Commonly used to troubleshoot memory leaks (continuously growing RSS usually indicates a leak)\n2. It is the part that the OS cannot easily reclaim",
    "容器cache 占用大小": "Container cache footprint",
    "容器cache使用": "Container Cache Usage",
    "容器使用的CPU核数": "CPU Cores Used by Container",
    "容器使用的内存大小": "Memory Used by Container",
    "容器内存 Limit": "Container Memory Limit",
    "容器内存Limit配置，0表示没有设置内存 Limit": "Container memory Limit configuration; 0 means no memory Limit is set",
    "容器内存使用": "Container Memory Used",
    "容器内存使用率": "Container Memory Usage",
    "容器内存使用率(已使用/Limit)": "Container Memory Usage (Used / Limit)",
    "容器内存超过80%": "Container memory usage exceeds 80%",
    "容器内核态CPU使用核数": "Container System-Mode CPU Cores Used",
    "容器内核态CPU使用率": "Container System-Mode CPU Usage",
    "容器发生CPU throttle的比率": "Container CPU throttle ratio",
    "容器发生OOM次数": "Container OOM count",
    "容器可写层（Writable Layer）的磁盘配额大小": "Disk quota size of the container Writable Layer",
    "容器启动时长（小时）": "Container Uptime (Hours)",
    "容器在所有 CFS period 中，被限流的比例（百分比）\n1.0% 表示：每 100 个周期有 1 个周期被 throttle": "The percentage of all CFS periods in which the container was throttled\n1.0% means 1 out of every 100 periods was throttled",
    "容器已使用的文件系统大小": "Filesystem size used by the container",
    "容器文件系统Limit": "Container Filesystem Limit",
    "容器用户态CPU使用核数": "Container User-Mode CPU Cores Used",
    "容器用户态CPU使用率": "Container User-Mode CPU Usage",
    "容器网络发送的字节数（1 分钟内）": "Bytes sent by the container network (last 1 minute)",
    "容器网络接收的字节数（1 分钟内）": "Bytes received by the container network (last 1 minute)",
    "展示该工作负载每个 Pod 的 CPU 使用量。": "Shows the CPU usage of each Pod in this workload.",
    "展示该工作负载的每个 Pod 内存使用量。": "Shows the memory usage of each Pod in this workload.",
    "工作副本数量统计": "Workload replica count",
    "当前并发请求apiserver的数量": "Current number of concurrent apiserver requests",
    "当前版本apiserver使用，未来版本中要移除的资源": "Resources used by the current apiserver version that will be removed in future versions",
    "按照请求动作分类统计apiserver的请求数": "Apiserver request count by request verb",
    "按照返回码分类统计apiserver请求数": "Apiserver request count by response code",
    "整体概况": "Overview",
    "文件系统inode使用率高": "Filesystem inode usage high",
    "文件系统inode将在24小时内填满": "Filesystem inodes will fill up within 24 hours",
    "文件系统写入速率": "Filesystem Write Rate",
    "文件系统读取速率": "Filesystem Read Rate",
    "有PID压力": "Under PID Pressure",
    "有其他云主机窃取cpu": "CPU stolen by other cloud guests",
    "有内存压力": "Under Memory Pressure",
    "有磁盘压力": "Under Disk Pressure",
    "有网络压力": "Network Unavailable",
    "服务端证书过期-S1": "Server certificate expiring - S1",
    "服务端证书过期-S2": "Server certificate expiring - S2",
    "服务证书续签错误": "Server certificate renewal errors",
    "查看每个容器使用的cpu核数": "View the CPU cores used by each container",
    "检测到 HostOomKill": "HostOomKill detected",
    "检测到主机 OOM 终止 (instance {{ $labels.instance }})": "Host OOM kill detected (instance {{ $labels.instance }})",
    "每个容器的内存使用量": "Memory usage of each container",
    "没有配额的情况下默认值是0": "The default value is 0 when no quota is set",
    "用户级 CPU 使用率相对于整体 CPU 使用量的比例。": "User-level CPU usage as a ratio of overall CPU usage.",
    "用户级 CPU 消耗占整体 CPU 消耗量的比例": "Ratio of user-level CPU consumption to overall CPU consumption",
    "磁盘": "Disk",
    "网络": "Network",
    "网络发送丢包数": "Network Transmit Dropped Packets",
    "网络发送丢包数（1分钟内）": "Network transmit dropped packets (last 1 minute)",
    "网络发送数据包": "Network Transmit Packets",
    "网络发送数据包数（1分钟内）": "Network transmit packets (last 1 minute)",
    "网络发送速率": "Network Transmit Rate",
    "网络发送速率(Top10)": "Network Transmit Rate (Top 10)",
    "网络发送错误数": "Network Transmit Errors",
    "网络发送错误数（1分钟内）": "Network transmit errors (last 1 minute)",
    "网络接收丢包数": "Network Receive Dropped Packets",
    "网络接收丢包数（1分钟内）": "Network receive dropped packets (last 1 minute)",
    "网络接收数据包数": "Network Receive Packets",
    "网络接收数据包数（1分钟内）": "Network receive packets (last 1 minute)",
    "网络接收速率": "Network Receive Rate",
    "网络接收速率(Top10)": "Network Receive Rate (Top 10)",
    "网络接收错误数": "Network Receive Errors",
    "网络接收错误数（1分钟内）": "Network receive errors (last 1 minute)",
    "节点cpu负载高": "Node CPU load high",
    "节点内存不足 (instance {{ $labels.instance }})": "Node out of memory (instance {{ $labels.instance }})",
    "节点内存压力大 (instance {{ $labels.instance }})": "Node under memory pressure (instance {{ $labels.instance }})",
    "节点文件系统空间填满-S1": "Node filesystem filling up - S1",
    "节点文件系统空间填满-S2": "Node filesystem filling up - S2",
    "节点文本文件收集器抓取错误": "Node textfile collector scrape errors",
    "节点磁盘快满了-S1-inodes-20": "Node disk almost full - S1 - inodes - 20",
    "节点磁盘快满了-S1-inodes-3": "Node disk almost full - S1 - inodes - 3",
    "节点磁盘快满了-S1-space-3": "Node disk almost full - S1 - space - 3",
    "节点磁盘快满了-S2-inodes-40": "Node disk almost full - S2 - inodes - 40",
    "节点磁盘快满了-S2-inodes-5": "Node disk almost full - S2 - inodes - 5",
    "节点磁盘快满了-S2-space-5": "Node disk almost full - S2 - space - 5",
    "节点网络接口抖动": "Node network interface flapping",
    "该值大于0，说明CPU在运行期间发生throttle情况，即容器设置的CPU规格，不满足容器当前对CPU的实际需求，在这种情况下，往往需要调大容器CPU规格，或者优化程序，降低CPU开销。": "A value greater than 0 means CPU throttling occurred at runtime, i.e. the CPU spec configured for the container does not meet its actual CPU demand. In this case you usually need to raise the container CPU spec, or optimize the program to reduce CPU cost.",
    "请确认Pod Yaml中设置了resources.limits": "Make sure resources.limits is set in the Pod YAML",
    "调度器cache中pod node和已绑定pod的数量": "Number of Pods, Nodes and bound Pods in the scheduler cache",
    "调度器驱逐容器的次数": "Number of containers evicted by the scheduler",
    "调度插件在每个扩展点的执行时间,90分位": "P90 execution time of scheduler plugins at each extension point",
    "调度算法+绑定 90分位耗时": "P90 duration of scheduling algorithm plus binding",
    "调度算法90分位耗时": "P90 scheduling algorithm duration",
    "调度队列中pending pod的数目": "Number of pending Pods in the scheduling queue",
    "过去10s的CPU负载": "CPU load over the last 10s",
    "队列中最耗时任务的处理时间（500ms采样周期，未完成的任务-任务开始处理即出队的时间)": "Processing time of the longest-running work item in the queue (500ms sampling period, unfinished items, timed from the moment the item is dequeued and starts processing)",
    "阿里ACK K8s": "Alibaba ACK K8s",
    "集群容量：CPU Cores": "Cluster Capacity: CPU Cores",
    "集群容量：Ephemeral Storage": "Cluster Capacity: Ephemeral Storage",
    "集群容量：Memory": "Cluster Capacity: Memory",
    "非运行状态Pod数量": "Non-Running Pod Count"
}
