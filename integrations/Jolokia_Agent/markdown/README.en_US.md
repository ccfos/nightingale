# Jolokia Agent

forked from telegraf/inputs.jolokia2_agent

## Disabling this plugin

- Option 1: rename the `input.jolokia_agent_misc` directory to something that does not start with `input.`
- Option 2: leave the configuration in xx.toml empty
