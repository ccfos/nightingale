{
    "1) 先确认是否是计划内变更；\n2) 非计划则执行 journalctl -u slapd --since '-30min' 定位退出原因；\n3) 执行 dmesg -T | grep -i 'killed process' 确认是否被 OOM 杀掉，mdb 后端的 olcDbMaxSize 配置过大是常见诱因；\n4) 重启后执行 ldapsearch -x -b '' -s base namingContexts 验证服务已正常提供，并确认副本同步已追平。": "1) First confirm whether this was a planned change;\n2) If unplanned, run journalctl -u slapd --since '-30min' to find the cause;\n3) Run dmesg -T | grep -i 'killed process' to check for an OOM kill; an oversized olcDbMaxSize on the mdb backend is a common trigger;\n4) After the restart verify service with ldapsearch -x -b '' -s base namingContexts and confirm replicas have caught up.",
    "1) 执行 ldapsearch -x -D \u003cbind_dn\u003e -w \u003cpw\u003e -b 'cn=Connections,cn=Monitor' 查看当前连接明细，定位连接数最多的客户端 IP；\n2) 大量连接来自同一应用，说明它没有复用连接或没有关闭连接，推动应用侧改用连接池；\n3) 应急可调大 slapd 的 ulimit -n（systemd 下改 LimitNOFILE）并重启；\n4) 同步检查 olcIdleTimeout 是否配置，未配置时空闲连接会一直占用描述符。": "1) Run ldapsearch -x -D \u003cbind_dn\u003e -w \u003cpw\u003e -b 'cn=Connections,cn=Monitor' to list current connections and find the client IP holding the most;\n2) Many connections from a single application means it is not reusing or closing connections; push the application team to use a connection pool;\n3) As a stopgap raise slapd's ulimit -n (LimitNOFILE under systemd) and restart;\n4) Also check whether olcIdleTimeout is configured, since without it idle connections hold descriptors forever.",
    "1) 执行 ldapsearch -x -D \u003cbind_dn\u003e -w \u003cpw\u003e -b 'cn=Operations,cn=Monitor' 对比各类操作的 Initiated 与 Completed，定位卡住的操作类型；\n2) 卡在 Modify/Add 的检查后端数据库锁与磁盘 IO（iostat -x 1），mdb 后端写放大时容易阻塞；\n3) 卡在 Search 的按慢查询思路补索引；\n4) 若是复制（syncrepl）拖住主库，检查各 consumer 的连通性与延迟，必要时先摘掉异常 consumer。": "1) Run ldapsearch -x -D \u003cbind_dn\u003e -w \u003cpw\u003e -b 'cn=Operations,cn=Monitor' and compare Initiated with Completed per operation type to find where things are stuck;\n2) If Modify/Add are stuck, check backend database locks and disk IO (iostat -x 1); the mdb backend blocks easily under write amplification;\n3) If Search is stuck, follow the slow-query path and add indexes;\n4) If replication (syncrepl) is holding the provider back, check the connectivity and lag of each consumer and detach a broken consumer if necessary.",
    "1) 执行 ldapsearch -x -D \u003cbind_dn\u003e -w \u003cpw\u003e -b 'cn=Operations,cn=Monitor' 看哪类操作（Search/Bind/Modify）量最大；\n2) Search 占大头时检查慢查询：在 slapd.conf 打开 loglevel stats 后从日志里找 SRCH 耗时高的过滤器，给对应属性建索引（olcDbIndex）；\n3) 索引补完执行 slapindex 重建（需先停 slapd）；\n4) 确认不是索引问题则调大 olcThreads（默认 16），并评估 CPU 核数是否成为瓶颈。": "1) Run ldapsearch -x -D \u003cbind_dn\u003e -w \u003cpw\u003e -b 'cn=Operations,cn=Monitor' to see which operation type (Search/Bind/Modify) dominates;\n2) When Search dominates, hunt slow queries: enable loglevel stats in slapd.conf and find the SRCH filters with high duration in the log, then index the matching attributes (olcDbIndex);\n3) After adding indexes rebuild them with slapindex (slapd must be stopped first);\n4) If indexing is not the problem, raise olcThreads (default 16) and check whether CPU core count has become the bottleneck.",
    "1) 执行 ldapsearch -x -D \u003cbind_dn\u003e -w \u003cpw\u003e -b 'cn=Threads,cn=Monitor' 确认 Active/Max 的实时值；\n2) 打开 stats 日志定位慢操作，重点看是否存在无索引的 substring 过滤（如 (cn=*abc*)）导致全库扫描；\n3) 给高频过滤属性补 olcDbIndex 并 slapindex 重建，这是最有效的降负载手段；\n4) 索引已完备仍饱和的，调大 olcThreads 或增加只读副本（syncrepl consumer）分流查询。": "1) Run ldapsearch -x -D \u003cbind_dn\u003e -w \u003cpw\u003e -b 'cn=Threads,cn=Monitor' to see the live Active and Max values;\n2) Enable stats logging to locate slow operations, paying particular attention to unindexed substring filters such as (cn=*abc*) that force a full scan;\n3) Add olcDbIndex for the frequently filtered attributes and rebuild with slapindex; this is the most effective way to shed load;\n4) If indexing is already complete and it is still saturated, raise olcThreads or add read-only replicas (syncrepl consumers) to offload queries.",
    "OpenLDAP 操作完成数落后于发起数": "OpenLDAP completed operations lagging behind initiated",
    "OpenLDAP 线程池饱和": "OpenLDAP thread pool saturated",
    "OpenLDAP 请求积压": "OpenLDAP requests backing up",
    "OpenLDAP 近期发生过重启": "OpenLDAP restarted recently",
    "OpenLDAP 连接数逼近文件描述符上限": "OpenLDAP connections approaching the file descriptor limit"
}
