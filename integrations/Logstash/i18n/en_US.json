{
    "1) 执行 curl -s localhost:9600/_node/stats/jvm?pretty 对比 old collection_count 与 collection_time_in_millis 的增速；\n2) 先按堆内存告警的思路调小 pipeline.batch.size 并检查有状态 filter；\n3) 用 jmap -histo:live \u003cpid\u003e | head -30 看堆里对象分布，异常时通常是某类事件对象堆积；\n4) 调整后观察 old GC 频率是否回落，仍高则考虑拆分 pipeline 或横向扩容 Logstash 实例。": "1) Run curl -s localhost:9600/_node/stats/jvm?pretty and compare the growth of old collection_count and collection_time_in_millis;\n2) Start with the heap alert path: lower pipeline.batch.size and check stateful filters;\n3) Run jmap -histo:live \u003cpid\u003e | head -30 to see the object distribution; when abnormal it is usually a build-up of event objects;\n4) After tuning, confirm the old GC rate falls; if it does not, split the pipeline or scale Logstash horizontally.",
    "1) 执行 curl -s localhost:9600/_node/stats/jvm?pretty 确认 heap_used_percent 与 GC 次数；\n2) 最常见诱因是 pipeline.batch.size 配得过大，先按 batch.size × worker 数 × 单事件大小估算内存占用并调小；\n3) 检查是否有 aggregate、ruby 等在内存里缓存大量状态的 filter，限制其缓存规模；\n4) 确认配置合理后再调大 jvm.options 里的 -Xmx，一般不超过物理内存的一半且不超过 8GB。": "1) Run curl -s localhost:9600/_node/stats/jvm?pretty to confirm heap_used_percent and GC counts;\n2) The most common trigger is an oversized pipeline.batch.size; estimate memory as batch.size x worker count x average event size and lower it;\n3) Check for filters that cache large amounts of state in memory (aggregate, ruby) and cap their cache size;\n4) Only once the configuration is sane should you raise -Xmx in jvm.options, generally staying under half of physical memory and under 8 GB.",
    "1) 执行 curl -s localhost:9600/_node/stats/pipelines?pretty 对比各 plugin 的 duration_in_millis，找出耗时占比最高的那个；\n2) 耗时集中在 output 的先查下游（Elasticsearch 写入拒绝率、bulk 队列），下游是根因时优化下游而不是 Logstash；\n3) 耗时集中在 filter 的重点看 grok：把复杂正则改成 dissect 或加锚点，避免回溯爆炸；\n4) 单机 CPU 已打满则调大 pipeline.workers 无效，需要横向扩容实例。": "1) Run curl -s localhost:9600/_node/stats/pipelines?pretty and compare duration_in_millis across plugins to find the biggest contributor;\n2) If the time is concentrated in an output, investigate the downstream first (Elasticsearch write rejection rate, bulk queue); when the downstream is the root cause, fix it there rather than in Logstash;\n3) If it is concentrated in filters, focus on grok: replace complex regexes with dissect or add anchors to avoid catastrophic backtracking;\n4) If the host CPU is already saturated, raising pipeline.workers will not help and you need to scale out.",
    "1) 执行 curl -s localhost:9600/_node/stats/pipelines?pretty 找出 CPU 消耗最大的 pipeline 与 plugin；\n2) 用 jstack \u003cpid\u003e 抓栈，大量线程停在正则相关方法说明是 grok 开销；\n3) 优化 filter 后仍高的，确认 pipeline.workers 是否超过实际 CPU 核数导致上下文切换开销；\n4) 单机确实到顶则横向扩容并在采集端做负载分流。": "1) Run curl -s localhost:9600/_node/stats/pipelines?pretty to find the pipeline and plugin burning the most CPU;\n2) Capture stacks with jstack \u003cpid\u003e; many threads inside regex methods point to grok overhead;\n3) If it is still high after tuning filters, check whether pipeline.workers exceeds the real CPU core count and is causing context-switch overhead;\n4) If the host is genuinely at its limit, scale out and spread the load at the collection layer.",
    "1) 执行 curl -s localhost:9600/_node/stats/pipelines?pretty 找出 events.out 不增长的 pipeline；\n2) 对该 pipeline 看各 plugin 的 events 明细，卡住的通常是 output（Elasticsearch 拒写、Kafka 不可达）；\n3) 查看 logstash-plain.log 里的 retry/backoff 报错，确认下游是否 429 或连接超时；\n4) 下游恢复后观察 events.out 是否回升，仍不动则重启 Logstash 并检查持久化队列是否损坏。": "1) Run curl -s localhost:9600/_node/stats/pipelines?pretty to find the pipeline whose events.out is not increasing;\n2) For that pipeline, look at the per-plugin event breakdown; the blockage is usually in an output (Elasticsearch rejecting writes, Kafka unreachable);\n3) Check logstash-plain.log for retry/backoff errors and confirm whether the downstream is returning 429 or timing out;\n4) After the downstream recovers, watch events.out come back; if it stays flat, restart Logstash and check whether the persistent queue is corrupted.",
    "1) 执行 curl -s localhost:9600/_node/stats/pipelines?pretty 按 plugin 的 duration_in_millis 排序定位慢插件；\n2) grok 慢是最常见原因，用 --config.debug 配合小样本压测，把宽泛正则替换为 dissect；\n3) 涉及外部查询的 filter（jdbc_streaming、elasticsearch、dns）要确认已开启缓存并设置超时；\n4) 优化后用同一批样本回归对比 duration，确认确实降下来。": "1) Run curl -s localhost:9600/_node/stats/pipelines?pretty and sort plugins by duration_in_millis to find the slow one;\n2) Slow grok is the usual cause: load-test with --config.debug on a small sample and replace broad regexes with dissect;\n3) Filters that make external lookups (jdbc_streaming, elasticsearch, dns) must have caching enabled and timeouts set;\n4) After tuning, re-run the same sample set and compare duration to confirm the improvement is real.",
    "Logstash JVM 堆内存使用率过高": "Logstash JVM heap usage too high",
    "Logstash 入队等待时间过长": "Logstash queue push latency too high",
    "Logstash 单事件处理耗时过长": "Logstash per-event processing time too long",
    "Logstash 管道停止输出": "Logstash pipeline stopped emitting",
    "Logstash 老年代 GC 频繁": "Logstash old generation GC too frequent",
    "Logstash 进程 CPU 使用率过高": "Logstash process CPU usage too high"
}
