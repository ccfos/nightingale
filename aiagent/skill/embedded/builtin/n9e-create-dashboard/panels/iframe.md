# iframe — 内嵌页面

嵌入外部网页。

## custom 配置

```json
{
  "version": "3.4.0",
  "src": ""
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `src` | string | 嵌入的 URL(支持 `$variable_name` 变量替换) |

## 推荐布局

按需调节，常用 `h=12, w=24`（全宽）

## 适用场景

嵌入 Grafana 面板、外部监控页面、第三方报表
